1. In the code from Listing 14, ChatGPT uses inline styles to modify the header logo. What style property sets the size of the top margin?

margin-top

1. What is the id of the main div containing the main image and login form on the Zoom clone? (don't include anything before the '=')

app