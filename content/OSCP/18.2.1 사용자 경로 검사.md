1. Connect to VM 1 and repeat the steps learned in this section. Which command is used to list sudoer capabilities for a given user?

sudo -l

1. Connect to the VM 2 machine with the provided credentials and try to get the flag that resides under another user's file.