1. Follow the steps in this section to set up a remote dynamic port forward from CONFLUENCE01. Scan ports 9050-9100 on MULTISERVER03 through it. Which port is open? (Note: Make sure to scan MULTISERVER03 on its internal interface at 10.4.X.64).

2. Capstone Lab: Download the ssh_remote_dynamic_client binary from the Resources section. If you're running the aarch64 build of Kali, download the ssh_remote_dynamic_client_aarch64 binary. Run the binary against the port you just found on MULTISERVER03 through the remote dynamic port forward.

Note: the source files used to build the ssh_remote_dynamic_client and ssh_remote_dynamic_client_aarch64 binaries can be downloaded from /exercises/client_source.zip on CONFLUENCE01.