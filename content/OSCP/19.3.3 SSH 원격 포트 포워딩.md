1. Start VM Group 1 and follow the example from this section. What's the value of the flag found in the hr_backup database payroll table?

2. Start VM Group 2. Download the ssh_remote_client binary from the Resources section. If you're running the aarch64 build of Kali, download the ssh_remote_client_aarch64 binary. Create an SSH remote port forward on CONFLUENCE01 that allows you to run the binary against port 4444 on PGDATABASE01 from your Kali machine.

Note: the source files used to build the ssh_remote_client and ssh_remote_client_aarch64 binaries can be downloaded from /exercises/client_source.zip on CONFLUENCE01.