---
tags:
  - type/audit
  - platform/pg
manual_tags: true
manual_cves: true
---
# 웨이브 13 — `_PLAYBOOK.md` 단독 반영 기록

대상: `03. PG\_PLAYBOOK.md` **4584 → 5124행**(+540).
입력 제안: `Exfiltrated-playbook.md`(7) · `PwnLab-playbook.md`(13) · `Nagoya-playbook.md`(12) · `PlanetExpress-playbook.md`(6) = **38건**.
백업: `03. PG\_backup\_PLAYBOOK.md.bak9`(반영 «전» 판본).
반영자: writeup-auditor(단독 기록). 2026-08-26.

---

## 1. 배정한 신규 번호 (13개)

| 번호 | 제목 | 출처 박스 |
|---|---|---|
| `A-1-23` | 확장자 사전을 늘려도 새 경로가 안 나온다 — 라우팅형 앱은 파일이 아님 | Exfiltrated + Nagoya **통합** |
| `A-1-24` | 정찰 페이지가 방금 «내가 보낸 값»을 되비친다 | PlanetExpress |
| `A-3-12` | `event not found` — 히스토리 확장이 `!` 를 먹고 «줄 전체»를 폐기한다 | PwnLab(bash) + Nagoya(zsh) **통합** |
| `A-4-13` | root 는 잡았는데 `cat` 이 «조용히» 아무것도 안 뱉는다 — 오염된 PATH 가 상속됨 | PwnLab |
| `A-52` | 자격증명은 맞는데 «어떤 대화형 서비스도» 로그인을 안 받는다 | Nagoya |
| `A-53` | DC 에서 `whoami` 가 SYSTEM 이 아니라 `DOMAIN\HOST$` 로 나온다 — 실패가 아님 | Nagoya |
| `A-6-10` | 도구를 `~/git/` 에서 직접 실행하는 습관이 매번 경로·아키텍처 탐색 비용을 만든다 | Nagoya |
| `B-1-38` | LFI 진입점이 한 앱에 «두 개» 있을 수 있다 — 하나는 소스 전용, 하나는 실행 가능 | PwnLab |
| `B-3-12` | SUID 가 절대경로 없이 외부 명령을 부르면 PATH 하이재킹이 된다 | PwnLab |
| `B-3-13` | Go 정적 SUID 바이너리는 `strings` 가 아니라 «심볼»로 읽는다 | PlanetExpress |
| `B-46` | `SeImpersonatePrivilege : Enabled` + Spooler 생존 → PrintSpoofer | Nagoya |
| `B-55` | 웹 정찰이 곧 스프레이 재료다 — 실명은 계정명, 저작권 연도는 비밀번호 후보 | Nagoya |
| `B-56` | Kerberoast 로 깬 비밀번호가 로그인이 안 되면 «티켓 재료»로 쓴다 | Nagoya |

배정 규칙 — 각 카테고리의 현재 최대 번호 다음. 9 초과분은 하이픈 추가(`A-1-23`·`A-3-12`·`A-4-13`·`A-6-10`·`B-1-38`·`B-3-12`·`B-3-13`). 두 자리로 남은 것은 아직 9 이하인 카테고리(`A-52`·`A-53`·`B-46`·`B-55`·`B-56`).
**기존 항목의 제목·번호는 하나도 바꾸지 않음.**

## 2. 병합(append)한 곳 — 24곳

`A-12` · `A-14` · `A-16` · `A-1-10` · `A-2-14` · `A-24` · `A-25` · `A-31` · `A-41`(3건) · `A-51` · `B-1-20` · `B-1-30` · `B-1-36` · `B-1-37` · `B-31`(⑷~⑺ 신설) · `B-32` · `B-33` · `B-53` · `B-66` · `B-69` · `B-71` · `B-81` · `B-82` · `C-2`(2건) · `D`(Nagoya 시간표 + 박스별 표 3행) · `E`(Metasploit 미사용 3박스).

**기존 문장은 하나도 지우거나 고치지 않음. 전부 뒤에 덧붙임.** 기존 서술의 반증 사례 없음.

## 3. 삭제한 것

**없음.** 이번 반영은 전량 append. 되살릴 원문이 발생하지 않음.

## 4. 반증한 것

### 4-1. 「신규」로 올라왔으나 실제로는 «기존 항목»이었던 것 — 3건

| 제안 | 실제 | 처리 |
|---|---|---|
| Exfiltrated 제안 7 — 「확장자 블랙리스트 우회 — Apache handler」 **신규 `B-1-2x`** | `B-1-36`(인증 후 파일 업로드 → RCE)의 **우회 사다리 2번**에 이미 `.phar`·`.phtml` 과 Debian 정규식 경고가 있음. `[[Exfiltrated]](.phar)` 도 「같은 계열」에 이미 인용돼 있었음 | 새 카드를 만들지 않고 `B-1-36` 에 **정규식 원문 · `7*6` 프로브 · `mime: text/x-php` 증거 · 판정 규칙**만 append |
| PlanetExpress 제안 2 — 「php-fpm RCE 는 docroot 문제」 **신규 `A-2x`** | `A-2-14`(웹셸을 심을 웹루트를 «틀린 곳»에 골랐다)가 정확히 같은 증상 카드임 | `A-2-14` 에 append |
| PwnLab 제안 9 후반 — 「`php://filter` 확장자를 붙일지는 그 코드가 결정」 | `B-1-30` 의 「막힐 때 순서대로」 ⓐ에 **이미 동일 내용**이 있음 | 중복 삽입하지 않고 `B-1-30` 출처 줄에 PwnLab 케이스만 한 줄 추가. 제안 9 의 **전반부(싱크가 두 개)만** 신규 `B-1-38` 로 |

### 4-2. 서로 다른 박스의 제안 «둘»이 같은 카드였던 것 — 2쌍

- **Exfiltrated 제안 6 + Nagoya 제안 4** — 둘 다 「확장자 사전을 늘려도 소용없다, 라우팅이지 파일이 아니다」. front-controller CMS(Subrion)와 ASP.NET Core MVC 는 **같은 부류**임. 카드를 둘 만들면 시험장에서 어느 쪽이 맞는지 알 수 없어짐 → **`A-1-23` 하나로 통합**(ⓐ와일드카드 중단 / ⓑ확장자 무의미 / .NET 판 / `.htaccess` 크기 판별).
- **PwnLab 제안 4 + Nagoya 제안 11** — 둘 다 히스토리 확장이 `!` 를 먹는 문제(bash `#!` vs zsh 비밀번호). **원인이 동일**하므로 → **`A-3-12` 하나로 통합**(판 ⓐ·ⓑ).

### 4-3. 총괄 지시 중 확인 결과 틀렸던 것 — 1건

**「PlanetExpress 제안의 자기 검산이 틀렸다. §6 하위 절은 실제로 7개」** → **반증됨.**
`_backup\PlanetExpress.md.bak`(개작 직전, Aug 26 13:25) 의 §6 하위 헤딩은 **8개**임 — (1)·(2)·(3)·(4)·「헛다리 relayd `-C`/`-P`」·「헛다리 gobuster」·**「안 해본 것」**·**「관측 없음」**.
그중 뒤의 둘은 **이관 대상이 아니라 노트 잔류분**이고, 실제로 **둘 다 현재 노트에 살아 있음**:
- 「관측 없음」 표 → 현재 `PlanetExpress.md` `### 관측 없음 — 박스 정지로 재수집 불가` 로 그대로 존재
- 「안 해본 것」 3항목(relayd `-a`/`-r`/`-i`/`-U`, Pico CMS 2.1.4 + Twig SSTI, `/etc/shadow` sha512 룰·다른 사전) → **같은 「관측 없음」 표에 3행으로 흡수돼 있음**

즉 **이관 대상 하위 절은 6개가 맞고 제안의 「6개 단위 = 6건 1:1」 검산이 정확했음.** 손실 없음.

### 4-4. 제안이 절을 잘못 지목한 것 — 1건

PwnLab 제안 10 이 「`D. 시간 배분`의 Metasploit 문장」을 지목했으나, 「Metasploit 대안이 있어도 일부러 안 쓰는 판단이 시험 전략임」은 **`E. OSCP 시험 규정` 절**에 있음. → `E` 절에 반영.

### 4-5. 총괄이 준 확정 사실 ⑦ 대조 결과

- **PlanetExpress 443 차단은 단정이 정확함** — 제안 3 이 이미 표에 `OUTPUT DROP` 으로 단정해 두었고 `[가정]` 표기가 없었음(고칠 것 없었음). 다만 근거가 본문에 없어 **`root_enum_fw.log`(4197B)의 `-A OUTPUT -j DROP` + 53/80/9000 ACCEPT + DROP 카운터 `131K packets`** 를 「추론이 아니라 규칙 원문」이라고 명시해 넣음
- **Exfiltrated pty 프롬프트를 「창작」으로 적은 제안은 없었음** — 고칠 것 없음
- **PwnLab `┌──(kali㉿kali)` 는 playbook 에 한 건도 옮기지 않음.** A-1-10 은 로그 경로 인용으로 처리했고, A-3-12·A-4-13 에 실린 `kane@pwnlab:/tmp$`·`root@pwnlab:/tmp#` 는 **타겟 pty 실측**이라 그대로 보존
- **Nagoya 는 `부분 1/2`** — 제안 12 도 「완료」로 적지 않았음. `D` 절 신규 서술과 박스별 표에 **`부분 1/2`(proof.txt 만, local.txt 미확보)** 로 명시

## 5. `.bak` 직접 대조에서 «제안에 없던» 손실 — 4건 (전부 반영)

제안을 믿지 않고 4개 `.bak`(Exfiltrated `.bak2` Aug 26 13:23 · PwnLab `.bak` Aug 26 13:27 · Nagoya `.bak2` Aug 26 13:23 · PlanetExpress `.bak` Aug 26 13:25)의 삭제 장(0장·5장·6장·7장) 전문을 직접 읽어 대조함.

| # | 잃을 뻔한 것 | 원본 위치 | 반영처 |
|---|---|---|---|
| 1 | **막힌 지점별 분기 계획 3행** — 「`/panel` 기본 자격증명 실패 시」(사용자명 열거 → 소수 후보 스프레이 / `/forgot/` 로직 / `/install/`·`/updates/` 재실행) · 「업로드 확장자 전부 차단 시」 · 「웹셸은 됐는데 크론이 없었다면」. 제안 3 은 **마지막 행(크론 우선순위)만** 반영했음 | `Exfiltrated.md.bak2` §6-⑩ 표 | **`B-1-37` 말미에 「기본값 10개가 전부 실패했을 때의 다음 후보」 5단계로 신설** |
| 2 | **「결과가 즉시 안 나오는 익스플로잇은 «관측 채널»을 먼저 확보한다 / 채널이 하나뿐이면 실패 원인을 좁힐 수 없다」** 일반 규칙 | `Exfiltrated.md.bak2` §6-⑧ 말미 | **`B-31` ⑸ 에 편입**(관측 채널 3개 예시 포함) |
| 3 | **「`ls -l /bin/bash` = 네트워크와 «무관한» 성공 판정 채널」** — 제안이 §6-⑨ 전체를 「A-31 중복」으로 기각했으나 **A-31 에 없는 내용**이었음 | `Exfiltrated.md.bak2` §6-⑨ 표 ①행 | **`B-31` ⑸ 에 편입** |
| 4 | **「base64 는 해시가 아니다 — 크랙 도구를 꺼내기 전에 인코딩인지부터 본다」** — 어느 제안에도 없었음 | `PwnLab.md.bak` §0·§7-4 | **`B-66` 에 append**(`$` 없음 + 4의 배수 + `=` 종결 = base64) |

**손실 아님으로 확인한 것**(부재 근거로 잘못 지적할 뻔한 것):
- Exfiltrated §7-1 `/etc/hosts` 등록 · §7-2 `robots.txt` 우선 — **현재 노트 본문 74·77·80행에 살아 있음.** playbook `A-23` 목록에도 이미 있어 신규 불필요
- PwnLab 「다른 경로였다면」(SUID exim4 4.84.2 백포트 · 커널 3.16) — **현재 노트 579~589행에 「배제한 대안 경로」로 살아 있고 `[가정]`·「배제가 아니라 미검증」 표기까지 보존됨**
- Nagoya 「수동 대안」 표 7행 — **전부 현재 노트의 finding 산문에 「수동 대안」 문단으로 흡수됨**(265·335·521·750행). 제안 검산이 정확했음
- Nagoya §5 「local.txt 는 기록에 없다」 → 현재 노트 35·365행에 `partial`(1/2) 근거로 보존됨

## 6. 기각한 것 — 6건

| 기각 | 사유 |
|---|---|
| Exfiltrated §7-9 도구 규정 표 | `E` 절과 완전 동일. 새 정보 0 |
| Exfiltrated §7-12 시간 배분 표 → `D` 절 박스별 행 | **근거부족.** `~/PG/Exfiltrated/` 에 `proof_user.txt`/`proof_root.txt` 가 없어 **종료 시각을 mtime 으로 확정할 수 없음.** nmap 08:50:32 → gobuster_files 09:00:22(약 10분)까지만 실측 가능. 박스별 표에 Exfiltrated 행을 넣지 않음 |
| Exfiltrated §6-⑨ 의심 순서 표 (①행 제외) | `A-31` 이 이미 리스너 생존·포트 교체·tun0 IP·dash 문법을 전부 다룸 |
| PwnLab §7-3 「`curl -F …;type=` 로 끝난다」 | `B-1-20` 신설 표의 「우회」 열이 같은 내용을 더 구조적으로 담음 |
| PwnLab §8 방어 관점 · Nagoya §8 · Exfiltrated §8 | 각 노트의 `Vulnerability Fix:` 로 이미 이관 완료(제안 검산대로) |
| PlanetExpress §7-12 「20분 안에 foothold 안 보이면 재열거」 | `D` 절 일반 원칙과 중복. 제안도 스스로 제외를 권고했고 동의 |

## 7. 앵커 추가 — 4개 노트, 26개

`## 관련` 절에만 손댔고 본문은 건드리지 않음. 노트에 `<박스>-playbook.md` 경로를 남기지 않음.

- `Exfiltrated.md` — 7개(`A-1-23`·`A-12`·`A-14`·`B-31`·`B-1-36`·`B-81`·`B-82`)
- `PwnLab.md` — 5개 추가(`A-3-12`·`A-4-13`·`B-1-38`·`B-3-12`·`A-25`) — 기존 6개 유지
- `Nagoya.md` — 10개(`A-52`·`A-53`·`B-55`·`B-56`·`B-46`·`A-1-23`·`A-3-12`·`A-6-10`·`A-51`·`B-53`)
- `PlanetExpress.md` — 6개(`A-1-24`·`B-3-13`·`A-31`·`A-16`·`A-2-14`·`B-33`)

## 8. 전수 검증 결과 — 전부 통과

| 항목 | 결과 |
|---|---|
| 코드펜스 짝 | **430개, 균형** |
| 헤딩 총수 | 227 |
| 헤딩에 파이프 문자 포함 | **0** |
| 제목 중복 | **0** |
| 항목 번호 중복 | **0**(항목 188개) |
| 박스 노트의 `[[_PLAYBOOK#…]]` 앵커 | **372개 검사 / 깨진 것 0** |

⚠️ 볼트 전역 스캔에서 `_AUDIT\*` · `.claude\agents\*` · `_WRITEUP-STANDARD.md` 의 참조 22건이 미해결로 잡히나 **전부 `A-31`·`B-84` 같은 번호 약칭이거나 예시 문자열(`...`)이라 앵커 대상이 아님.** 발행되는 박스 노트에는 하나도 없음.

## 9. 총괄 판단이 필요한 것

1. **색인 갱신 필요** — `_PLAYBOOK.md` 는 수동 프론트매터 보존 대상이라 `extract.py` 가 건너뛰지만, 박스 노트 4개(`Exfiltrated`·`PwnLab`·`Nagoya`·`PlanetExpress`)의 `## 관련` 이 바뀌었음. `refresh.ps1` 은 규율대로 **돌리지 않았음**.
2. **`D` 절 말미의 「공통 패턴 — 위 «넷» 중 셋이」** 문장이 박스별 표 행 수(현재 17행)와 안 맞음. **이번 반영 이전부터 있던 불일치**이고 내 증거로 반증된 것이 아니라 손대지 않음. 정정 여부는 총괄 판단.
3. **Exfiltrated 의 `proof_user.txt`/`proof_root.txt` 부재** — 시간표를 `D` 절에 넣지 못한 이유. 박스가 이미 정지돼 재수집 불가.
