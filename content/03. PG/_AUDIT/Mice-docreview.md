# Mice.md 문서 검토 (축② 공개 적합성 · 축③ 한국어) — 2026-08-21

축① 문체 개조식 전환은 **수행하지 않았다**.

## 보존 검산

| | 전 | 후 |
|---|---|---|
| 코드펜스 | 44 | **44** |
| 인식론적 표시 `[가정]` | 3 | 2 |
| 본문 `[가정]` (삭제한 감사 블록 제외) | 2 | **2** |
| 바이트 | 37,469 | 35,094 |
| 행 | 506 | 489 |

감소한 1건은 삭제한 감사 표 안의 「리버스셸 차단은 정황 추론이라 `[가정]` 으로 강등」이다. 본문의 `[가정]` 2건(6장 평문 리버스셸 추론, 6장 트레이 아이콘 렌더링 실패)은 그대로다.

## 고친 것

| 위치 | 분류 | 전 → 후 |
|---|---|---|
| 2장 CVE 콜아웃 | 축②(초고 언급 제거, 사실 보존) | 「**이 노트 초고는** foothold 를 **CVE-2021-43326** 이라고 적었다. NVD 에서 그 번호는 …」 → 「foothold 를 **CVE-2021-43326** 으로 **적기 쉽다.** 그런데 NVD 에서 그 번호는 …」 |
| 6장 첫 줄 | 축③ | 「시간의 대부분이 **들었다**」 → 「시간의 대부분을 **썼다**」 |
| 6장 원시 캡처 절 | 축②(에이전트·하네스) | 「그 시점에 **러너의** Kali SSH 가 **세션 안전분류기 발동으로** 끊겨 있었다」 → 「그 시점에 Kali SSH 가 끊겨 있었다」 |
| 6장 원시 캡처 절 | 축③(오해 소지) | 「**탈출** 시도도 실패했다」 → 「**빼내려는** 시도도 실패했다」 (문맥은 SYSTEM cmd 출력의 반출) |
| 7장 1번 | 축②(초고 언급 제거, 사실 보존) | 「— **이 노트가 처음에 붙였던** CVE-2021-43326 은 전혀 다른 제품**이었다**」 → 「— **여기 붙이기 쉬운** CVE-2021-43326 은 전혀 다른 제품**이다**」 |
| 남긴 흔적 | 축②(에이전트·하네스) | 「경위부터 적는다. **러너 잘못이 아니다.** 세션 중 **안전분류기가 발동해** Kali SSH 를 세션 내내 잃었고 … Kali 쪽은 **라인 관리자가 대신** 정리했다」 → 「경위부터 적는다. 세션 중 Kali SSH 를 세션 내내 잃었고 … Kali 쪽은 **나중에 따로** 정리했다」 |

지시대로 「CVE-2021-43326 은 Remote Mouse 와 무관하다」라는 **기술적 사실은 두 곳 모두 그대로 남겼고**, 뺀 것은 「초고가 그렇게 적었다」라는 작성 이력뿐이다. 남긴 흔적 절도 정리 방법(세션 이름으로만 종료, `ss` 로 PID 특정, `pkill` 미사용)과 「타겟 쪽 정리 불가」라는 결과는 한 글자도 바뀌지 않았다.

## 삭제한 것 — 원문 전문

frontmatter 직후의 「적대적 검증 정정 이력」 콜아웃 전체(16행). 표의 기술적 판정은 전부 본문에 이미 반영돼 있다 — CVE-2022-3365 확정→요약·2장·9장·frontmatter, `/a` 스테이저→3장, egress 프로브→3장, `e6.ps1`→4장, 세션 1 판정→6장, AMSI/PrivescCheck→6장, `icacls C:\`→6장, 스크린샷 1차 증거→4장·5장.

```
> [!warning] 적대적 검증 정정 이력 (2026-08-21)
> 초고를 산출물(`~/PG/Mice/`)·스크린샷 4장·NVD·Kali 실행으로 대조해 아래를 정정했다.
>
> | 위치 | 무엇이 틀렸나 | 근거 |
> |---|---|---|
> | 요약·2장·9장·frontmatter | foothold CVE 를 **CVE-2021-43326** 이라 적음. 그 번호는 **Automox Agent** 취약점이다 | NVD. 올바른 번호는 **CVE-2022-3365**, PoC 는 EDB 46697 — Metasploit `remote_mouse_rce` 가 둘을 같이 참조 |
> | 3장 | foothold 명령이 `iex(irm .../o.ps1)` 로 적힘 | `http.log` — 셸을 연 것은 `/a`. `o.ps1` 은 22:42 에 받은 프로세스 열거 스크립트다 |
> | 3장 | "4444 는 SYN 조차 나가지 않았다" (tcpdump 근거 없음) | 온타겟 egress 프로브 결과가 `http.log` 에 그대로 남아 있어 그것으로 교체 |
> | 4장 | `ValidateCredentials` 를 대화형으로 친 것처럼 적힘 | 실제로는 `www/e6.ps1` 스크립트. 결과(`False`/`True`)는 `shell443.log:671` 에 있어 값 자체는 옳았다 |
> | 4장 | "freerdp·runas 로도 3중 확인" | `runas` 는 어느 산출물·히스토리에도 없다. 삭제 |
> | 6장 | "내 RDP 는 세션 2, RemoteMouse 는 콘솔 세션 3" | `shell443.log:7803` 의 `query session` — divine 은 **세션 1**, GUI 프로세스도 **세션 1**. 세션이 갈린 게 아니었다 |
> | 6장 | AMSI 차단 메시지를 리버스셸 것으로 적음 | 실제로 그 메시지가 붙은 것은 **PrivescCheck(`pc.ps1`)**. 리버스셸 차단은 정황 추론이라 `[가정]` 으로 강등 |
> | 6장 | `icacls "C:\Program Files (x86)"` 결과를 인용 | 그 명령은 돌린 적이 없다. 측정된 것은 `icacls C:\` 이고, 그쪽이 오히려 더 결정적이다 |
> | 4장·5장 | proof.txt 증거를 원시 캡처처럼 제시 | 원시 캡처는 회수 실패(6장). **1차 증거는 스크린샷**이고 코드블록은 그 전사본임을 명시 |
>
> **반증된 지적**(확인해보니 노트가 옳았다): GUI·Core 의 부모가 서비스 PID 라는 4장 서술 · `TESTREBOOT` 흔적 · Metasploit `remote_mouse_rce` 모듈 존재 · Remote Mouse 3.008 버전 판정 · SAM 접근거부·AlwaysInstallElevated 부재. 전부 산출물에 있다.
```

## 넘긴 것 — 사실 의심

없음. 이 노트는 본문과 요약·frontmatter 가 서로 어긋나는 곳이 없었다.

## 손대지 않기로 한 것

- **9장의 「(혼동 주의) CVE-2021-43326 은 Automox Agent 취약점이다」** — 초고 언급이 없는 순수 기술 경고라 그대로 둔다.
- **4장 `PS C:\WINDOWS\system32>` 프롬프트, 6장 `query session` 출력** — 타겟 pty 실측이다. 전량 유지.
- **`![[PG-Mice-*.png]]` 5장** — 5장·6장이 「1차 증거는 스크린샷」이라고 명시하는 근거다.
- **코드펜스 내부 전량** · **Kali/랩 IP** · **`~/PG/Mice/` 출처 캡션**.
