# Hub — 적대적 검증 기록

## 본문에서 이관한 정정 이력

(2026-08-20 이관 — 원래 `Hub.md` 상단에 있던 블록. 원문 그대로.)

> [!warning] 적대적 검증 정정 이력 (2026-08-20)
> 이 노트는 [[_WRITEUP-STANDARD#적대적 검증 — 노트를 쓴 다음 반드시 거친다|적대적 검증]]을 거쳐 **14건**을 정정했다. **실측 터미널 블록은 훼손되지 않았고, 틀린 것은 전부 나중에 덧붙인 설명 문장**이었다.
>
> | 무엇이 틀렸었나 | 무엇이 사실인가 | 반증 근거 |
> |---|---|---|
> | "8082·9999는 기본 1000포트 밖이라 `-p-`를 빼면 시작조차 못 한다" | **둘 다 `nmap-services` 등재 = top-1000 안.** 같은 세션의 `nmap_quick.log`가 `--top-ports 1000`으로 넷 다 잡았다 | `~/PG/Hub/nmap_quick.log` |
> | "CVE-2023-24078과 CVE-2024-27697이 같은 결함을 공유한다" | **별개 취약점.** 실제로 탄 것은 **2023-24078** 계열. 2024-27697은 NVD 미공개이고 공개 PoC 설명상 About 페이지 Lua 주입이다 | NVD |
> | 도메인 `hub.local` | **임의로 지은 값.** 인증서 SAN은 `FuguHub.local` | `~/PG/Hub/nmap.log` |
> | 80/tcp "전부 403" | 6개 중 **4개가 301**(후행 슬래시 리다이렉트) | `~/PG/Hub/ferox_80.log` |
> | `user.dat`이 "구조를 알 수 없는 바이너리" | **순수 ASCII JSON** `{"v":"<base64>"}`. 노트 자신의 판정표로는 **두 번째 범주** | `~/PG/Hub/user.dat` (467바이트) |
> | "확장자를 넓혀 재스캔해서 `bdd.conf`·`user.dat`을 찾았다" | 2차 스캔은 **`-x conf,dat,zip,log,bak,txt`로 이미 돌렸는데도 못 찾았다.** 원인은 확장자가 아니라 **워드리스트에 그 파일명이 없다는 것** | `~/PG/Hub/ferox_80_files.log` |
> | Lua 에러 `field 'popen' is not callable` | 표준 Lua는 **`attempt to call a nil value (field 'popen')`** 을 낸다. 인용 문구는 코어에 없다 | Kali `lua5.4` 실행 |
> | `/rtl/about.lsp`가 무인증 표면 | 본문 4곳이 **인증 후** 접근을 전제한다 → 표를 `401 → 인증 후 200`으로 정정 | 내부 모순 |
> | `nmap`이 `abyss`로 "오인" | `abyss`는 **9999/tcp의 표준 서비스 이름**이고 `?`는 지문 미확정 표시. 오인이 아니라 포트 기반 기본 라벨 | `nmap-services` |
>
> ※ **아이러니를 남겨둔다** — 6장 ②가 "랩 브리핑의 CVE 번호가 틀렸다"고 지적했는데, **그 지적이 제시한 대체 번호(CVE-2024-27697)가 또 틀렸다.** 남의 번호를 의심할 때 자기 번호도 같은 기준으로 확인해야 한다.

---

# 2026-08-26 감사 — 개작본(1133 → 432행) 적대적 검증

작성자: 별도. 감사 범위: `03. PG\Hub.md` 전문 · `~/PG/Hub/` 산출물 9종 · git `ebc992e`(최초판 310행) / `HEAD`(직전판 1133행) · `_PLAYBOOK.md` 앵커 10종.

## 1. 고친 것

| 위치(문자열) | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| frontmatter `services: [http, https, ssh]` | **`manual_services: true` 선언이 빠져 있었다.** 작성자가 「`ssl/abyss` 오탐 제거 + `manual_services: true`」를 반증으로 신고했으나 실제로는 값만 고치고 선언을 안 넣었다. `extract.py:386-392` 는 선언이 없으면 본문을 재스캔하므로, 노트 본문의 `9999/tcp open  ssl/abyss?` 를 다시 긁어 **다음 `refresh.ps1` 에서 `abyss` 가 조용히 되살아난다** | `manual_services: true` 추가 |
| `# Nmap 7.98 scan initiated … nmap_quick.log` 블록 | 11줄짜리 원본에서 **4줄이 생략표시 없이 잘려** 1행 + 6~9행이 이어 붙어 있었다. 잘린 것 중 `Not shown: 996 closed tcp ports (reset)` 은 **「이것이 top-1000 스캔이었다」의 직접 증거**라 노트의 최대 자산(ⓖ)을 스스로 약화시켰다 | `nmap_quick.log` 전량(11줄) 복원. 바이트 일치 확인 |
| `/bdd.conf  200  56 바이트  설치 경로·포트` 3줄 | ```` ```text ```` 펜스 «안»에 「바이트」·「설치 경로·포트」 등 **한국어 해설이 들어간 손제작 표**였다. raw 출력이 아닌데 실측 표식(펜스)을 달고 있었다 | 펜스 밖 마크다운 표로 전환. 56 B·467 B 는 산출물과 일치 확인, 71872 B 는 `[가정]` 유지 |
| `root@debian:…# ls /root/` 다음 줄 | 직전판에 있던 **타겟 pty 프롬프트 1건 + 출력 1줄**(`cat /root/email4.txt` → `MENURkBvZmZzZWMuY29t`)이 세션 캡처 블록에서 삭제되고 나머지가 재봉합돼 있었다. 캡처된 세션에서 줄을 빼는 것은 실측 편집이다 | 두 줄 복원. 단 직전판이 그 값 뒤에 붙여둔 손제작 주석 `# base64 → 0CTF@offsec.com  (미끼)` 는 `cat` 이 낼 수 없는 출력이라 제외하고, 해독 결과는 아래 산문에 유지(`base64 -d` 로 `0CTF@offsec.com` 재확인) |
| `…등 50여 건이 전부 이 302 위양성이었음` | **실제 62건.** `ferox_9999.log` 의 결과 라인 62개 전부가 302, 다른 상태코드 0건 | `결과 62건 전량` 으로 정정 + 근거 명시 |
| 요약 `권한상승: 없음 — FuguHub 서비스가 User=root 로 구동` | 요약이 **미보존 유닛 파일**(본문에서 `[가정]`)을 근거로 단정. 실측 근거는 `ba.exec("id")` → `uid=0` 쪽이다 | `FuguHub 서비스가 root 로 구동. ba.exec("id") 첫 실행이 이미 uid=0(root)` 로 교체. ⚠️ 절 제목은 지시대로 유지 |
| `9999 = 같은 앱의 HTTPS 리스너 … curl -k https://T:9999/ 가 8082 와 동일한 Home 반환` | 후단이 산출물로 뒷받침되지 않음(`ferox_9999.log` 에 루트 결과 라인 없음). **부재이므로 삭제가 아니라 강등** | `[가정 — 산출물 미보존]` 부착. 동시에 확증되는 부분(`bdd.conf sslport=9999` · nmap `ssl-cert CN=FuguHub` · ferox 가 `https://…:9999/` 로 완주)을 근거로 분리해 명시 |
| 세션 캡처 펜스 2곳(`listening on [any] 4444`, `find / -name local.txt`) | 타겟 pty 프롬프트가 든 **세션 캡처인데 `text` 태그**. 표준의 「Kali·타겟 세션 캡처 → `bash`」에 어긋나고, 태그가 곧 증거 분류다 | 여는 펜스 줄만 `bash` 로 변경(블록 내용 무변경) |

## 2. 삭제한 것

**없다.** 이번 감사에서 노트 본문에서 제거한 서술은 0건이다. 유일하게 뺀 문자열은 위 4행의 손제작 인라인 주석 하나이고 원문을 그대로 인용해 남긴다:

```
MENURkBvZmZzZWMuY29t          # base64 → 0CTF@offsec.com  (미끼)
```

(해독 결과는 노트 산문에 그대로 살아 있으므로 정보 손실 0.)

## 3. 반증한 것 — 지적으로 올랐다가 확인 결과 노트가 옳았던 것

| 제기한 지적 | 확인 결과 | 근거 |
|---|---|---|
| **코드블록 바이트 절단(ⓑ, 최우선 의심)** | **반증. 불일치 0건.** 6개 블록 전부 원본과 바이트 일치 — `nmap.log`(46줄) · `ferox_80.log`(9줄, 284~292) · `ferox_8082.log`(6줄, 286~291) · `bdd.conf`(3줄) · `user.dat`(1줄) · 복원 후 `nmap_quick.log`(11줄). 유일한 diff 는 닫는 펜스와 원본 파일의 최종 개행 부재뿐 | `scp` 로 회수 후 md5 일치 확인 → `diff` |
| **`Privilege Escalation` 에 4항목이 없다** | 지적하지 않음(지시대로). 표준 「권한상승이 없는 박스는 4항목을 생략한다」의 정본. 중복 4항목도 없음 | `_WRITEUP-STANDARD.md:194-202` |
| **타겟 pty 프롬프트 6건을 창작으로 볼 것인가** | **반증 — 전부 실측 표식으로 남긴다.** Kali 프롬프트(`└─$` 18건)는 전량 제거됐으나 타겟 프롬프트(`root@debian:…#`)는 §3 이 「반드시 남겨라」로 규정한 쪽이다. 오히려 1건이 «지워져 있어» 복원했다(위 1절) | `CLAUDE.md §3` 표 |
| **`root@debian:…# cat /etc/systemd/system/fuguhub.service` 는 `[가정]` 블록에 붙은 프롬프트이므로 떼야 한다** | **반증 — 유지.** 바로 아래에 `[가정 — 유닛 파일 원문 미보존]` 과 제품 관례 불일치 · 리버트 시 확인 명령이 전부 붙어 있어 독자가 오독할 여지가 없다. 프롬프트 제거는 Fikklish 사고의 재현이 된다 | 노트 406행 자체 |
| **작성자 반증 ② `cves: [CVE-2023-24078]` + `manual_cves: true`** | **참.** 선언·값 모두 반영됨. 본문이 CVE-2024-27697 을 반증용으로 언급하므로 선언이 반드시 필요한 케이스가 맞다 | 프론트매터 18·21행 |
| **작성자 반증 ③ 옛 `ferox_80` 펜스의 재정렬·축약·한국어 주석 삽입** | **참 — 4중으로 확인.** 직전판은 ①403/301 을 상태코드별로 재정렬(원본 순서: 403,403,301,301,301,403,200,200,301) ②`=> http://192.168.248.25/data/` 를 `=> /data/` 로 축약 ③열 간격 임의 변경 ④`← (함정) 8.0 changelog` 한국어 주석 삽입. 개작본은 원본 순서·전체 URL·간격 그대로 | `git show HEAD:"03. PG/Hub.md"` 209~218행 vs `ferox_80.log` 284~292행 |
| **ⓖ 「8082·9999 는 top-1000 안」 정정이 소실됐는가** | **반증 — 전량 생존.** 사실·근거·수치가 모두 남아 있고 `nmap-services` 값도 Kali 원본과 문자 일치(`blackice-alerts 8082/tcp 0.000878` / `abyss 9999/tcp 0.004441`). `서비스명?` 의 `?` = 지문 미확정 서술도 생존. 자기 정정 이력 프레이밍(「이 노트는 원래 …라고 적어놨었다」)은 전부 제거됨 | `grep -E '^(blackice-alerts\|abyss)' /usr/share/nmap/nmap-services` |
| **`_PLAYBOOK` 앵커 10건이 깨졌는가** | **반증 — 10/10 실재.** A-13·A-14·A-15·A-23·A-31·A-38·B-13·B-1-11·B-1-36·B-1-40 전부 헤딩 존재 | `_PLAYBOOK.md` grep |
| **ⓗ 남긴 흔적** | **참 — 완비.** 업로드한 `.lsp` 4개(`t1`·`en`·`sh9`·`x`) · 선점 계정 `admin:password` · 생성된 `user.dat` · Kali tmux `hub` 정리까지 명시 | 노트 `Post-Exploitation` |
| **`nnmap` 별칭 `-oN nmap.log` 와 노트의 `-oN /home/kali/PG/Hub/nmap.log` 불일치** | **반증 — 노트가 옳다.** 노트는 별칭이 아니라 `nmap.log` 파일 1행에 실제로 기록된 명령줄을 그대로 옮겼다 | `nmap.log` 1행 |
| **`feroxbuster … -x php,txt,html,lsp -t 100` 재구성 명령이 창작인가** | **반증 — 로그 `Configuration` 블록과 전 항목 일치**: wordlist `raft-medium-directories.txt` · extensions `php,txt,html,lsp` · threads `100` · output `/home/kali/PG/Hub/ferox_8082.log` · target `http://192.168.248.25:8082/` | `ferox_8082.log` |
| **`MENURkBvZmZzZWMuY29t` → `0CTF@offsec.com` 이 맞는가** | **참.** 직접 디코드 확인 | `echo -n … \| base64 -d` |
| **`raft-medium-files.txt` 에 `bdd`·`user.dat` 이 없다는 노트의 `[가정]` 각주** | **참.** 재실측 0건 | Kali `grep -icE '^(bdd\|user\.dat)$'` |
| **작성자 자기신고 「나머지는 기존 카드 중복」** | **2건에서 틀렸다** — 아래 4절 | — |

## 4. 이관 손실 — 관리자 지시 항목(ⓒ)에 대한 답

- **삭제 ≈700줄 중 워커 제안 파일이 원문 인용으로 덮는 범위 = 223줄**(제안1 37 · 제안2 27 · 제안3 74 · 제안4 85).
- 나머지 ≈477줄을 옛 노트와 `_PLAYBOOK` 전문 대조로 전수 판정한 결과 **「기존 카드 중복」 신고가 2건에서 거짓**이었다:
  1. **옛 §6⑤(25줄)** — 「브루트포스 실패의 두 축(확장자 부족 vs 워드리스트 부족)」과 「2단 확장자 목록」. A-23 전문에 **둘 다 없다**(`grep 'conf,cfg,ini,env,json'` → 0건).
  2. **옛 §6⑥(51줄)** — 「base64 는 경유지지 종착지가 아니다 → 디코드 후 접두사 0건이면 제품 고유 포맷 = 즉시 손절」과 30초 판정 명령 3줄. B-66 은 「base64 면 디코드해서 재판정」까지, B-67 은 해시 vs 가역 구분까지이고 **셋째 범주로의 귀결이 양쪽에 없다.**
- 두 건을 **제안 5·6 으로 `Hub-playbook.md` 에 추가**했다(원문 인용 포함). 이관 커버 범위 223 → **304줄**.
- 나머지 8개 구간(≈396줄)은 신고가 참이었다 — 전수 판정표를 `Hub-playbook.md` 말미 「이관 회계 — 감사 재검산」에 남겼다.

## 5. 근거 출처

- Kali 산출물 — `~/PG/Hub/{nmap.log, nmap_quick.log, allports.nmap, ferox_80.log, ferox_8082.log, ferox_9999.log, ferox_80_files.log, user.dat, bdd.conf}`. `scp` 회수 후 md5 9/9 일치, mtime 전부 2026-08-19 17:07~17:19 (+0900) 로 서사와 정합.
- git — `git show HEAD:"03. PG/Hub.md"`(1133행) · `git show ebc992e:"03. PG/Hub.md"`(310행, 최초판).
- 1차 사료 / 직접 실행 — `/usr/share/nmap/nmap-services` · `raft-medium-files.txt` 전수 grep · `base64 -d` · `~/.zsh_history`(`192.168.248.25`·`fugu`·`.lsp` **0건** — 이 박스의 프롬프트 판정이 「비대화형」이라는 판단의 근거) · `_INDEX/_tools/extract.py:173-174, 386-392`.
- 볼트 `파일보관\` — 이 박스 스크린샷 **0장**(박스 정지로 소급 촬영 불가. 표준 430행에 따라 결함으로 취급하지 않음).

## 6. 총괄 판단이 필요한 것

- **색인 갱신이 필요하다.** `manual_services: true` 를 새로 넣었으므로 `refresh.ps1` 을 돌려야 프론트매터가 정착한다. **감사자는 실행하지 않았다**(공유 인프라).
- `_STATUS.md` 판정: **Hub = 완료 1/1**(root 플래그 `702262325fd3a55a2f23d49e0256571c`, user 플래그 부재가 `find` 무결과 + 포털 슬롯 1개로 확정). **감사자는 편집하지 않았다.**
