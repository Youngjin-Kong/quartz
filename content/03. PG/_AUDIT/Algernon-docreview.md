# Algernon — 문서 검토 (문체·공개 적합성·한국어)

대상: `03. PG\Algernon.md` · 백업: `03. PG\_backup\Algernon.md.bak`
축 ①(명사 종결형 개조식) 소급 적용은 총괄이 이 파일에 한해 승인.

## 검산 3축

| 항목 | 전 | 후 | 판정 |
|---|---|---|---|
| 바이트 | 78,291 | 76,755 | **-1,536 (-1.96%)** — 줄어드는 방향 |
| 행수 | 1,264 | 1,264 | 불변 (구조 개편 0) |
| 코드펜스 마커 | 90 | 90 | 불변 |
| 코드블록 45개 **본문 바이트** | — | — | **전부 동일** (프로그램 대조) |
| `[가정]` | 9 | 9 | 불변 |
| 「관측 없음」 | 0 | 0 | 원래 0 |
| 「근거부족」 | 0 | 0 | 원래 0 |
| `[[위키링크]]` | 56 | 56 | 불변 |
| `![[임베드]]` | 1 | 1 | 불변 |
| 프론트매터 | — | — | 바이트 동일 |

무변경 확인한 값(출현 횟수 전후 동일): 플래그 `6126cdddc1ed43d92e8a34f71ecda278`(3) · `192.168.248.65`(25) · `192.168.45.207`(5) · `10.44.44.128`(7) · `CVE-2019-7214`(7) · `49216`(15) · `100.0.6919.30415`(2) · `6919 (Dec 11, 2018)`(1) · `login-v-100.0.6919.30414.8d65fc3f1d47d00.min.css`(1) · `C:\Users\Administrator\Desktop\proof.txt`(4) · `17001`(40) · `9998`(35) · `1360`(17) · `1394`(5) · `nt authority`(5) · `LocalSystem`(13) · `PG-Algernon-smartermail-login.png`(2)

## 고친 것 — 296행

**분류별 건수**

| 분류 | 건수 | 비고 |
|---|---|---|
| 문체(서술형 → 명사 종결형) | 294 | 헤딩 12 · 콜아웃 제목 8 · 표 셀 46 · 본문/리스트 228 |
| 한국어(오탈자) | 1 | §1-4 `익스플로릿` → `익스플로잇` (코드펜스 밖 산문) |
| 한국어(번역투) | 1 | §2-5 `호출 순서에 대한 위 설명` → `위 호출 순서 설명` |
| 공개 적합성 | **0** | 아래 「반증」 참조 |

**대표 예 (전 → 후)**

| 위치 | 전 | 후 |
|---|---|---|
| §0 콜아웃 | `` `local.txt`는 존재하지 않는다`` | `존재하지 않음` |
| §1-1 | `전체 스캔은 3분 넘게 걸린다. … 때린다.` | `전체 스캔은 3분 초과 소요. … 때림.` |
| §1-3 헤딩 | `--min-rate`를 썼으면 반드시 한다 | `…반드시 할 것` |
| §1-6 | `발견물이 아니라 … 지문일 뿐이다. 80은 여기서 버린다.` | `…지문일 뿐. 80은 여기서 버림.` |
| §2-2 인용 | `…과 동등하다. 예외는 없다.` | `…과 동등. 예외 없음.` |
| §4 헤딩 | `권한상승 — **없다.** … 학습 포인트다` | `권한상승 — **없음.** … 학습 포인트` |
| §6-③ | `**중첩 따옴표가 붕괴했다.**` | `**중첩 따옴표가 붕괴함.**` |
| §8 표 | `…으로 올린다. … 방치돼 있었다` | `…으로 올릴 것. … 방치돼 있었음` |

전체 목록은 `git diff "03. PG/Algernon.md"` 로 확인 가능. 치환은 전부 **행 단위 완전일치 + 코드펜스 인덱스 가드**로 수행했고, 다중 매치나 펜스 내부 매치가 뜨면 스크립트가 중단되도록 했다.

## 삭제한 것 — 없음

내용 삭제 0건. 절·문장·표 행·콜아웃 어느 것도 제거하지 않았다. 바이트 감소 1,536B는 **전부 어미 축약**(`~했다`→`~함` 등)에서 나온 것이며, 행수가 1,264로 불변인 것이 그 증거다.

## 넘긴 것 — `writeup-auditor` 로 (사실 축, 나는 판정하지 않음)

1. **§6 도입의 「약 9분」과 §6-⑨ 표의 「약 10분」이 어긋난다.** 게다가 ⑨ 표의 항목별 실제값 합(1.3초 + 3.5분 + 약 2분 + 약 1분 + 약 5분 + 약 3분)은 약 14.5분이라 두 숫자 어느 쪽과도 맞지 않는다. 또 §6 도입이 종료 시각으로 든 `11:08`은 코드펜스상 **저레이트 교차검증 nmap의 종료 시각**(`11:08:08`)이지 플래그 획득 시각이 아니다. 어느 숫자가 실측인지 산출물 mtime 으로 확인 필요.
2. **§8 「6919로 6년 이상 방치」** — 빌드 6919는 노트 자체가 `6919 (Dec 11, 2018)` 로 적고 있고 박스 관측일은 2026-08-20 이다. 「6년 이상」은 참이지만 실제는 7년 8개월이다. 표현 문제가 아니라 수치 문제라 넘긴다.
3. **출처 캡션 누락** — §1-6 gobuster 결과 2블록, §2-5 가젯 타입 목록 블록, §3-1 `searchsploit` 블록에 `출처: ~/PG/Algernon/…` 캡션이 없다. `CLAUDE.md` §3 의 「출력만 싣고 출처를 캡션으로」 요건. 캡션 추가는 실측 대조가 필요해 내 축이 아니다.

**타임존은 내가 대조해 봤고 모순이 아니었다** — §4-1 의 타겟 `Get-Date` = `2026-08-19 19:09` 은 nmap `smb2-time: 2026-08-20T02:05:27`(UTC) 및 Kali 로그 `11:05:40`(KST=UTC+9)과 일관된다. 이건 넘기지 않는다.

## 손대지 않기로 한 것

| 대상 | 왜 남겼나 |
|---|---|
| 코드펜스 45블록 전부 | 명령·출력·실측. 한 바이트도 안 건드림 (프로그램으로 대조 확인) |
| `┌──(kali㉿kali)-[~/PG/Algernon]` / `└─$` 프롬프트 (코드펜스 내 다수) | 레거시 대화형 Kali 터미널 기록. `CLAUDE.md` §3 이 삭제를 금지 |
| `PS C:\Windows\system32>` 타겟 pty 프롬프트 | 플래그를 **대화형 셸에서 읽었다**는 판정 근거. 이 프로젝트 최고 등급 증거 |
| `[가정]` 9건 전부 | 인식론적 표시. 특히 §2-5 호출 순서, §6-① 첫 발사 원인, §6-② `pkill` 매치 범위, §4-3 서비스 바이너리 쓰기 권한 |
| §6-⑧ 제목의 「이번 산출물에는 실측 기록이 없음 — 구분해 적음」 | 유보 표시. 종결형만 바꾸고 의미는 유지 |
| §1-5 「①과 ②의 마지막 자리가 1 다름 … 결론에는 영향이 없음」 | 유보 + 무해 판정. 압축하지 않음 |
| 산문에 남은 서술형 21곳 (인용 19 + 명사절 2) | 누적 패턴 명칭(`"응답이 성공을 뜻하지 않는다"`, `"인용이 깨지면 인코딩으로 도망간다"`), 당시 오판 발언(`"실패했다"`), 인용된 원칙(`"안전하게 만들 수 없다"`). **인용문을 개조식으로 고치면 인용이 아니게 된다** |
| 헤딩·콜아웃 제목의 과장 형용사 | §2-7 「이 노트에서 가장 중요한 디테일」, §6 「이 장이 가장 중요한 이유」, §0 「이 박스가 값진 이유」. 표준의 「과장 형용사」 조항에는 걸리지만 **제목 문구 개작은 구조 개편에 가깝고 소급 전환의 손실 위험을 키운다.** 종결형만 고치고 문구는 뒀다 `[가정]` — 판정이 갈려 「남기는 쪽」으로 결정 |
| `192.168.248.65` · `192.168.45.207` · `10.44.44.128` | 공개 랩 환경. 무해 |
| 프론트매터 `ports`에 5040 없음 | `CLAUDE.md` §5 의 의도된 Windows 제외. 버그 아님 |

헤딩 12개의 문구가 바뀌었으므로 앵커 링크 파손을 확인했다 — 볼트 전체에 `[[Algernon#...]]` 형태 링크는 **0건**이라 파급 없음.

## 소급 전환 위험 측정 (총괄 요청)

### ① 인과가 사라질 뻔한 지점 — 12개 (6장에 7개)

압축했다면 「왜」가 날아갔을 곳. 전부 인과절을 그대로 살렸다.

| 위치 | 위험했던 것 |
|---|---|
| §6-① | `첫 발사가 헛돈 원인일 가능성이 큼 [가정] — 셸이 실제로 다른 pane에 떨어졌는지는 확인하지 못함` — **[가정]과 미확인 유보가 한 문장에 겹쳐 있다.** 「첫 발사 실패」로 줄이면 둘 다 소멸 |
| §6-① | `tmux new-session -d 가 등록을 **비동기로** 처리 → 세 개를 밀어 넣으면 순서가 어긋남 [가정]` — 메커니즘 |
| §6-② | `pkill -f 는 전체 커맨드라인에 매칭 → tmux pane 커맨드라인에도 그 문자열이 있음 → 광범위 매치` — 3단 인과 |
| §6-③ | `Windows에서 /dev/null 은 **경로로 해석되어** 리다이렉트 실패` |
| §6-③ | `각 계층이 백슬래시와 큰따옴표를 **자기 방식으로 소비하므로** 도착 시 원래 문자열이 아님` |
| §6-③ | `dir /s /b 는 어떤 상황에서도 수만 줄 → 0줄이면 "없다"가 아니라 "실행 안 됨"` — 판별 논리 전체 |
| §6-⑤ | `익스플로잇 코드가 PORT=17001 을 기본값으로 갖고 있다는 사실 자체가 증거` — 근거절 |
| §2-7 | `ljust 는 자르지 않음 → 1394만 읽힘 → 남은 바이트를 다음 레코드로 해석 → 파싱 실패 → 무반응` — **4단 인과.** 여기가 이 노트에서 가장 압축 유혹이 큰 곳 |
| §2-5 | `실제로 셸이 떨어졌으므로 결과는 확정이나, 호출 순서 설명은 코드 계측으로 확인하지 않았으므로 [가정]` — 확정과 유보의 경계선 |
| §2-3 | `SmarterMail이 그랬음 [가정] — 페이로드가 델리게이트 가젯으로 성공했다는 사실이 근거` |
| §1-5 | 리비전 1 차이의 이유 + `[가정]` + 「결론에 영향 없음」 |
| §4-3 | `확인하지 않음 [가정]. SYSTEM이므로 무의미` — 「안 해봤다」와 「불필요했다」의 구분 |

**패턴:** 위험은 **「~해서/~므로 + 결과」 구조**에 집중된다. 개조식은 종결 어미를 자르는데, 한국어에서 인과 연결어미(`~하므로`·`~해서`·`~때문에`)는 **문장 중간에 있어 종결과 함께 잘릴 이유가 없다.** 실제로 인과가 날아가는 것은 어미 치환이 아니라 **「두 문장을 한 문장으로 합치려는 충동」**에서 온다. 어미만 건드리는 한 인과 손실은 구조적으로 발생하지 않는다.

### ② 자동 치환 가능성 — **17%**

기계적 어미 사전(`했다→함`·`한다→함`·`된다→됨`·`이다→임`·`있다→있음`·`없다→없음`·`않는다→않음`·`아니다→아님` 등 14쌍)만으로 재현되는 변경행: **296행 중 49행(17%).**

**사람 판단이 필요했던 유형 (83%)**

| 유형 | 예 | 왜 자동화 불가 |
|---|---|---|
| **인용문 판별** | `"응답이 성공을 뜻하지 않는다"` · `"제품은 있는데 표준 포트가 닫혀 있다"` · `"실패했다"고 판정` | **인용 19곳 + 명사절 2곳 = 21곳을 «건드리면 안 되는 것»으로 골라내야 했다.** 기계 치환은 인용부호 안까지 바꿔 인용을 훼손한다. 자동 치환의 최대 위험 |
| **어간 재선택** | `가리켰다`→`지목`, `물어봤다`→`물음`, `갖는다`→`가짐`, `뱉는다`→`뱉음`, `찍는다`→`찍음` | 불규칙 활용. `~는다`를 일괄로 `~음`으로 바꾸면 비문이 대량 발생 |
| **명령형 유지 판정** | `찾아라`·`쓰지 마라`·`받아라` (그대로) vs `검증한다`→`검증할 것` | 이미 명령형인 것을 건드리면 안 되고, 서술형 지시는 `~할 것`으로 가야 한다. 문맥 판단 |
| **어미 없는 종결** | `~것이다`→`~것`, `~뿐이다`→`~뿐`, `~다`→(삭제) | 사전 치환이 아니라 **삭제**. 조사/의존명사 뒤라 규칙화가 어렵다 |
| **표 셀** | 46건. `| … 크다 |` → `| … 큼 |` | 셀 경계(`|`) 안에서 종결이 끝나므로 문장부호 앵커가 없다 |
| **코드펜스 배제** | 45블록 | 백틱 상태 추적 필수. 단순 정규식 sed 로는 **반드시 실측을 오염시킨다** |

**결론:** 순수 자동 치환은 **17%밖에 못 하고, 나머지 83%를 잘못 건드리면 인용 훼손·비문·실측 오염이 난다.** 특히 인용문 21곳과 코드펜스 45블록은 자동 치환이 **적극적으로 해로운** 구간이다. 49개 확대 시에도 「스크립트에 사람이 만든 행 단위 완전일치 목록을 먹이고, 다중 매치·펜스 매치는 즉시 중단」 방식이 필요하다 — 이 파일에서 쓴 방식이 그것이다.

### ③ 바이트·행수 변화

`78,291B → 76,755B` (**-1,536B, -1.96%**) · `1,264행 → 1,264행` (불변).

**늘어난 곳 없음.** 행 단위로 대조했을 때 길이가 늘어난 변경행은 296행 중 **14행이고 최대 증가폭이 +3자**다. 전부 어미 교체에서 온 미세 증가로, 원인은 둘뿐이다 — 서술형 지시를 `~할 것`(1자 증가)으로 바꾼 것 12건, `이렇다`→`다음과 같음`(+3자) 1건, `차이:`→`차이 —`(+1자) 1건. **새로 쓴 문장은 0개다.** GLPI 에서 났던 「정정이 분량을 +50% 부풀리고 그 증분에 날조가 섞였다」 유형은 이 파일에서 발생하지 않았고, 발생할 수 없는 작업 방식(기존 행의 어미만 교체, 행 추가 불가)을 썼다.

### ④ 검산 3축 — 위 「검산 3축」 표 참조

코드펜스 45블록 본문을 백업본과 **바이트 단위로 전량 대조**해 100% 동일함을 확인했다(단순 개수 대조가 아님).
