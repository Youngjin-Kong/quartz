---
tags:
  - type/audit
  - platform/pg
manual_tags: true
---
# pyLoader — 적대적 검증 감사 (웨이브 14)

대상: `03. PG\pyLoader.md` (감사 전 718행 → 감사 후 726행).
원본: `03. PG\_backup\pyLoader.md.bak2`(1003행) · 이관 제안: `03. PG\_AUDIT\pyLoader-playbook.md`(18건).

---

## 근거 출처 — 실제로 열어본 것

| 출처 | 확인 내용 |
|---|---|
| `~/PG/pyLoader/nmap.log` (1418B, 2026-06-29 10:46:49) | 노트 nmap 블록과 **후행 공백까지 바이트 일치** |
| `~/PG/pyLoader/CVE-2023-0297/CVE-2023-0297.sh` (2472B, 11:08:04) | 노트의 `curl` 원문 블록과 **이중 공백까지 일치** · `read -p "Run nc -lvnp ${LPORT}, press enter to continue"` 실재 |
| `~/PG/pyLoader/CVE-2023-0297/51532.py` (1783B, 11:12:16) | 이관 제안 P4 의 인용 2건이 원문과 일치 · `test` 미사용·`GET` 생존확인 실재 |
| `~/.zsh_history` | `searchsploit pyLoad` · `mkdir pyLoader` · `cd pyLoader` · `searchsploit pyload` 4건 — **대화형 세션 흔적** |
| 볼트 `파일보관\Pasted image 20260629{105429,111029,111100,111246,111431,111457}.png` | **6장 직접 열람** |
| Kali 직접 실행 | `dash` `/dev/tcp` 부재 · `nmap-services` 9666 빈도 · top-1000 커트라인 · `--packet-trace` 프로브 · `curl --data-urlencode` 이중 인코딩 · `/usr/bin/nmap` 래퍼 원문 · `getcap /usr/lib/nmap/nmap` |

---

## 고친 것 (3건)

| 위치(문자열) | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| `### Privilege Escalation – 없음 (pyLoad 프로세스가 root 로 …)` | **구조 결손.** `_WRITEUP-STANDARD` 「finding 4항목 — `Initial Access` 와 `Privilege Escalation` **각각**에 붙인다」 위반. 볼트 선례([[Bratarina]]·[[Hawat]]·[[Wombo]])는 「해당 없음」 형태로 4항목을 채움 | 4항목 삽입. `Vulnerability Explanation` 은 실측 근거(프롬프트·`whoami`)만 인용, 나머지 셋은 「해당 없음 — `Initial Access` 에 계상」 |
| `실제로 날아간 HTTP 요청 본문(익스플로잇 스크립트 원문 그대로):` | **라벨이 틀림.** 뒤 블록은 스크립트 원문이 아님 — `${LHOST}`·`${LPORT}` 가 실행 당시 값으로 치환됐고 셸 이스케이프 `\"` 가 풀린 상태임. 스크립트 원문은 같은 노트의 `curl` 절에 따로 실려 있어 두 블록이 서로 다른데 라벨이 둘 다 「원문」이라 함 | 「치환·이스케이프 해제한 것」임을 명시하고 원문 블록의 위치를 가리킴 |
| `이 설명을 채택하는 근거는 셋임 — ⑴…⑵…⑶…` | **근거 부족이 아니라 근거 미사용.** 노트가 자기 산출물을 덜 씀 — 같은 `-p-` 스캔의 `Not shown: 65533 closed tcp ports (reset)` 이 **8000 이 외부에 안 열려 있었다**를 하드하게 증명하는데 `[가정]` 콜아웃이 이를 세지 않음 | ⑷ 로 추가하고 「셋」→「넷」. `[가정]` 표시 자체는 그대로 둠(`pyload.cfg` 를 읽지 않은 것은 사실이므로) |

## 삭제한 것

**없음.** 이번 감사에서 노트 본문에서 지운 문장은 0건.

---

## 반증한 것 — 지적으로 올랐다가 확인 결과 노트가 옳았던 것 (7건)

이 절이 검증 절차 자체의 정확도 기록임.

1. **「Kali 프롬프트 9곳이 창작 아닌가」 → 반증.** `파일보관\Pasted image 20260629111100·111246·111431·111457.png` 4장을 직접 열어 `┌──(kali㉿kali)-[~/PG/pyLoader]` 형태가 **화면에 그대로 찍혀 있음**을 확인. `~/.zsh_history` 에 `mkdir pyLoader`·`cd pyLoader`·`searchsploit pyload` 가 남은 것도 대화형 세션 근거임(zsh 는 비대화형 `ssh kali "cmd"` 를 기록하지 않음). **9곳 전량 보존이 옳음. 지우면 실측 파괴였음.**
2. **「타겟 pty 프롬프트가 과다한가」 → 반증.** `root@pyloader:~/.pyload/data#` · `root@pyloader:~#` 8곳 전부 `Pasted image 20260629111100.png` 에 실재. 이것이 「대화형 셸에서 플래그를 읽었다」의 유일한 근거라 **보존이 필수**.
3. **「`su` TTY 블록 삭제가 실측 파괴 아닌가」 → 반증.** 삭제 원문이 이관 제안 `pyLoader-playbook.md` P15 에 **13행 전문 그대로** 보존돼 있음(bak2 667~679행과 대조 일치). 또한 그 블록이 정정하던 원 서술(「`su`·`sudo` 도 동작하지 않는다」)이 개작본에서 함께 사라져 **고아 오류가 남지 않았음**. 이 박스에서 `su` 를 쓴 적이 없어 심사관 재현에 불필요하므로 삭제 판정이 옳음.
4. **「Python Version 값이 개변됐나」 → 반대로 정정된 것.** bak2 는 `3.10.6 (main, May 29 2023) [GCC 11.3.0]` 로 **절삭**돼 있었음. 스크린샷 실측은 `3.10.6 (main, May 29 2023, 11:10:38) [GCC 11.3.0]` — **개작본이 맞고 원본이 틀렸음.**
5. **「nmap 블록이 압축·개변됐나」 → 반대.** bak2 는 `| http-title: Login - pyLoad` 등에서 **후행 공백을 잃은** 상태였고, 개작본이 `nmap.log` 원문의 후행 공백까지 복원했음. **개작본이 원본보다 실측에 가까움.**
6. **「`ip a` 부재 명시가 보고에만 있고 노트에 없나」 → 반증.** 노트 `Post-Exploitation` 에 ⚠️ 한 줄로 실재하고, `Pasted image 20260629111100.png` 를 직접 열어 **실제로 `ip a` 가 없음**을 확인. 서술이 정확함.
7. **「'15분을 아낄 수 있었다' ↔ 실측표 '~21분' 내부 모순이 남았나」 → 반증.** 개작본에는 두 서술 모두 없음(`_PLAYBOOK` 이관 대상). 이관 제안 P17 은 원문을 ④에 보존하면서 **본문에서는 「~21분 구간」으로 통일**했고 825행에 경고까지 달아 뒀음. 모순 해소됨.

**부재 증거로 강등한 것: 0건.** 이번 감사에서 「산출물에 없다」를 근거로 삭제·강등한 판정은 없음.

---

## 이관 손실 대조 — `.bak2` 직접 대조 (제안서 검산 줄을 믿지 않고 세었음)

bak2 의 §0~§9 를 순서대로 훑어 **노트에 남았거나 / 제안에 ④ 원문 인용과 함께 있는가**를 단위별로 확인.

| bak2 구간 | 판정 |
|---|---|
| §0 불릿 6개 · 출제 가능성 표 · 변형 문단 · 「25분」 한 줄 | P9 ④ · P10 ④ · P11 ④ · P8 ④ · P12 ④ 에 전문 인용 ✅ |
| §1-1 nmap raw · `nnmap` 별칭 · 플래그 표 · top-1000 콜아웃 · `closed` 문단 | **노트에 존치** ✅ |
| §1-1 `[!danger] -A 의 OS 추측` 콜아웃(129~136) | 노트 141~153 에 산문으로 존치 ✅ + P2 |
| §1-2 · §1-3 | 노트 존치 ✅ · 「판정 비용 > 시도 비용」 일반화는 P3 ④ ✅ |
| §2-1 배경 / 「설계상 코드를 실행하는 엔드포인트」 | 노트 존치 / P9 ④ ✅ |
| §2-2 js2py 소스 3종 / 커밋 계보 표·「2년 4개월」·`git log -S` 콜아웃·문서 경고 부재 | 노트 존치 / P9 ④ ✅ |
| §2-3 소스 3개·흐름도·`[가정]` 콜아웃·`[!tip] /flash/*` / 「실전에서도 자주」·「인증 예외 목록」 | 노트 존치 / P7 ④ · P8 ④ ✅ |
| §2-4 표·패치 diff / 「CVE 패치 ≠ 엔드포인트 안전」 | 노트 존치 / P8 표 마지막 행 + 「이관하지 않은 §7」 #19 ✅ |
| §2-5 페이로드 표·URL 인코딩 콜아웃 / `/dev/tcp` 대안 콜아웃 | 노트 존치 / P5 ④ ✅ |
| §3-1 clone·searchsploit·`ls` 블록 / 시험 규정 문단 | 노트 존치 / P14 ④ ✅ |
| §3-2 · §3-3 · §3-4 | 노트 존치 ✅ · `su` 블록만 P15 ④ ✅ |
| §4 근거 표 / §4-1 5줄 반사 | 노트 존치 / P16 ④ ✅ |
| §5 플래그·`email5.txt` / 증거 형식 콜아웃 | 노트 존치 / P13 ④ ✅ |
| §6 ①~⑦ 전량 | P6 · P4 · P17 · P5 · P2 · P18 · P8 · P12 의 ④ 에 전문 인용 ✅ |
| §7 21항목 | 이관 10건(3·5·7·8·9·10·14·15·17·21) + 기존 중복 11건(1·2·4·6·11·12·13·16·18·19·20) = **21 검산 통과** ✅ |
| §8 방어 관점 표 10행 | 노트 `Vulnerability Fix:` 9불릿에 **10/10 흡수 확인**(아웃바운드·탐지가 한 불릿에 병합) ✅ |
| §9 참고 자료 · 남긴 흔적 · 관련 노트 | 노트 존치 ✅ |

**손실 1건(경미).**

- bak2 612행 말미 「**리버스셸 리스너에는 항상 붙인다**」(`rlwrap` 습관화 권고) — 노트 578행이 `rlwrap` 설명은 남겼으나 이 한 절만 떨어져 나갔고 제안 어디에도 없음.
- 판정: **정정하지 않음.** 「리스너에 항상 `rlwrap`」은 `_PLAYBOOK` A-31 계열의 일반 습관이고 원문이 이 감사 파일에 인용돼 되살릴 수 있음. 총괄이 `_PLAYBOOK` 반영 중이므로 **거기 한 줄로 붙일지는 총괄 판단**임(에이전트가 `_PLAYBOOK` 을 고치지 않는다는 지시에 따름).

원문 보존:

```text
리스너에 `rlwrap nc -lnvp 4444` 처럼 `rlwrap` 을 건 것은 readline 을 얹기 위해서다. ↑↓ 명령 히스토리와 ←→ 커서 이동이 생긴다. TTY 없는 리버스셸에서는 오타를 백스페이스로 못 고치는데 `rlwrap` 이 그 고통을 절반쯤 없애주므로, 리버스셸 리스너에는 항상 붙인다.
```

---

## 실행해 확인한 단정형 일반 지식 (6건 전부 노트가 맞음)

| 노트 서술 | Kali 실측 |
|---|---|
| Ubuntu 의 `/bin/sh` 는 dash 이고 dash 에는 `/dev/tcp` 가 없음 | `dash -c 'exec 3<>/dev/tcp/…'` → `cannot create … Directory nonexistent` |
| 9666 은 `nmap-services` 에 `zoomcp` 빈도 `0.000304` 로 등재 | `zoomcp	9666/tcp	0.000304	# Zoom Control Panel Game Server Management` |
| top-1000 커트라인 빈도 `0.000152` | `awk … \| sort -rn \| sed -n 1000p` → `0.000152` |
| 기본 스캔이 실제로 9666 을 프로브 | `nmap -sT -Pn --top-ports 1000 --packet-trace 127.0.0.1 \| grep -c ":9666"` → `2` (nmap 7.98) |
| `--data-urlencode` 는 `%` 를 `%25` 로 이중 인코딩 | 로컬 캡처 → `jk=pyimport%2520os` |
| `--privileged`·`/usr/lib/nmap/nmap` 은 Kali `/usr/bin/nmap` 래퍼가 붙인 것(capability 부여) | 래퍼 원문 `exec /usr/lib/nmap/nmap --privileged "$@"` · `getcap` → `cap_net_bind_service,cap_net_admin,cap_net_raw=eip` |

---

## 넘긴 것 — `pg-doc-reviewer` 관할 (3건)

문체는 감사자 검사 항목이 아니므로 고치지 않음.

- `**① 취약 핸들러에는 세션 인증이 아예 없다.**` — 서술형 종결
- `**② `local_check` 는 `REMOTE_ADDR` 을 믿는다.**` — 서술형 종결
- `**③ pyLoad 자신이 9666 에 프록시를 세워 `REMOTE_ADDR` 을 위조해준다.**` — 서술형 종결

⚠️ 절 제목·콜아웃 제목의 서술형(예: `[!danger] 「9666 은 기본 1000포트에 없다」는 틀렸다`)은 **볼트 관례**임([[Robust]] 동일). 문체 지적 대상이 아님.

---

## 통과시킨 검사 항목

- **nmap raw 보존** — `22/tcp`·`9666/tcp` 라인 포함 전문 존치. `extract.py` `PORT_RE` 가 긁을 수 있음 ✅
- **코드펜스 언어 태그** — 펜스 34쌍(일반 30 + 콜아웃 내부 4) **여는 펜스 전부 태그 있음**, 무태그 0 ✅
- **두 `Initial Access` 제목 상이** — 앞 긴 서술형 / 뒤 짧은 형 ✅
- **4항목 안 상대 참조** — 「아래」·「위」 0건 ✅
- **`Local.txt value:` 위치** — `Initial Access` 상세 절 끝, 「없음」 + `[가정]` 유보 ✅
- **플래그 값** — `8e83040ffcced0d40655d685a19821af`, 스크린샷과 32자 전부 일치 ✅
- **시간 서사** — `CVE-2023-0297.sh` mtime 11:08:04.039 · `51532.py` 11:12:16.064 · 스크린샷 11:11:00, 노트 서술과 일치. 로컬(KST)·UTC 혼동 없음(전부 `ls` 로컬 기준) ✅
- **금지 문자열** — `_AUDIT`·`_backup`·「적대적 검증」·「정정 이력」 노트 본문에 0건 ✅
- **`_PLAYBOOK` 앵커 9개** — 전부 실재하고 제목 문자열 완전 일치 ✅
- **프론트매터** — `tech/enum/searchsploit` 제거 + `tech/web/rce`·`tech/web/auth-bypass` 추가 = 5개, `tech_count: 5` 정합 · `manual_tags`/`manual_cves` 선언 있음 · 값 든 줄에 주석 없음 · `cves: [CVE-2023-0297]` 은 실제 악용한 것 하나뿐 ✅
