# pyLoader — 적대적 검증 기록

## 본문에서 이관한 정정 이력

(2026-08-20 이관 — 원래 `pyLoader.md` 상단에 있던 블록. 원문 그대로.)

> [!warning] 적대적 검증 정정 이력 (2026-08-20)
> 이 노트는 [[_WRITEUP-STANDARD]]의 적대적 검증을 거쳤다. 소스 인용(2-3 전체·CVE 사실관계·PoC 코드)은 전량 정확했고 스크린샷 9장도 본문과 오탈자까지 일치했다. 무너진 것은 새로 써넣은 단정과, 실행해서 확인했어야 할 것들이다 — 표준이 경고한 실패 모드 그대로다.
>
> | # | 초판의 서술 | 판정 | 반증 근거 | 반영 위치 |
> |---|---|---|---|---|
> | 1 | "9666은 nmap 기본 1000포트에 없다. `-p-` 없이 스캔하면 22번만 보인다" (3곳) | 반증됨 | `nmap-services` 에 `zoomcp 9666/tcp 0.000304` 로 등재. top-1000 커트라인은 `0.000152` 라 안쪽이다. `--packet-trace` 로 기본 스캔이 9666을 프로브함을 확인 | 1-1 · 0장 표 · 6-⑦ · 7-1 |
> | 2 | "TTY 없는 셸에서는 `su` 가 동작하지 않는다" | 반증됨 | util-linux `su` 는 TTY가 없으면 stdin에서 비밀번호를 읽는다. `must be run from a terminal` 문자열은 바이너리에 존재하지 않는다. [[plum]]의 스크린샷이 결정적 증거 | 3-4 |
> | 3 | 패치 커밋 diff 를 "실질 변경 한 줄"로 축약 인용 | 부정확 | `7d73ba79` 는 `1 insertion(+), 2 deletions(-)` 다 — `-import socket` 도 지운다. 코드펜스 안은 1차 사료 인용이다 | 2-4 |
> | 4 | "차단 수단이 7년 전부터" · "2020-08 js2py 도입" | 부정확 | 2016-11-15 → 2023-01-03 = 6년 2개월. 2020-08은 재도입이다 (최초 2018-08-09 `79a9cac1`, 2019-06-05 제거 `ec90b12c`) | 2-2 |
> | 5 | "구버전 기본 계정 `admin`/`password`" | 1차 사료 없음 | `v0.4.20` 의 `setup.py` 는 대화형 질문(기본 제안 `"User"`), `default.conf` 는 `username = None` | 1-3 |
> | 6 | "셸 획득 11:14 / 총 30분" | 자기 스크린샷과 충돌 | 플래그 화면 `…111100.png`(11:11:00)이 `51532.py` mtime(11:12:16)보다 앞선다 → root는 searchsploit 이전 | 6-① · 6-⑦ · 0장 |
> | 7 | "`closed` 가 많으니 아웃바운드도 열려 있을 가능성" | 추론 불성립 | 인바운드 RST는 egress에 대해 아무 정보도 주지 않는다. 결과가 맞은 것과 추론이 서는 것은 별개다 | 1-1 |
>
> 터미널 블록·플래그·IP·자격증명은 한 바이트도 수정하지 않았다. 고친 것은 설명 문장과 인과뿐이다.

---

## 본문 중간에서 잘라낸 작업 과정 서술

아래는 전부 이관 전 `pyLoader.md` 의 원문 그대로다. 행번호는 이관 전 기준.

### 57행 — 0장 출제 가능성 표, `9666` 행 (지시어만 제거: `(1-1 정정)` → `(1-1)`)

| 비표준 고포트에 붙은 관리 웹앱 (9666) | 높음 | 제품명이 곧 검색 키워드다. 9666 자체는 `nmap-services` 등재 포트라 기본 스캔에도 잡힌다(1-1 정정) — 진짜 위험은 미등재 고번호다 |

### 119행 — 1-1 플래그 표, `-p-` 행 (지시어만 제거: `(아래 정정 참조)` → `(아래 참조)`)

| `-p-` | 65535 포트 전수 | 이 박스에서는 손해가 없다. 9666은 `nmap-services`에 `zoomcp`로 등재돼 있고 빈도 `0.000304`로 top-1000 안이라 기본 스캔도 9666을 프로브한다(아래 정정 참조). `-p-` 의 실익은 **등재되지 않은 고번호 포트**에서 나온다 |

### 126-127행 — 1-1 `nmap-services` 콜아웃의 머리말 두 줄

콜아웃 본체(실측 코드펜스 · top-1000 커트라인 논증 · `-p-` 의 진짜 실익)는 본문에 남겼다. 잘라낸 것은 「초판이 세 곳에서 이렇게 단정했다」는 과정 서술뿐이다.

> [!danger] 정정 — "9666은 기본 1000포트에 없다"는 이 노트의 옛 서술이 틀렸다
> 초판은 세 곳에서 "`-p-` 없이 스캔하면 22번만 보인다"고 단정했다. 반증된다. Kali nmap 7.98 실측:

### 155행 — 1-1 `closed` 해석 문단 (`초판은 … 적었는데` 만 제거)

여기서 한 걸음 더 나가면 안 된다. 초판은 "그러니 아웃바운드도 열려 있을 가능성이 높다"고 적었는데 추론이 성립하지 않는다. `closed`(RST)는 타겟으로 들어오는 방향에 대한 정보일 뿐이고, 타겟에서 나가는 트래픽에 대해서는 아무것도 말해주지 않는다. 인바운드 무필터 + 아웃바운드 화이트리스트는 흔한 구성이다. 실제로 4444 리버스셸이 붙은 것은 사실이지만 **결과가 맞았던 것과 추론이 성립하는 것은 별개다.** 아웃바운드는 직접 때려봐야만 안다 — 리스너를 띄우고 포트를 바꿔가며 시도하는 수밖에 없다.

### 184행 — 1-3 기본 자격증명 문단 (`초판은 여기에 … 라고 적었는데,` 만 제거)

초판은 여기에 구버전 기본 계정이 `admin`/`password` 라고 적었는데, 검색 결과에 흔히 도는 조합일 뿐 0.4.20 소스에는 근거가 없다. 설치 스크립트가 사용자에게 묻고, 제안 기본값은 `"User"` 이며, 설정 파일의 초기값은 `None` 이다. **[가정]** 어떤 배포판 패키지나 도커 이미지가 `admin`/`password` 를 심었을 가능성은 남지만 확인하지 못했다. 실전 영향은 없다 — 이 박스는 `pyload/pyload` 로 들어갔다. 다만 기본 계정이 무엇이라고 노트에 적으려면 소스를 열어야 한다. 검색 결과 두 번째 줄은 1차 사료가 아니다.

### 286-287행 — 2-2 js2py 연표 콜아웃

287행은 통째로 이관했다. 그 안의 사실관계(2016-11-15 → 2023-01-03 = 6년 2개월, 2018-08-09 최초 도입 / 2019-06-05 제거 / 2020-08-20 재도입)는 본문 279-284행의 연표와 요약 문장에 이미 그대로 들어 있다. 286행은 머리말에서 과정 서술만 잘라냈다.

> **이관 감사 정정 (같은 날)** — 이관 실무가 남긴 제목 `> [!warning] \`git log\` 한 줄` 은 콜아웃 내용을 가리키지 못한다. 본문에 이미 있던 표현만으로 `> [!warning] 한 번 걷어냈던 의존성이 되돌아왔다 — \`git log\` 한 줄로 보인다` 로 복원했다("한 번 걷어냈던 의존성이 되돌아왔다"·"보인다" 는 콜아웃 본문 축자).

> [!warning] 이 표의 두 군데가 초판에서 틀렸다 — 둘 다 `git log` 한 줄로 반증됐다
> 초판은 차단 수단이 "7년 전"부터 있었다고 적었지만 2016-11-15 → 2023-01-03 은 6년 2개월이다. 그리고 2020-08 을 js2py "도입"이라고 적었는데 그건 재도입이다 — 최초 도입은 2018-08-09이고 2019-06-05에 한 번 걷어냈다가 되돌아왔다.

### 468행 — 2-4 패치 diff 문단 (첫 문장만 제거)

초판은 이 diff 를 "실질 변경 한 줄"로 줄여 인용했는데 원문과 달랐다. 실제 `git show --stat` 은 `1 file changed, 1 insertion(+), 2 deletions(-)` 이고 `-import socket` 도 함께 지워진다(같은 커밋에서 미사용 임포트를 정리했다). 보안 관점의 요지 — 막은 것은 `pyimport` 한 줄뿐이라는 것 — 은 그대로지만, 코드펜스 안의 diff 는 1차 사료 인용이라 원문과 한 글자도 달라선 안 된다. 요약하고 싶으면 코드펜스 밖 산문으로 쓴다.

### 683행 — 3-4 TTY 콜아웃 (`정정 — 초판은 여기에 … 고 적었는데` 만 제거)

`su` 가 TTY 없이 stdin 에서 비밀번호를 읽는다는 실측 코드펜스는 본문에 그대로 남겼다.

> 정정 — 초판은 여기에 "`su`·`sudo`(비밀번호 입력)도 동작하지 않는다"고 적었는데 `su` 에 대해서는 틀렸다. util-linux `su` 는 TTY 가 없으면 stdin 에서 비밀번호를 읽는다:

### 795-796행 — 6-① 타임라인 콜아웃의 머리말 두 줄

796행은 통째로 이관했다. 콜아웃 본체(mtime 3단 논증 · `[가정]` 붙여넣기 시각 유보 · 프레임 전환)는 본문에 남겼다.

> [!danger] 정정 — 초판의 "셸 획득 11:14 / 총 30분" 은 자기 스크린샷과 충돌했다
> 초판은 §6-①에서 파일 mtime 논증으로 clone → searchsploit 순서를 정정해 놓고, 같은 잣대를 스크린샷 파일명에는 적용하지 않았다. 적용하면 결론이 하나 더 나온다.

### 831행 — 6-① 서술 순서 문단 (과정 절만 제거)

노트의 서술 순서도 정정해야 했다. 기존 노트는 `searchsploit` → `git clone` 순으로 적혀 있었으나 실제 순서는 반대다. 근거는 셋이다.

> **이관 감사 정정 (같은 날)** — 이관 실무는 이 문단을 `근거는 셋이다.` 만 남기고 잘라냈는데, 그러면 결론 문장이 사라져 뒤따르는 근거 셋이 무엇을 뒷받침하는지가 없어진다. `실제 순서는 반대다` 는 과정 기록이 아니라 기술 결론이다. 본문을 `실제 순서는 `git clone` → `searchsploit` 이다. 근거는 셋이다.` 로 복원했다(순서 자체는 같은 절 위의 실측 타임라인 표에 이미 있다).

### 835행 — 6-① 근거 셋째 항목 (지시어만 제거: `(위 정정 참조)` → `(위 참조)`)

- 그리고 플래그 스크린샷(11:11:00)이 그 둘 사이에 있다 (위 정정 참조)

### 837행 — 6-① 마무리 문단 (통째로 이관)

박스를 풀며 겪은 시행착오가 아니라 노트를 고친 기록이다. 기술 사실은 한 건도 들어 있지 않다.

이 절은 원래 mtime 이 서술 순서를 반증한다는 논증을 담고 있었는데, 같은 논증을 스크린샷 파일명에는 적용하지 않아 상단 요약과 시간 배분표가 모두 틀린 채로 남아 있었다. 한 종류의 증거로 서술을 뒤집었으면 같은 노트의 다른 증거도 그 잣대로 다시 훑어야 한다. **반쪽만 적용한 정정은 정정하지 않은 것보다 위험하다** — 이 부분은 검증했다는 인상을 주기 때문이다.

### 928행 — 6-⑦ 시간 배분 (둘째 문장만 제거)

정찰 시작(10:45:55) → root 셸(11:11:00 이전), 실측 약 25분이다. 초판은 "셸 11:14 / 30분"이라고 적었으나 ①에서 반증됐다.

### 939행 — 6-⑦ 마무리 문단 (첫 문장 제거 + `(1-1 정정)` → `(1-1)`)

초판은 여기에도 "9666은 기본 1000포트에 없으니 `-p-` 를 빼면 박스가 끝난다"고 적었는데 틀렸다. 9666은 `nmap-services` 등재 포트이고 top-1000 안이다(1-1 정정). 이 박스는 `-p-` 없이도 풀렸을 것이고, 시간이 샐 수 있었던 지점은 오히려 **로그인 화면을 보고 인증부터 뚫으려 든 것**(②)이었다. 그래도 `-p-` 는 넣는다. 근거는 미등재 고번호 포트다 — [[Hawat]]의 50080(cloud 로그인 패널)은 기본 스캔에 잡히지 않았고 그 포트가 박스의 유일한 경로였다. 시간이 급하면 `--min-rate` 를 올리지 포트 범위를 줄이지 않는다.

### 947행 — 7장 1번 (지시어만 제거: `(1-1 정정)` → `(1-1)`)

1. `-p-` 전수 스캔부터 — `sudo nmap -sCV -p- -Pn -A --min-rate 5000 -oN nmap.log <IP>`. 단 이 박스가 그 근거는 아니다. 9666은 top-1000 안이다(1-1 정정). 근거는 [[Hawat]]의 50080 같은 미등재 고번호다. **포트 범위를 줄이는 것은 시간 절약이 아니라 박스를 버릴 위험을 사는 것이다.**
