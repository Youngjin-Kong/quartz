---
tags:
  - type/audit
  - platform/pg
manual_tags: true
manual_cves: true
---
# 웨이브 12 — `_PLAYBOOK.md` 단독 반영 기록

대상: `03. PG\_PLAYBOOK.md` (2917행 → 3833행, **+916**)
입력: `_AUDIT\{Fanatastic,Muddy,Zipper,Monster}-playbook.md` 4건(제안 58건 상당)
백업: `03. PG\_backup\_PLAYBOOK.md.bak7`(작업 전 복사, 283,792B)
⚠️ **`_PLAYBOOK.md` 는 git 미추적(`??`)임** — git 이력이 복구를 못 받쳐 줌. `.bak7` 이 유일한 복구점.

---

## 1. 신규 항목 34개 — 번호 배정

카테고리별 「현재 최대 번호 다음」으로 배정. 기존 제목·번호는 **한 건도 안 바꿈**(예외 1건은 §4).

| 번호 | 제목 | 출처 제안 |
|---|---|---|
| `A-1-18` | `--min-rate` 를 높이면 «유령 포트»가 생긴다 | Muddy A-1 |
| `A-1-19` | 생성한 사전·목록이 0줄인데 «조용하다» — 파이프라인이 실패를 삼킨다 | Monster 9 |
| `A-1-20` | PHP 사이트인데 브루트 확장자에 `php` 를 안 넣었다 | Zipper 7 |
| `A-2-18` | 유명 CMS 가 봉쇄돼 있고 정체불명 서비스가 함께 떠 있다 | Muddy A-2 |
| `A-2-19` | 파라미터가 아무 데도 없다 — 입력 형식부터 | Muddy A-3 |
| `A-2-20` | 같은 익스플로잇에서 «일부 입력만» 실패한다 | Muddy A-4 |
| `A-2-21` | 웹셸은 올렸는데(201) 실행이 401 | Muddy A-5 + C-4 |
| `A-2-22` | 같은 기능의 엔드포인트가 여럿이다 — «파싱 방식»으로 | Muddy C-5 |
| `A-2-23` | `#` 는 반드시 `%23` | Zipper 5 |
| `A-2-24` | 로그인 잠금이 «클라이언트 쿠키»에만 있으면 그것은 잠금이 아니다 | Monster 1 |
| `A-2-25` | 소스가 손에 있는데 «두드리기»부터 했다 | Monster 2 |
| `A-2-26` | 업로드가 죽어 있다 — 「확장자 필터인가 기능 자체인가」 | Monster 3 |
| `A-2-27` | 「확인 후 폐기한 벡터」를 표로 남길 것 | Monster 7 |
| `A-3-10` | `su` 가 TTY 를 요구한다 — 그러나 «항상» 그런 것은 아니다 | Zipper 9 |
| `A-4-12` | 범용 로컬 권한상승 CVE 는 «5개 반사 명령 뒤»에 던진다 | Zipper 3 |
| `A-68` | 작업이 «끊긴» 것을 실패로 적지 말 것 | Monster 10 |
| `B-1-26` | 경로 트래버설은 「파일 경로 조립」의 문제 + Go `filepath.Join` | Fanatastic B-a |
| `B-1-27` | 임의 파일 읽기를 확보했다 — 무엇을 읽을 것인가 | Fanatastic B-c |
| `B-1-28` | Go 서비스를 만나면 `/debug/pprof/` 부터 | Fanatastic B-g |
| `B-1-29` | 제품별 비인증 버전 엔드포인트 | Fanatastic B-h + C/D/E-1 |
| `B-1-30` | PHP 스트림 래퍼로 LFI 를 RCE 로 확장 | Zipper 1 |
| `B-1-31` | 업로드 파일명이 `time()` 기반이면 브루트로 뚫린다 | Zipper 8 |
| `B-1-32` | CMS 사용자명 «무료» 열거 | Monster 11 |
| `B-1-33` | XXE — DTD·엔티티 배경과 판단 절차 | Muddy B-XXE |
| `B-1-34` | Grafana 체인에서 막히는 지점 `[가정]` | Fanatastic A-신규-5 |
| `B-2-12` | 번들 스택의 구성요소가 «돈다»고 가정하지 말 것 | Monster 13 + 추가 D |
| `B-39` | 와일드카드 인젝션 — 도구별 벡터 | Zipper 2 |
| `B-3-10` | `disk` 그룹 = root, 그리고 `debugfs` 사용법 | Fanatastic B-i + A-신규-3 |
| `B-66` | 해시 접두어로 포맷을 즉시 판별한다 | Muddy B-HASH |
| `B-67` | 해시 vs 가역 암호화 — 시간 배분을 결정하는 구분 | Fanatastic B-e |
| `B-68` | salt · IV · ciphertext 연접 포맷 | Fanatastic B-f |
| `B-69` | 설정 파일의 «주석 처리된» 항목 = 기본값 문서 | Fanatastic B-d + A-신규-2 |
| `B-6-10` | 사이트 콘텐츠 기반 자체 사전이 rockyou 를 이긴다 | Monster 12 |
| `C-5` | 남긴 흔적 (`###` 절) | Monster 추가 F |

**검산** — 항목 번호 182개 / **중복 0**(스크립트 검증). 9 초과 카테고리는 전부 하이픈 한 번 더(`A-1-18`·`A-2-27`·`A-3-10`·`B-1-34`·`B-3-10`·`B-6-10`).

## 2. 병합 17곳 — 전부 append, 기존 문장 무삭제

| 절 | 무엇을 붙였나 | 출처 |
|---|---|---|
| `A-1-10` | 파일 의존성을 세션보다 먼저 만들 것 / `~/.zshrc` 별칭이 비대화형 tmux 에서 안 펴짐 | Monster 4 · 추가 A |
| `A-1-12` | 예열 결과로 방향 정하지 말고 `-p-` 완주까지 유보 | Monster 6 |
| `A-12` | 쌍둥이 명제 — 실패 응답이 「취약하지 않음」을 뜻하지 않음 | Fanatastic A-신규-1 |
| `A-13` | traceback 을 버전 지문으로 + 「멈추는 기준이 절반」 | Muddy C-2 |
| `A-24` | 앱 DB 사용자명 ↔ OS 계정 재사용 3겹 근거 | Fanatastic §3·§7-9 **(제안 누락분 복구)** |
| `A-31` | dash 문법으로 깨지는 페이로드 + 증상별 원인표 4행 | Zipper 4 · §6-② |
| `A-41` | 「추론이 맞아도 끝까지」 · 「없다」 확인 비용 · `whoami /all` 로 확정 금지 | Monster 8 |
| `A-65` | 리버트로 업로드·웹셸 증발 → 절차로 적을 것 | Zipper 6 |
| `B-14` | 도구별 정규화 차이표 · 생소켓 · 인코딩 사다리 · 404 체크리스트 5단계 | Fanatastic B-b |
| `B-16` | XPath 반증 사례(직후 동등검사가 막음) | Monster 7 · 추가 C |
| `B-31` | ⑵ PATH 순서 하이재킹 — 메커니즘·조각 역할표·무반응 진단 | Muddy B-31 확장 |
| `B-38` | B-39 로의 포인터 한 줄 | — |
| `B-44` | ACL → 소유자 확인 순서 · `StartName` 습관 · `[가정]` 유보 | Monster 8 addendum · 추가 E |
| `B-82` | 바이너리 다운로드 검증 순서(`-o` → `file` → `md5sum`) | Fanatastic A-신규-4 |
| `B-84` | SSH 두 겹 인용 깨짐 → `--data-urlencode 'content@파일'` | Monster 5 · 추가 G |
| `C-1` | 무명 CMS 4단계 순서 | Monster 14 |
| `C-3` | `local.txt` 가 홈에 없으면 계정 목록부터 | Muddy C-3 |
| `C-4` | 헤드리스 캡처가 오류 페이지도 성공으로 저장 | Muddy A-6 |
| `D` | Monster·Zipper·Fanatastic·Muddy 시간 배분 4묶음 + 실측 표 2행 | Monster 15·10, Zipper §6-⑤, Fanatastic A-신규-6, Muddy D |
| `E` | 금지 도구 ↔ 수동 대안 짝 표 · Fanatastic Metasploit 카드 절약 | Muddy C-1, Fanatastic §7-16 |

## 3. 기존 서술 정정 — 1건(제목·번호는 그대로)

- **`B-68` 제목만 변경**: `` `salt || IV || ciphertext` — … `` → `salt · IV · ciphertext 연접 포맷 — …`
  - 사유: **Obsidian 위키링크에서 `|` 는 별칭 구분자**라 `[[_PLAYBOOK#B-68. `salt || IV …]]` 앵커가 `B-68. \`salt` 에서 잘려 **깨졌음**(스크립트로 실측 확인). 이 항목은 **이번 웨이브에 내가 만든 것**이라 기존 링크가 없음 — 외부 파급 0.
  - ⚠️ **파생 규율: `_PLAYBOOK` 헤딩에 `|` 를 넣지 말 것.**
- 그 외 기존 항목의 제목·번호·문장 **삭제·변경 0건**.

## 4. 삭제한 것 — 0건

`_PLAYBOOK` 에서 지운 문장 없음. 전부 append.

## 5. 원문 인용 누락 16건 — `.bak` 직접 대조 결과

### Fanatastic (`_backup\Fanatastic.md.bak`, 886행, 2026-08-26 12:27)

- **④필드가 아예 없던 제안 8건** — `B-신규-a`·`b`·`c`·`d`·`e`·`g`·`h`·`i`.
- **④가 「본문과 동일」 자기참조인 제안 6건** — `A-신규-1`~`6`.
- `.bak` 의 삭제 대상 절(`§0`·`§1`(9090·`/metrics`)·`§2-1`·`2-4`·`2-5`·`2-6`·`2-7`·`2-8`·`§3`·`§4-1`~`4-5`·`§6`·`§7`·`§8`)을 전수 대조.

**결과 — 이관 손실 1건 발견, 복구함:**
- ⛔ **`§7-9` + `§3`(「DB 의 사용자명이 OS 계정명과 같으면 재사용을 의심한다」 + 3겹 근거 + `/etc/passwd` 읽는 법)가 어느 제안에도 없었음.** 제안서의 흡수표는 「9·10·11·12·13·14 → B-신규-i」로 적었으나 **B-i 는 `disk` 그룹·`debugfs` 카드**라 §7-9 를 담지 않음. → `A-24` 에 복구 반영.

**손실 없음 확인** — `인과`·`수치`·`[가정]`·「관측 없음」·출처 경로:
- `§2-1` 방어 3지점 표 · Go `filepath.Join` 예시 · **`[가정]`「8.3.0 소스를 직접 대조하지 않았음」** → `B-1-26` 에 원문 유지
- `§2-4` 도구별 정규화 표 6행 · 생소켓 `printf|nc` · 인코딩 사다리 6줄 · **「`../` 8~12개, 깊이 계산은 시간 낭비」** → `B-14`
- `§2-5` 우선순위 표 6행 · 데비안 3종 세트 · **`/etc/shadow` 실패가 「프로세스가 root 가 아니다」의 증거**라는 인과 → `B-1-27`
- `§2-6` 3상태 표 · 907행 빈 값 설명 · **「복호화 성공이 사후 증명」** → `B-69`
- `§2-8` PBKDF2/IV/CFB 역할표 4행 · **CFB segment_size 실측 검증(`ct1 != ct2`)** → `B-68`
- `§4-1`~`4-5` VFS 다이어그램 · 그룹표 6행 · `-R/-w` 위험도표 · 대안 경로표 6행 · **`ls -l` 열 순서와 크기 추론** → `B-3-10`
- `§6-⑤` `(a)`~`(e)` **`[가정]` 헤더 원문 그대로** → `B-1-34`
- `§6-⑥` 시간 배분표 7행 + 블랙홀 2개 · **`[가정]`「측정치가 아님」 원문 유지** → `D`
- `§8`(방어 관점 표 13행) — 각 finding 의 `Vulnerability Fix` 로 분산돼 **순수 삭제가 아님.** `_PLAYBOOK` 대상 아님

### Zipper (`_backup\Zipper.md.bak2`, 940행, 2026-08-26 12:28)

- ⚠️ **총괄 지시의 「원문 인용 4건 누락」은 반증됨** — 제안 1~9 **전부 ④필드를 갖고 있음**(파일 확인).
- ⚠️ **총괄이 지목한 대조 대상 `_backup\Zipper.md.bak` 은 2026-08-20 판**이라 웨이브 12 baseline 이 아님. 실제 개작 직전 백업은 **`.bak2`**(제안서 자체도 `.bak2` 를 인용함). `.bak2` 로 대조함.

**결과 — 제안 자체에서 빠진 이관 손실 5건 발견, 전부 복구:**

| 옛 절 | 내용 | 복구처 |
|---|---|---|
| `§6-④`(1)(2) | `php://filter` 안 나옴 / `zip://` 무반응 — 원인 사다리 **`[가정]` 헤더 포함** | `B-1-30` |
| `§6-④`(3) | 업로드는 되는데 실행 안 됨 — `.htaccess` · include 지점 찾기 | `A-2-21` |
| `§6-④`(4) | 와일드카드 발화 안 함 — 글로브 미매칭 · cwd · `--` 누락 · 1주기 | `B-39` |
| `§7-2` | 확장자 append 형 LFI 를 구분할 것(`/etc/passwd` 부재 ≠ LFI 아님) | `B-1-30` |
| `§7-11` | 「내가 못 읽는 파일은 **root 에게 읽히게** 만든다」 | `B-39` |
| `§7-12` | 비밀번호를 argv 로 넘기는 스크립트는 `ps` 로 샘 | `B-39` |
| `§7-15` | `ls` 가 아니라 `ls -al` — `enox.zip -> /root/secret` 단서 | `B-39` |

**손실 없음 확인** — `§6-①` IP·아카이브명 변경 수치 → `A-65` / `§6-②` 증상표 4행 + 「한 번에 한 변수만」 → `A-31` / `§6-③` mtime 표 4행 + **`[가정]` 실패 사유 미상** → `A-4-12` / `§6-⑤` 시간표 6행 → `D` / `§3-4` **「이 박스에서 필요하지 않았음」 유보** → `A-3-10`.

## 6. 반증한 것 — 제안이 틀렸거나 이미 있던 것

1. **[총괄 지시] Zipper 「원문 인용 4건 누락」** — 반증. 제안 9건 전부 ④필드 존재. 실제 손실은 **「제안에 아예 없는 5건」**(위 §5)이었고 성격이 다름.
2. **[총괄 지시] Zipper 대조 대상 `.bak`** — `.bak` 은 2026-08-20 판. 웨이브 12 baseline 은 `.bak2`. `.bak2` 로 수행.
3. **[총괄 지시] Fanatastic 「12건 누락」** — 실제로 ④필드가 **없는** 것은 8건(`B-a`~`e`·`g`·`h`·`i`), 자기참조 6건까지 세면 14건. 12 라는 수치와 안 맞음. 다만 **대조로 실제 손실 1건(§7-9)을 잡았으므로 지시의 취지는 유효**했음.
4. **[Muddy 제안 B-31]** — 「Muddy 에서 `/dev/shm/netstat` 하이재킹을 실측했다」는 **근거부족**(`~/PG/Muddy/` 에 뒷받침 산출물 0). 제안서 자체가 경고했고, 반영본에 **⚠️ 「박스 귀속으로 인용하지 말 것」**을 붙임.
5. **[Muddy 제안 C-2]** — 「PyPI sdist 11종 대조」는 산출물 부재로 **실측 서술로 안 씀.** `A-13` 에 일반 절차로만 반영하고 「부류까지만 좁혔고 패치 버전은 미확정」으로 적음.
6. **[Muddy 제안 A-6]** — 「헤드리스 캡처 오류 페이지 4장」의 **실물이 없음**(`~/PG/Muddy/` `.png` 0개, `파일보관\` 의 Muddy 2장은 정상 렌더). `C-4` 에 **근거부족 표기**를 붙이고 기법만 일반 지식으로 반영.
7. **[Fanatastic 제안 B-f]** — 「pycryptodome segment_size 가 이 박스 최대의 함정」은 **반증됨**(실제 `decrypt.py` 는 `cryptography` hazmat 사용, 그 API 에 `segment_size` 없음). `B-68` 에 **일반 지식으로만** 남기고 「[[Fanatastic]] 은 이 함정을 겪지 않았음」을 명시.
8. **[Monster 제안 추가 B]** — 「XAMPP `httpd-xampp.conf` 가 `.php7` 도 PHP 핸들러에 물린다」는 **검증되지 않은 단정**. 소스 전문은 이관하지 않고 `A-2-26` 에 ⚠️ 미확인 경고만 남김.
9. **[선재 결함] `_PLAYBOOK` 헤딩의 `|`** — 위키링크 별칭 구분자와 충돌해 앵커가 깨짐. 내가 만든 `B-68` 제목을 즉시 교정(§3).
10. **[선재 결함] 박스 노트 깨진 앵커 2건** — 이번 웨이브 대상 밖이나 ⑤ 「깨진 것 0」 요구에 걸려 함께 교정:
    - `Exfiltrated.md:294` — `` [[_PLAYBOOK#B-82. 파일 전송 후 ls/md5sum으로 확인한다]] `` → 백틱 포함 정확한 제목으로
    - `PwnLab.md:421` — `` `[[_PLAYBOOK#B-33]]` `` → 전체 제목으로
11. **[제안대로 미반영]** Monster 옛 `§0` 여섯째 항목(「쓰기 가능한 바이너리… 아무 의미가 없었다」) — `A-61` 에 **이미 정정된 형태로** 있음. 원문 그대로 넣으면 **반증된 결론이 재유입**되므로 이관하지 않음(제안서 판단과 동일).
12. **[제안대로 미반영]** Monster 옛 `§7` 여섯째(「웹셸로 읽은 플래그는 0점」) — `E` 절에 이미 있음. 중복.

## 7. 기각 5건 — 사유

| 건 | 사유 |
|---|---|
| Monster 옛 `§0`-6 | `A-61` 중복 + 결론이 반증됨(위 6-11) |
| Monster 옛 `§7`-6 | `E` 절 중복(위 6-12) |
| Monster 추가 B — `$forbidden_types` 소스 전문 | 이 박스에서 업로드 경로가 안 쓰였음 → **심사관 기준 과잉**(깊이 기준). 미검증 단정 경고만 `A-2-26` 에 |
| Monster 추가 G — Kali 운영 정리 상세·감사 개정 이력·색인 운영 | **작업 과정 기록**이라 `CLAUDE.md` §4 로 본문 금지. `C-5` 에 규율만 일반화 |
| Muddy 옛 `§6-⑧` 시간표 | mtime 근거 부재 → `D` 절 **「실측」이 아니라 「참고」**로 격하 반영 |

## 8. 검산

- **행수** 2917 → **3833**(+916)
- **항목 번호** 182개 · **중복 0**(스크립트)
- **`[[_PLAYBOOK#…]]` 앵커 전수** 319건 · **박스 노트 깨진 것 0**
  - 남은 15건은 전부 `_AUDIT\*`·`_WRITEUP-STANDARD.md` 의 **플레이스홀더**(`…`·`...`·제목 없는 `A-31`/`B-84`/`B-23. …`) — 실제 내비게이션 링크가 아님. 선재 상태이며 이번 작업과 무관
- **노트별 앵커 추가** — `Fanatastic.md` **12** · `Muddy.md` **10** · `Zipper.md` **10** · `Monster.md` **12** (합 44). 전부 `## 관련` 절에만 추가, 본문 무수정
- **프론트매터 안전 확인** — `_PLAYBOOK.md` frontmatter 무수정. 4개 노트에 `  - tech/… #주석`·`cves: […] #주석` 형태 **0건**(스캔)
- ⛔ `refresh.ps1`·`extract.py` **미실행**. `_STATUS.md`·`_WRITEUP-STANDARD.md` **미수정**

## 9. 총괄에 올림

- **색인 갱신 필요** — `_PLAYBOOK.md` 와 4개 박스 노트가 변경됨. 파이프라인은 공유 인프라라 총괄 몫(`CLAUDE.md` §5)
- **`_PLAYBOOK.md` 가 git 미추적(`??`)** — 3,833행 단일 자산인데 이력 보호가 없음. 커밋 여부는 총괄 판단
- **파생 규율 제안** — `_WRITEUP-STANDARD` 또는 `_PLAYBOOK` 머리말에 **「헤딩에 `|` 금지(위키링크 별칭 구분자와 충돌)」** 한 줄 추가. 공유 문서라 직접 안 고침
