# Graph — 문체 검토 (pg-doc-reviewer, 2026-08-21)

## 범위
`03. PG\Graph.md` 문체만 대상. 사실 검증(날조·출처 대조)은 하지 않음 — writeup-auditor 몫.

## 고친 것
분류: 전부 **문체**(서술형 종결 → 명사 종결형 개조식). 약 99개 산문 줄(코드펜스·캡션·헤더 포함) 대상, 패턴은 전부 다음 중 하나:
- `~했다/~았다/~였다` → `~함/~했음`
- `~이다/~다` → `~임`
- `~된다/~한다` → `~됨/~함`
- `~있다/~없다` → `~있음/~없음`
- 헤더 6곳도 동일 적용: L232, L254, L270, L291, L418, L422, L426, L430, L438 (`sudo -l 원문을 안 남겼다`→`안 남김` 등)

미드클로즈(`-고`·`-며`·`-서`·`-는데` 등 연결어미)는 서술형이어도 그대로 둠 — 문장 최종 종결만 대상. 인용부호 안 문장("찾은 게 없다" 등)과 슬로건성 인용("확인은 짧게, 미련은 갖지 않는다")은 저자의 의도된 인용이라 원문 그대로 보존.

전문은 `git diff` 또는 백업 대조로 확인 가능 — 개별 위치 나열은 99건이라 이 파일에 표로 옮기지 않음(파일 자체가 diff 대체).

## 삭제한 것
없음. 순수 종결형 치환만 수행.

## 넘긴 것 — 사실 의심 지점
없음. 사실 판정은 하지 않았고, 읽는 과정에서 날조 의심 신호도 발견되지 않음(코드펜스·수치·경로는 손대지 않았고 훑어본 바 특이사항 없음).

## 손대지 않기로 한 것
- `[가정]` 10곳 · 「관측 없음」 9곳 — 마커 자체는 무변경, 마커가 딸린 문장의 종결만 조정
- 코드펜스 32개 전부 — byte-identical 확인(스크립트 대조)
- `출처:` 캡션 25곳, `![[PG-Graph-*.png]]` 2곳 — 무변경
- 타겟 pty 프롬프트(`jane@graph:~$`·`root@graph:~#`) — 코드펜스 안이라 무변경
- 6장 인과("~해서"/"~여서") — 전부 원형 유지, 종결부만 변환

## 검산
- `[가정]`: 10 → 10
- 「관측 없음」: 9 → 9
- 출처 캡션: 25 → 25
- 이미지 링크: 2 → 2
- 코드펜스 32개: byte-identical (old==new, diff 0)
- 숫자 토큰 842개: 시퀀스·개수 100% 일치(순서까지 동일)
- 행수: 478 → 478 (변화 없음)
- 잔여 서술형 종결 재스캔(정규식 `다[.):]`, 코드펜스 제외): 0건

백업: `03. PG\_backup\Graph.md.pre-docreview-20260821.bak`
