---
tags:
  - type/audit
  - platform/pg
---
# Cockpit — 2차 적대적 검증 (352행 개작본 대상, 2026-08-26)

대상: `03. PG\Cockpit.md` (670행 감사본 → **352행 개작본** → 368행 정정본)
기준선: `03. PG\_backup\Cockpit.md.bak2`(670행) · git `607fbc5:"03. PG/Cockpit.md"`(**148행 사람 원본**, 개작 전 1차 사료)
증거원: `~/PG/Cockpit/`(6파일) · `파일보관\` 스크린샷 12장(**전량 직접 열람**) · `~/.zsh_history` 1160~1210 · Kali 직접 실행
타겟에 패킷 없음 — 박스 정지됨.

---

## 0. 최종 판정

| 항목 | 판정 |
|---|---|
| 플래그 진행도 | **완료 2/2** (볼트 기준 = 플래그 확보). 두 값 모두 스크린샷 픽셀 확대로 재확인 |
| OSCP 채점 기준 | **0/2.** 두 플래그 다 Cockpit 브라우저 내장 터미널(web-based shell) 산출물 |
| pty 판정 | **타겟 pty 아님 = Cockpit 웹 터미널.** 단 `james@blaze:~$`·`bash-5.0#` 은 **실측 표식이므로 전부 보존**했다 |
| 날조 | **없음.** 단 «raw 출력 3건이 개작 과정에서 훼손»돼 있었고 전부 원상 복구 |

**중요 — 이 노트의 터미널 블록 상당수는 「사람이 2026-06-26 에 직접 붙여넣은 원본 노트」가 유일한 출처다.**
git `607fbc5` 의 148행 원본이 그 사료이고, 여기 실린 `sudo tar` 실행 블록·feroxbuster 배너·nmap 전문이 전부 그 안에 이미 있었다. 「산출물에 없다 → 근거부족」까지는 맞지만 **「에이전트가 지어냈다」로 올라가지 않는다.**

---

## 1. 고친 것

| 위치(문자열) | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| nmap TRACEROUTE `2   66.76 ms 192.168.251.1` | **raw 출력 훼손.** `~/PG/Cockpit/nmap.log` · 148행 원본 · 670행 기준선 모두 `192.168.45.254`. 개작이 hop3 값을 hop2 에 복사 | `2   66.76 ms 192.168.45.254` 로 복구 |
| feroxbuster 블록 배너 | **raw 출력 훼손.** `👌  Status Codes          │ [200]` 행과 `🏁  Press [ENTER] …`·구분선 2행이 삭제됨. Kali 에서 같은 인자로 실행해 그 행이 실제로 출력됨을 확인 | 원본대로 전량 복구 |
| feroxbuster 블록 프롬프트 | 대화형 Kali 프롬프트 `┌──(kali㉿kali)-[~/PG/Cockpit]` / `└─$ feroxbuster …` 삭제. **레거시(사람 대화형) 실측이라 CLAUDE.md §3 상 삭제 금지 대상** | 명령줄과 함께 복구. 코드펜스도 `text`→`bash` 로 원복 |
| feroxbuster 블록 출처 `— 출처: ~/PG/Cockpit/ferox_80.txt` | **출처 오귀속.** `ferox_80.txt` 는 **1일차 `192.168.150.10`** 을 `directory-list-2.3-medium.txt` 로 돌린 **다른 회차**이고 200 응답 6건에 `login.php` 가 없다. 노트에 실린 것은 2일차 `192.168.161.10` 회차이며 `-o` 없이 돌아 stdout 파일이 없음 | 출처를 「원 노트 붙여넣기 + 상태파일 `ferox-http_192_168_161_10_-1782438695.state` 대조」로 정정. 상태파일 응답 7건이 URL·행/단어/바이트 수까지 전부 일치함을 확인 |
| `이전까지의 실패한 열거 회차(… 1일차 80 스캔 무응답)` | **사실 오류.** 1일차 80 스캔은 무응답이 아니라 200 응답 6건을 냈다 | 1일차 결과 6건을 명시하고, `blocked.html` 존재 + 9090 장시간 스캔 직후라는 정황으로 **차단 `[가정]`** 서술로 교체(670행 기준선의 분석을 복원) |
| (같은 문단, 신규) 워드리스트 차이 가설 | 내가 초안에 넣었다가 **자기 반증.** `grep -n '^login$'` 결과 `directory-list-2.3-medium.txt`·`directory-list-lowercase-2.3-medium.txt` **양쪽 모두 53행** — 워드리스트 차이로 설명 불가 | 가설을 빼고 실측(53행)을 근거로 명시적으로 배제 |
| `sudo tar` 블록 첫 줄 | **원본 1행 유실.** 148행 원본은 `james@blaze:~$ cd /tmp` 로 시작하고, 마지막이 `bash-5.0# ` 로 끝남 | 두 줄 복구 |
| `⚠️ 이 블록은 … 이 감사에서 별도 스크린샷으로 재확인하지 못했다.` | **감사 과정 문구 누출**(CLAUDE.md §4 위반) | 「대응 스크린샷 없음 — 근거부족」 + 출처 캡션으로 교체. `[가정]`·근거부족 표시 자체는 유지 |
| (같은 자리, 신규) 13:36 회차 | 노트가 **자기 스크린샷에 반박당하는 자리**였다. `133613.png` 은 `~` 에서 `echo "" > '--checkpoint=1'` 로 만들고 `privesc.sh` 에 `echo 'kali ALL=(root) NOPASSWD: ALL' > /etc/sudoers` 를 넣은 **다른 회차**인데, 노트에는 `/tmp` + `touch --` + `chmod +s` 블록만 있어 독자가 모순으로 읽는다 | 13:36 이 별개 회차(폐기)임을 스크린샷 출처와 함께 3줄로 명시 |
| `원 노트 기준 87×14px 픽셀 비교 — 이 감사에서 재실측하지 않았으나 반증 근거 없음` | **감사 과정 문구 누출** + 수치 부정확 | **직접 재실측**해 값 교체. 초록 프롬프트 구간은 **82×14px**(87 아님)이고, `135233` 기준 크롭과 `111320`·`130129`·`133613` 이 전부 `np.array_equal → True`, 평균 절대차 `0.0` |
| `— 출처: ~/.zsh_history(1189행 부근)` | 행번호 부정확 | `1193행` (Kali `grep -n` 실측) |
| `(CWE-289 계열 인증 우회)` | **틀린 일반 지식.** CWE-289 는 Authentication Bypass by Alternate Name(이름·경로 변형)으로 `LIKE` 와일드카드 매칭과 무관. 개작본에서 신규 등장 | CWE 인용 삭제(다른 번호로 대체하지 않음 — 심사관 재현에 불필요) |
| `앱 로그인 폼에는 james/… 가 거부됨(\`Invalid user or password.\`)` | **미출처 관측 주장.** 670행 기준선·148행 원본 어디에도 없고 스크린샷 12장에 해당 화면 없음. 개작본에서 신규 등장한 **UI 에러 문자열 인용** | 인용 문자열 제거, 「관측 없음」으로 강등 |
| `/` → 302 `login.php` | **미출처.** ferox 는 `/` 를 200(3349c)으로 기록했고 302 관측 기록 없음 | 문장 제거, `login.php` 로 바로 진입하는 서술로 교체 |
| `셸을 잡자마자 sudo -l 을 확인` | **시간 서사 반증.** `local.txt` 11:13(`111320.png`) → `sudo -l` 13:01(`130129.png`), 약 108분 공백 | 「13:01, 약 108분 뒤」로 정정 |
| `**Vulnerability Explanation:** 세 요소가 체인됨.` | **내부 모순.** 바로 아래 불릿이 4개 | 「네 요소」 |
| 프론트매터 | `manual_cves` 미선언. 본문이 EDB 49390/49397 을 **반증 목적**으로 언급 | `manual_cves: true` 추가(현재 `CVE-` 문자열 0건이나 향후 오염 방지) |

## 2. 삭제한 것

**본문 삭제는 3건이며 전부 「반증된 서술」이다.** 되살릴 수 있게 원문을 인용한다.

1. > `login.php` 의 인증 쿼리가 … 인증 통과**(CWE-289 계열 인증 우회)**

2. > 앱 로그인 폼에는 `james/canttouchhhthiss@455152` 가 거부됨(`Invalid user or password.`) — 앱 로그인이 어느 저장소를 참조하는지는 관측 없음. Cockpit(PAM, tcp/9090)에는 그대로 통했다.

3. > `/` → 302 `login.php`. 임의 한 글자를 입력하고 제출:

터미널 블록·플래그·IP·해시·자격증명·명령 원문은 **한 바이트도 삭제하지 않았다.** 순증분만 있다(복구 5행 + 설명 3문단).

## 3. 반증한 것 — 지적으로 올랐다가 확인 결과 노트/작성자가 옳았던 것

| 지적 후보 | 확인 결과 |
|---|---|
| 「스크린샷 12장 중 2장 무관」이 작성자의 새 창작 아닌가 | **작성자가 옳다.** 두 장을 직접 열람 — `20260625135833.png` = boroCTF 2026 참가 인증서(4Leaf, #15), `20260626142047.png` = **Gerapy** 로그인 화면(zsh_history 상 직후 작업한 [[Levram]] / CVE-2021-43857). 둘 다 Cockpit 무관 |
| 「IP 이원화 150.10→161.10」이 지어낸 서사 아닌가 | **일치.** 상태파일 파일명 두 개가 `…_192_168_150_10_9090_…`(06-25 11:12)와 `…_192_168_161_10_…`(06-26 10:51), zsh_history 1179~1192 의 feroxbuster 타겟 전환과도 일치 |
| 「sudo tar 성공 블록은 근거부족」이 사실상 날조 신호 아닌가 | **아니다. 등급은 근거부족이 맞고 그 이상 올라가지 않는다.** 이 블록은 **git `607fbc5` 의 148행 사람 원본에 이미 있다** — 에이전트 개작 이전 사료다. 13:01~13:36 스크린샷 부재는 사실이나 부재 증거의 상한은 근거부족 |
| 개작이 Kali 프롬프트 `┌──(kali㉿kali)` 를 지운 것 전부가 위반 아닌가 | **절반만 위반.** feroxbuster 블록은 사람이 대화형으로 친 것이라 삭제가 위반(복구함). 반면 **sudoers `ztest` 검증 블록**의 Kali 프롬프트는 2026-08-20 감사 에이전트가 비대화형 `ssh kali "…"` 로 돌린 것에 붙인 **창작**이므로 개작본이 지운 것이 **옳다** — 복구하지 않았다 |
| 타겟 pty 프롬프트를 지워야 하는가 | **아니다. 전부 보존.** `james@blaze:~$`·`james@blaze:/tmp$`·`bash-5.0#` 은 스크린샷 4장에서 픽셀 대조로 실측 확인됨. Fikklish 사고(감사자가 실측 pty 를 제거)를 반복하지 않았다 |
| 이전 감사의 「87×14px」 수치 | **부정확.** 직접 재실측 결과 **82×14px**. 결론(동일 렌더러)은 그대로 유지되고 오히려 강화됨(`array_equal → True`) |
| `flag2.txt = RWFzdGVyRWdn` 이 또 훼손되지 않았는가 | **정확.** 6배 확대 판독 결과 스크린샷과 일치, Kali `base64 -d` → `EasterEgg` |
| `searchsploit Cockpit` 서술 | **정확.** Kali 재실행 5건 — Cockpit CMS 3건(0.11.1 / 0.4.4<0.5.5 / 0.6.1) + openITCOCKPIT 1건 + Cockpit v234 1건. 「5건 중 4건이 다른 제품」·「49397 은 제품은 맞으나 234 라 198–220 밖」 둘 다 맞음 |
| `sudoers(5)` 인용 2건 | **정확.** Kali `man 5 sudoers` 원문 확인 — "Wildcards in command line arguments should be used with care."(1015행), "a slash does get matched by wildcards since command line arguments may contain arbitrary strings"(1011–1013행) |
| MySQL 에러 텍스트 5행 | **정확.** `105549.png` 6~14배 확대 판독 — 줄바꿈 위치와 끝의 `'%%" at line 1` 까지 일치. 입력 필드도 `'` 한 글자 확인 |
| base64 자격증명 2건 | **정확.** `105320.png`(인코딩값)·`110833.png`(Burp Decoder 디코딩값) 대조 일치 |
| 두 Initial Access·4항목·Port Scan 표·nmap raw 라인 | **구조 결손 없음.** `22/tcp   open  ssh …` 원문 라인 보존돼 `extract.py PORT_RE` 정상 |
| `_PLAYBOOK` 이관 손실 | **없음.** `Cockpit-playbook.md` 296행 12건이 옛 0·6·7장을 인과·소요시간·`[가정]`·출처 경로까지 살려 담고 있음. 다만 **아직 `_PLAYBOOK.md` 본체에 병합되지 않은 「제안」 상태** — 총괄 판단 필요 |

## 4. 근거 출처

- Kali 산출물 — `~/PG/Cockpit/{nmap.log, 49390.py, ferox_80.txt, ferox.txt, ferox-http_192_168_150_10_9090_-1782353548.state, ferox-http_192_168_161_10_-1782438695.state}` (6파일 전량)
- git — `607fbc5:"03. PG/Cockpit.md"`(148행 사람 원본), `5d2ceca`(670행 감사본), `03. PG\_backup\Cockpit.md.bak`·`.bak2`
- 스크린샷 12장 전량 직접 열람 + PIL 로 6~14배 확대 판독 및 RGB 배열 대조
- `~/.zsh_history` 1160~1210 (`ssh james@` **0건**, `base64 -d …` **1193행**)
- Kali 직접 실행 — `feroxbuster`(배너 재현), `searchsploit Cockpit`, `man 5 sudoers`, `grep -n '^login$'` 워드리스트 2종, `echo -n 'RWFzdGVyRWdn' | base64 -d`, `ls /etc/sudoers.d/`

## 5. 총괄 판단이 필요한 것

- **색인 갱신 필요.** `manual_cves: true` 추가 + 본문 변경. `refresh.ps1` 은 공유 인프라라 실행하지 않았다.
- **`Cockpit-playbook.md` 12건이 `_PLAYBOOK.md` 본체에 미병합.** 이관 「제안」 상태로만 존재 — 병합 전에는 박스 노트에서 걷어낸 시행착오가 검색되지 않는다.
- **`_STATUS.md` 판정**: 완료 2/2. 단 시험 채점 기준으로는 0/2 이므로, 상태 문서에 그 단서를 다는지 여부는 총괄 결정.
