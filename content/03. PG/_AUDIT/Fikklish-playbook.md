# Fikklish → _PLAYBOOK 이관 제안

작성: pg-note-forge (개작 작업 중 산출). `_PLAYBOOK.md` 는 열지 않았음 — 아래 제안을 line-manager 가 반영.
검산: 노트 645→490행, 삭제분(0/6/7장 전체 + 8장 일부 + 9장 검색 습관 1건) = 아래 14건 제안으로 전량 이관.

---

## 1. [병합 → A-41. 셸은 잡았는데 권한상승 실마리가 없다]

**넣을 본문:** `.bash_history` 가 `/dev/null` 이어도 다른 클라이언트 히스토리를 본다(`.psql_history`·`.mysql_history`·`.rediscli_history`·`.python_history`·`.viminfo`·`.lesshst`). [[Fikklish]] 실측 — tom 홈의 `.bash_history` 는 `/dev/null` 링크였으나 `.psql_history` 는 살아 있었고, 거기 남은 평문 DB 비밀번호(`ALTER USER WEBLATE PASSWORD '...'`) 중 최신 값이 tom 의 시스템(SSH/sudo) 비밀번호로 재사용돼 있었음. 셸 히스토리만 지우고 클라이언트 히스토리를 빠뜨리는 것이 흔한 패턴.

**지우기 전 원문(구 7장 7번):**
> 7. **`.bash_history` 가 `/dev/null` 이어도 다른 히스토리를 본다.** `.psql_history` · `.mysql_history` · `.rediscli_history` · `.python_history` · `.viminfo` · `.lesshst`. 이 박스는 정확히 그 틈에 비번을 뒀다.

**지우기 전 원문(구 4장 열거 절):**
> `.bash_history` 는 `/dev/null` 로 링크돼 있는데 **`.psql_history` 는 살아 있다**. 히스토리 파일을 지울 때 셸 것만 챙기고 클라이언트 것을 빠뜨리는 전형이다.

---

## 2. [병합 → A-41. 셸은 잡았는데 권한상승 실마리가 없다]

**넣을 본문:** `id` 의 `groups=` 를 끝까지 읽는다 — uid 만 보고 지나가기 쉬우나 뒤쪽이 별도 경로 목록. [[Fikklish]]: tom 이 `110(lxd)` 를 갖고 있었으나(`groups=1000(tom),4(adm),24(cdrom),30(dip),46(plugdev),110(lxd)`) `sudo -l` 이 이미 열려 있어 lxd 경로는 시도하지 않았음`[가정]`. lxd 그룹 멤버는 컨테이너를 만들어 `security.privileged=true` 로 호스트 `/` 를 마운트하면 사실상 root — lxd 데몬이 실제로 떠 있었는지, 이미지가 있었는지는 미확인.

**지우기 전 원문(구 7장 10번):**
> 10. **`id` 는 uid 만 보지 말고 `groups=` 를 끝까지 읽는다.** 이 박스의 tom 은 `110(lxd)` 였다 — `sudo` 가 안 열렸으면 그쪽이 다음 경로였다. `lxd`·`docker`·`disk`·`shadow`·`adm` 은 각각 별개의 길이고, 셸을 잡은 첫 30초에 공짜로 보이는 정보다(4장).

**지우기 전 원문(구 4장 "안 써먹은 세 번째 경로"):**
> `110(lxd)`. **[가정] — 이 박스에서는 시도하지 않았다.** lxd 그룹은 사실상 root 와 동등하게 취급되는 대표적인 그룹이다. lxd 데몬이 root 로 도는 상태에서 그룹 멤버는 컨테이너를 만들 수 있고, 컨테이너에 호스트 루트(`/`)를 `security.privileged=true` 로 마운트해 붙이면 컨테이너 안의 root 로 호스트 파일을 그대로 쓸 수 있다. `sudo` 가 아예 없거나 비번을 못 구했다면 이쪽이 다음 후보였다.
> 확인하지 않은 것 — 이 박스에 **lxd 데몬이 실제로 떠 있었는지, 사용 가능한 이미지가 있었는지는 보지 않았다.**

(참고: 이 두 건은 새 노트 본문에는 이미 요약 형태로 남겨 두었음 — 완전 삭제가 아니라 "일반화된 반사"만 이관 대상.)

---

## 3. [병합 → A-31. 리버스셸이 안 붙는다]

**넣을 본문:** RCE 확인은 ICMP 로 먼저 — 리버스셸을 바로 던지면 "실행 실패" 와 "아웃바운드 차단" 이 구별 안 됨. [[Fikklish]]: `ping -c 3` 페이로드 1회 실행 지시에 `tcpdump` 상 ICMP id 가 1·2 두 번(각 seq 1~3) 완주함 — Weblate 가 컴포넌트 검증 중 hg 를 두 번 호출하는 것으로 추정되나 호출 횟수를 직접 센 기록은 없음`[가정]`. 실무 함의: 페이로드가 여러 번 실행될 수 있으니 리버스셸 전에 ICMP 로 횟수를 먼저 확인.

**지우기 전 원문(구 7장 9번):**
> 9. **RCE 확인은 ICMP 로 먼저.** 리버스셸을 바로 던지면 "실행 실패" 와 "아웃바운드 차단" 이 구별되지 않는다. `ping` 한 줄이면 두 층을 분리할 수 있다.

**지우기 전 원문(구 3-2장, ICMP 이중실행 부분):**
> `ping -c 3` 을 한 번 시켰는데 **`id` 가 1 과 2 로 각각 seq 1~3 씩, 즉 두 번 완주했다.** ping 프로세스가 두 개 떴다는 뜻이다. **[가정]** 원인은 Weblate 가 컴포넌트 검증 과정에서 hg 를 두 번 부르기 때문으로 보이지만, 호출 횟수를 직접 센 기록은 없다.

---

## 4. [병합 → A-31. 리버스셸이 안 붙는다, "리스너 함정" 문단]

**넣을 본문:** [[Fikklish]] — 다른 박스([[PwnLab]]) 리스너가 같은 포트(443)를 이미 점유한 상태에서 `sudo rlwrap nc -lvnp 443 | tee ...` 로 새 리스너를 띄운 사고. `ss -lntp` 로 PID 특정해 자기 것만 kill(1시간 45분째 살아있던 남의 프로세스를 죽이지 않음). 동시에 `| tee` 파이프 때문에 nc 출력이 블록 버퍼링되어 `tmux capture-pane` 에 접속 여부가 안 보였음 — 리스너는 파이프 없이 띄우고 기록이 필요하면 `tmux pipe-pane` 이나 별도 로그로.

**지우기 전 원문(구 6-5, 전문):**
> ### 6-5. 리스너를 다른 박스와 공유했다 (약 6분)
> 첫 리버스셸 리스너를 `sudo rlwrap nc -lvnp 443 | tee ...` 로 띄웠는데 두 가지가 겹쳤다.
> - 다른 세션의 **PwnLab 리스너가 이미 443 을 잡고 있었다.** `ss -lntp` 로 PID 를 특정해 보니 1시간 45분째 살아 있는 남의 프로세스였다. `pkill` 을 쓰면 그쪽 작업까지 죽는다 — **내 PID 만 골라서 `kill -9`** 했다.
> - `| tee` 로 파이프한 탓에 nc 의 출력이 블록 버퍼링돼 `tmux capture-pane` 에 아무것도 안 보였다. 셸은 붙었는데 붙은 줄 몰랐다. **리스너는 파이프 없이 띄우고, 기록이 필요하면 `tmux pipe-pane` 이나 로그를 따로 잡아라.**

---

## 5. [신규 — A. 증상별, 「판단·검증(메타)」류 적합]

**제목 후보:** 비동기 큐 앱의 "성공" 메시지는 "큐에 넣었다"는 뜻일 수 있다

**넣을 본문:** [[Fikklish]] — `REGISTRATION_OPEN=True` + `EMAIL_HOST=localhost:25` 를 보고 가입 확인 메일을 SMTP 캐처로 가로채려 시도(27분 소요, 완전 헛다리). 가입은 매번 "메일 발송됨" 화면(`/accounts/email-sent/`)을 반환했으나 `tcpdump` 로 타겟→Kali 25번 연결이 **0건**이었음. root 획득 후 확인한 원인 — `ps` 에 celery 워커가 아예 없음(Weblate 의 가입 메일은 `send_mails.delay()` 로 celery 큐에 들어가고, 워커가 없으면 큐에 쌓인 채 끝). `ss -lntup` 에도 25번 리스너 자체가 없었음(exim4 는 설치돼 있으나 데몬 미기동). 같은 원인이 정리 단계에서도 재현 — Weblate UI 로 프로젝트 삭제 시 `project_removal.delay()` 도 실행되지 않아 ORM 으로 직접 지움. **교훈: 성공 화면은 "태스크를 큐에 넣었다"는 뜻일 뿐, 워커 존재 여부를 확인하지 않고서는 완료로 판단하지 말 것. 아웃바운드 패킷이 0이면 5분 안에 손절.**

**지우기 전 원문(구 6-3, 전문):**
> ### 6-3. 가입 확인 메일을 가로채려다 27분 (완전한 헛다리)
> `REGISTRATION_OPEN=True` 에 `EMAIL_HOST=localhost:25` 를 보고 "메일 주소를 우리 Kali 로 지정하면 활성화 링크가 온다" 는 그림을 그렸다. 25번 SMTP 캐처를 파이썬으로 짜서(`smtpcatch.py`) tmux 로 띄우고 `swaks` 로 자체 시험을 먼저 통과시켰다.
> [swaks 테스트 로그 — smtp_capture.log, 이 파일에 남은 연결은 이 자체시험 한 건이 전부다]
> 주소를 바꿔가며 가입을 반복했다.
> - `@192.168.45.207` — Weblate 는 받아주지만 MTA 가 숫자 도메인을 호스트명으로 DNS 조회해 실패한다.
> - `catchx@[192.168.45.207]` — Django `EmailValidator` 가 도메인 리터럴을 허용해 통과. 가입 성공(`/accounts/email-sent/`). **그래도 아무것도 안 왔다.**
> - `@192.168.45.207.nip.io`, `@192-168-45-207.sslip.io` — 공개 와일드카드 DNS 로 이름 해석 문제를 우회. **역시 무응답.**
> `tcpdump -i tun0 host 192.168.248.19 and not tcp port 8000 and not tcp port 80` 를 걸어두고 봤는데 **타겟에서 25번으로 들어온 연결이 하나도 없었다.** 나중에 저장된 캡처(`reg_smtp.pcap`, 19:04~19:32)를 다시 세어봐도 `tcp port 25` 는 **0패킷**이고, 그 필터에 걸린 4139패킷은 전부 6-2 의 hydra SSH·ICMP 확인·443 리버스셸이다.
> root 를 잡고 나서야 원인이 확정됐다(`harvest_root.txt`):
> - `ps` 어디에도 **celery 워커가 없다.** 프로세스 목록에서 `celery` 는 0건이고, weblate 로 도는 것은 `runserver` 부모(PID 900)와 자식(PID 16427) 둘뿐이다. Weblate 4.11 의 가입 메일은 `send_mails.delay(...)` 로 celery 큐에 들어가므로, 워커가 없으면 큐에 쌓이고 끝이다.
> - `ss -lntup` 의 LISTEN 목록에 **25번이 없다.** 떠 있는 건 8000(weblate)·6379(redis)·80(apache)·53(resolved)·22(sshd)·5432(postgres) 뿐이다. `exim4` 패키지는 설치돼 있지만(`exim4 4.95-4ubuntu2.6`, `/usr/sbin/exim4` 존재) 데몬이 안 뜬다.
> 같은 원인이 정리 단계에서도 한 번 더 나왔다 — Weblate UI 로 프로젝트를 지우면 `project_removal.delay()` 라 **삭제도 실행되지 않는다.** 결국 Django ORM 으로 직접 지웠다.
> 교훈: **가입 폼이 "메일을 보냈습니다" 라고 말한다고 메일이 나간 것이 아니다.** 비동기 큐를 쓰는 앱에서 성공 화면은 "태스크를 넣었다" 는 뜻일 뿐이다. 27분을 태우기 전에 "밖으로 나가는 패킷이 하나도 없다" 를 5분 안에 손절 신호로 읽었어야 했다.

**지우기 전 원문(구 7장 11·12번):**
> 11. **비동기 큐를 쓰는 앱에서 "보냈습니다" 는 "큐에 넣었다" 는 뜻이다.** 메일·알림·삭제가 전부 워커에 달려 있으면 워커가 죽은 순간 UI 는 성공을 말하면서 아무 일도 안 한다. 이 박스는 그 때문에 **가입 메일도, 프로젝트 삭제도** 안 됐다.
> 12. **시간 배분** — 이 박스의 손절 지점은 명확하다. **메일 가로채기에서 아웃바운드 패킷이 0이면 5분 안에 접는다.** 나는 27분을 썼다. 반대로 로그인만 되면 이후는 9분짜리다.

---

## 6. [신규 — A. 증상별]

**제목 후보:** 웹 로그인에 계정 잠금/비번 무효화 정책이 있다 — 브루트하면 계정을 죽인다

**넣을 본문:** [[Fikklish]] — Weblate 로그인 5회 실패 후 "Too many authentication attempts... 10 minutes" 락아웃(`RATELIMIT_ATTEMPTS=5`, IP당 5회/5분). 게다가 `AUTH_LOCK_ATTEMPTS=10` — **계정당 마지막 로그인 이후 실패 10회면 그 계정 비밀번호가 `set_unusable_password()` 로 무효화됨**(소스 `weblate/accounts/models.py`, weblate-4.11 태그). 세 가지 오판:
① `X-Forwarded-For` 로 우회 시도 — 실패(`IP_BEHIND_REVERSE_PROXY=False` 라 `REMOTE_ADDR` 그대로 사용, 소스로 사후 확인. 추측을 먼저 시도하고 소스로 나중에 확인한 순서가 거꾸로였음).
② 락아웃 상태에서 재시도 루프를 돌림 — `weblate/utils/ratelimit.py` 의 `cache.set(key, attempts, LOCKOUT)` 이 **시도할 때마다 락아웃 타이머를 새로 갱신**해 스스로 10분을 계속 연장. 루프를 멈추고 조용히 기다리는 것이 유일한 해법.
③ `/admin/login/` 이 별도 경로라 레이트리밋을 우회할 거라 기대 — `weblate/wladmin/sites.py` 의 `AdminLoginForm(LoginForm)` 이 **같은 레이트리밋을 공유**함을 소스로 확인. 존재하지 않는 계정으로 먼저 12회 시험해 정책만 확인한 것(실계정 소모 없이)은 옳은 판단이었음.
결과적으로 실제 admin 계정 실패 시도를 5회까지 쓴 상태에서 6번째(`Niffenegger`, 대문자 오판)도 틀렸고 7번째(`niffenegger`)에 성공 — 잠금까지 4회 남기고 끝남. **cewl 348단어 전체를 웹 폼에 먹이는 것은 5회/5분 정책상 애초에 불가능한 계획**(6시간+ 소요, 그 전에 락아웃).
**교훈: "Too many attempts" 를 본 순간 남은 길은 ① 힌트로 후보를 3~5개로 줄이거나 ② 다른 진입점을 찾는 것 둘뿐. 존재하지 않는 계정으로 정책을 먼저 떠보고, 실계정 시도 예산을 의식할 것.**

**지우기 전 원문(구 6-1, 전문):**
> ### 6-1. 비밀번호를 브루트포스로 풀려다 계정을 죽일 뻔했다 (약 45분)
> Editors Note 를 본 직후 후보 8개를 만들어 `curl` 로 순서대로 던졌다. **다섯 번째에서 막혔다.**
> `Too many authentication attempts from this location. Please try again in 10 minutes.`
> 여기서 세 번 연속으로 잘못 판단했다.
> 1. **`X-Forwarded-For` 로 우회하려 했다.** 실패. 나중에 설정 덤프에서 `IP_BEHIND_REVERSE_PROXY = False` 를 확인했고, 소스(`weblate/utils/request.py`)도 그 경우 `REMOTE_ADDR` 을 그대로 쓴다. **추측으로 시도하고 소스로 확인하는 순서가 거꾸로였다.**
> 2. **재시도 루프를 돌렸다.** `weblate/utils/ratelimit.py` 를 읽어보니 잠금 상태에서의 시도가 `cache.set(key, attempts, LOCKOUT)` 으로 **락아웃 타이머를 매번 새로 감는다.** 기다리는 대신 두드린 탓에 스스로 10분을 계속 연장하고 있었다. 루프를 죽이고 조용히 기다리는 것이 유일한 해법이었다.
> 3. **Django admin 로그인이 별도 경로일 거라 기대했다.** `/admin/login/` 이 Weblate 화면이 아니라 Django 관리자 폼이길래 우회로인 줄 알았다. 존재하지 않는 계정으로 12회 시험한 결과 5회째부터 같은 메시지가 떴다. 소스를 보니 `weblate/wladmin/sites.py` 가 `login_form = AdminLoginForm` 을 지정하고 `AdminLoginForm(LoginForm)` 이라 **같은 레이트리밋을 탄다.** 존재하지 않는 계정으로 먼저 떠본 건 잘한 판단이었다 — 실재 계정으로 시험했으면 다음 항목에 걸렸다.
> 그리고 정말 위험했던 것:
> ```python
> if self.activity == "failed-auth" and self.user.has_usable_password():
>     failures = AuditLog.objects.get_after(self.user, "login", "failed-auth")
>     if failures.count() >= settings.AUTH_LOCK_ATTEMPTS:
>         self.user.set_unusable_password()
> ```
> 출처: `weblate/accounts/models.py` (weblate-4.11 태그)
> **마지막 로그인 이후 실패 10회면 admin 의 비밀번호가 무효화된다.** 레이트리밋에 막힌 시도는 `authenticate()` 에 도달하지 않아 계산에 안 들어가지만, 실제로 뚫고 들어간 실패는 그때까지 5회를 썼다. 남은 예산이 5회라는 걸 알고 나서야 브루트포스를 접고 "어느 단어 하나를 고를 것인가" 로 문제를 바꿨다. **웹 폼에 cewl 348 단어를 먹이는 건 애초에 불가능한 계획이었다** — 5회/5분이면 6시간이고, 그 전에 계정이 죽는다.
> 여섯 번째 시도로 `Niffenegger` 를 쳤다가 틀렸고(대소문자), 일곱 번째 `niffenegger` 가 맞았다. 잠금까지 4회를 남기고 끝났다.

**지우기 전 원문(구 7장 1·2번):**
> 1. **레이트리밋이 보이면 브루트포스는 그 자리에서 접는다.** "Too many attempts" 를 본 순간 남은 길은 ① 힌트로 후보를 3~5개로 줄이거나 ② 다른 진입점을 찾는 것 둘뿐이다. 시험에서 한 계정에 30분을 태우면 그 박스는 포기한 것과 같다.
> 2. **계정 잠금은 되돌릴 수 없는 사고다.** Weblate 처럼 실패 N회에 비번을 무효화하는 앱이 있다. 실재 계정으로 시험하기 전에 **없는 계정으로 정책을 먼저 떠보는** 것이 안전하다.

---

## 7. [신규 — B. 기법 카드, 웹 또는 일반 카테고리]

**제목 후보:** CLI 인자 주입(argument injection) 반사 — git/hg/tar/curl 등 웹 값이 argv 로 흘러가는 자리

(기존 B-1-15 는 GraphQL introspection→인자주입 전용 카드로, 이 항목과 주제는 인접하나 대상이 다름 — 병합보다 별도 카드 권장)

**넣을 본문:** 셸 메타문자 삽입(고전 커맨드 인젝션)과 다름 — `subprocess`(shell=False)로 안전하게 호출해도 **사용자 값이 argv 한 칸을 통째로 차지하면** 대상 프로그램이 그 값을 옵션으로 해석할 수 있음. 값이 `-` 로 시작하기만 하면 됨. git·hg·curl·tar 처럼 옵션이 풍부한 CLI 를 감싸는 웹앱은 전부 후보. [[Fikklish]] 두 사례:
- Weblate ≤4.11(CVE-2022-23915) — 컴포넌트의 VCS 저장소 URL·브랜치명을 검증 없이 git/hg 인자로 전달. `--config=alias.pull=!<cmd>` (hg alias 의 `!` 는 "뒤는 셸 명령"). 응답이 실패(fatal)해도 명령은 이미 실행됨 — 응답으로 성공/실패를 판정하지 말 것.
- ruby-git 1.10.2(CVE-2022-25648) — `Git::Base#fetch(origin, {ref:...})` 가 `git fetch <origin> <ref>` 로 조립, 두 값 다 미검증. `--upload-pack=<cmd>` 를 로컬 전송(local transport)에서 실행 훅으로 그대로 실행. git 은 **옵션 위치를 가리지 않아** 입력이 한 칸 밀려도(`origin`↔`ref` 뒤바뀜) 통함. 타겟 없이 로컬 재현 가능(`git fetch poc --upload-pack='sh -c "touch /tmp/x"'`).
대표 페이로드 목록 — git `fetch --upload-pack=<cmd>`·`push --exec=<cmd>`, hg `--config=alias.X=!<cmd>`, tar `--checkpoint-action=exec=<cmd>`, curl `-o <파일>`.
**반사: `sudo -l` 이 스크립트를 가리키면 스크립트 본문만 보지 말고 `require`/`import` 한 라이브러리의 버전까지 확인**(항목 9 참고).

**지우기 전 원문(구 0장 인자주입 정의):**
> - **인자 주입(argument injection)** 두 번. 둘 다 "명령 문자열에 `;` 를 끼워넣는" 고전적 커맨드 인젝션이 아니라, **사용자 값이 외부 프로그램의 argv 한 칸으로 그대로 들어가서 옵션으로 해석되는** 유형이다. hg 의 `--config=alias.X=!cmd`, git 의 `--upload-pack=cmd`.

**지우기 전 원문(구 2장 "인자 주입이란"):**
> 셸 메타문자를 넣는 커맨드 인젝션과 다르다. 프로그램이 `subprocess`(shell=False)로 안전하게 호출하더라도, **사용자 값이 argv 한 칸을 통째로 차지하면** 대상 프로그램이 그걸 "옵션" 으로 해석할 수 있다. 값이 `-` 로 시작하기만 하면 된다. git·hg·curl·tar 처럼 옵션이 풍부한 CLI 를 감싸는 웹앱은 전부 후보다.

**지우기 전 원문(구 7장 5번):**
> 5. **인자 주입 반사** — 웹 폼의 값이 CLI 로 흘러가는 자리(저장소 URL, 브랜치, 파일명, 호스트명)를 보면 `-` 로 시작하는 값을 한 번 넣어본다. git 은 `fetch --upload-pack=<cmd>`·`push --exec=<cmd>`, hg 는 `--config=alias.X=!<cmd>`, tar 는 `--checkpoint-action=exec=<cmd>`, curl 은 `-o <파일>` 이 대표적이다. git 의 `--upload-pack` 은 **타겟 없이 로컬에서 재현해 확인했다**(4장).

---

## 8. [신규 또는 병합 → B. 자격증명·크래킹 카테고리]

**제목 후보:** hydra/cewl 실무 함정 — `-c` 파일 형식과 `hydra.restore` 잔존

**넣을 본문:** [[Fikklish]] 두 함정.
① `cewl -c` 로 빈도를 함께 뽑으면 파일이 `Niffenegger, 4` 형식(쉼표+숫자 포함)이 됨 — 그대로 `-P` 에 넣으면 **정답을 사전에 갖고도 못 맞춤**(hydra 가 `Niffenegger, 4` 라는 문자열 전체를 시험). 빈도는 `-c` 로 눈으로 보고, 먹일 사전은 `-c` 없이 따로 뽑을 것.
② 첫 hydra 실행에 `-f`(첫 히트 시 중단)를 켰다 끄는 과정에서 `hydra.restore` 파일이 남으면, 다음 실행이 **"10초 안에 중단하라"며 10초를 대기**함. 자동화 안에서는 `-I`(기존 restore 무시)를 붙일 것.
실측: `hydra -L users.txt -P cewl.txt -t 4 ssh://<target>` — 1392 조합 21분 견적, 정답 `niffenegger` 를 포함한 사전이었음에도 형식 문제로 못 맞춤(중단 없이 로그인이 웹 쪽에서 먼저 뚫려 중단).

**지우기 전 원문(구 6-2, 전문):**
> ### 6-2. 레이트리밋을 기다리는 동안 SSH 브루트포스를 돌렸다 (2회, 전부 실패)
> 웹 로그인이 잠긴 동안 놀리기 아까워서 22번을 두드렸다. 계정 목록은 아직 tom 을 모를 때라 짐작으로 넣었다(`users.txt` — `tom admin weblate root`. 결과적으로 tom 은 맞았다).
> [hydra_theme.log 실행 결과 — 124 login tries, 0 valid password found]
> 두 번째는 cewl 사전을 통째로 밀어넣었다.
> [hydra_cewl.log 실행 결과 — 1392 login tries, 완주 기록 없음(로그인이 먼저 뚫려 중단)]
> 건질 것 셋.
> - **`~21시간`이 아니라 `00:21h`** 다. 21분. 그런데도 이건 손절 신호다 — 4계정 × 348단어를 22번에 던지는 동안 웹 쪽 힌트 하나 확인이 더 빨랐다.
> - **`cewl -c` 로 만든 파일은 `-P` 에 바로 못 넣는다.** 파일이 `Niffenegger, 4` 형식이라 hydra 는 쉼표와 숫자까지 붙은 문자열을 시험한다. 실제로 이 실행은 정답 `niffenegger` 를 **사전에 갖고도 못 맞혔다.** 빈도가 필요하면 `-c` 로 뽑아 눈으로 보고, 먹일 파일은 `-c` 없이 따로 만든다.
> - 첫 실행에 `-f` 를 붙였다가 지웠는데, 그 사이 `hydra.restore` 가 남아 다음 실행이 **"10초 안에 중단하라"며 10초를 세운다.** 자동화 안에서는 `-I` 를 붙여 그 대기를 없앤다.

**지우기 전 원문(구 7장 4번, cewl 관련 부분만):**
> 그리고 **`cewl -c` 로 만든 파일을 그대로 `-P` 에 넣지 마라.** `Niffenegger, 4` 형식이라 정답을 갖고도 못 맞힌다. 빈도는 눈으로 보고, 먹일 사전은 `-c` 없이 따로 뽑는다(6-2).

---

## 9. [신규 또는 병합 → A-43. `sudo -l` 이 좁아도 대상 파일 권한을 확인한다]

**넣을 본문:** `sudo -l` 이 스크립트를 가리키면 스크립트 본문만 읽고 끝내지 말 것 — **`require`/`import` 한 라이브러리의 버전**까지 확인. [[Fikklish]]: root 소유 `fetch.rb`·`checkout.rb` 자체는 평범한 8~10줄짜리였으나, `gem list git` 한 줄로 `ruby-git 1.10.2`(CVE-2022-25648 대상)임을 확인해 권한상승 성립. `gem list`·`pip list`·`npm ls` 를 습관으로 둘 것.

**지우기 전 원문(구 4장 "왜 그것이 권한상승이 되는가" 말미):**
> `sudo -l` 에서 스크립트가 나오면 스크립트 본문만 읽고 끝내지 마라. **`require`/`import` 한 라이브러리의 버전**까지 봐야 한다. 여기서는 `gem list git` 한 줄이 전부였다.

**지우기 전 원문(구 7장 6번):**
> 6. **`sudo -l` 이 스크립트를 가리키면 의존 라이브러리 버전까지 본다.** 스크립트 본문이 깨끗해도 gem/pip 패키지가 뚫려 있으면 끝이다. `gem list` · `pip list` · `npm ls` 를 습관으로.

---

## 10. [신규 — B. 기법 카드, 웹]

**제목 후보:** 정적 템플릿 사이트의 손댄 문단은 자격증명 힌트다

**넣을 본문:** 공개 웹 템플릿(FreeHTML5 등)을 그대로 쓴 사이트는 원본과 본문을 대조하면 손댄 곳이 바로 드러남 — 원본 템플릿을 내려받아 diff 하거나, README/푸터의 라이선스 문구가 그대로면 나머지도 원본일 가능성이 높음. [[Fikklish]]: "Book Bargains Online" 사이트에 원본에 없는 "Editors Note"(소설 속 인물명 노출) 문단이 그대로 admin 비밀번호 힌트였음. `[가정]`: `Last-Modified` 헤더로 손댄 파일만 최신인지 비교하는 방법도 유효할 수 있으나 이 박스에서 실제로 그 비교를 한 기록은 없음(관측 없음) — 실제 판단 근거는 README 원본과의 본문 대조뿐.

**지우기 전 원문(구 7장 3번):**
> 3. **웹 본문의 손댄 문단은 전부 자격증명 힌트로 취급한다.** 템플릿 사이트라면 원본 템플릿을 내려받아 본문을 대조하는 것이 가장 확실하다. `Last-Modified` 비교도 같이 쓸 만하지만, 이 박스에서는 실제로 하지 않았다(1장).

**지우기 전 원문(구 1장, [가정] 문단):**
> **[가정]** 처음에는 "정적 파일 mtime 은 전부 2015~2016 년인데 `index.html` 만 2022-12-14 로 혼자 최신"이라고 적었지만, **그 `Last-Modified` 비교를 남긴 산출물이 없다.** 판단 근거로 실제 남은 것은 README 원본과의 본문 대조뿐이다. 습관 자체는 유효하다(7장 3번) — 다만 이 박스에서 mtime 을 실제로 확인했다는 기록은 **관측 없음**이다.

---

## 11. [신규 소 — B. 기법 카드, 웹]

**제목 후보:** SVG 아이콘 캡차는 `path` 데이터로 연산자를 판정한다

**넣을 본문:** [[Fikklish]] — 가입 폼 캡차가 `What is 15 <svg>…</svg> 8?` 형태로 연산자가 SVG 아이콘. 정규식으로 숫자를 긁으면 SVG path 안의 좌표 숫자까지 딸려와 오탐. `d` 속성 앞부분으로 판정해야 함 — `M19,13H5V11H19V13Z` = 빼기, `M19,6.41L17.59,5L12,10.59...`(X 아이콘) = 곱하기, 그 외 = 더하기. 결과적으로 이 박스에서는 헛다리(항목 5)였으나 파싱 조각 자체는 재사용 가능(`/tmp/reg2.py`, Kali 쪽 산출물에는 미보존).

**지우기 전 원문(구 6-4, 전문):**
> ### 6-4. captcha 파싱 (약 8분)
> 가입 폼의 captcha 는 `What is 15 <svg>…</svg> 8?` 형태로 **연산자가 SVG 아이콘**이다. 처음에 정규식으로 숫자를 다 긁었더니 SVG path 안의 좌표 숫자까지 딸려와 계속 틀렸다. path 의 `d` 속성으로 판정해야 한다.
> | path 시작 | 연산 |
> | `M19,13H5V11H19V13Z` | 빼기 |
> | `M19,6.41L17.59,5L12,10.59...` (X 아이콘) | 곱하기 |
> | 그 외 | 더하기 |
> 곱하기가 있다는 걸 몰라서 한 번 더 틀렸다. 결과적으로 이 작업 전체가 6-3 의 헛다리였지만, 파싱 자체는 재사용 가능한 조각이다(`/tmp/reg2.py`).

---

## 12. [신규 소 — A. 증상별, 정찰류]

**제목 후보:** tmux 안의 조용한 실패 — gobuster 워드리스트 경로부터 확인

**넣을 본문:** [[Fikklish]] — 첫 `gobuster` 실행이 `/usr/share/seclists/...` 경로 부재로 0초에 죽었는데, tmux 세션 안에서 돌리면 실패가 화면에 남아도 넘겨보고 지나치기 쉬움. `capture-pane` 으로 확인하기 전까지 실패를 몰랐음 — 장시간 명령을 tmux 에 던진 뒤에는 최소 한 번 결과 파일 크기·줄 수를 확인할 것.

**지우기 전 원문(구 6-6 첫 항목):**
> 첫 `gobuster` 는 `/usr/share/seclists/...` 경로가 없어 0초에 죽었다. tmux 안에서 돌리면 실패가 조용하다 — `capture-pane` 으로 확인하기 전까지 몰랐다.

---

## 13. [신규 소 — 기존 A-1 정찰류 근접(24행 부근 "`searchsploit weblate` 는 0건" 유사 사례 있으면 병합 검토)]

**제목 후보:** `searchsploit` 이 비어도 익스플로잇이 없는 것은 아니다

**넣을 본문:** [[Fikklish]] — `searchsploit weblate`·`searchsploit ruby-git` 둘 다 0건(Kali 에서 재확인). CVE-2022-23915·CVE-2022-25648 모두 웹 검색으로만 발견됨. exploit-db 에 안 올라온 CVE 가 훨씬 많음 — searchsploit 결과가 비었다고 공개 PoC 부재로 단정하지 말 것.

**지우기 전 원문(구 6-6 마지막 항목):**
> 공개 익스플로잇을 찾을 때 `searchsploit weblate` 는 **0건**이었다(`ruby-git` 도 0건. Kali 에서 다시 확인했다). CVE 번호는 웹 검색으로만 나왔다. **searchsploit 가 비었다고 익스플로잇이 없는 게 아니다** — exploit-db 에 올라오지 않은 CVE 가 훨씬 많다.

---

## 14. [신규 — D. 시간 배분 · 손절 기준 표에 행 추가]

**넣을 본문:** [[Fikklish]] — nmap 18:26 → admin 로그인 19:19 = **53분**(레이트리밋 대기·헛다리 다수 포함, 항목들이 서로 겹쳐 병렬 소요). 이후 RCE→tom→root 는 **9분**(19:28). 최대 손실 구간은 메일 가로채기 헛다리 **27분**(항목 5) — 아웃바운드 패킷 0 확인까지 5분이면 충분했던 자리. 브루트포스 락아웃 회피 시행착오(항목 6)에 약 45분(다른 작업과 시간 겹침).

**지우기 전 원문(구 6장 도입부 + 7장 12번):**
> nmap 을 건 18:26 부터 `admin` 으로 로그인한 19:19 까지 **53분**이 걸렸다. 그 뒤 RCE → tom → root 는 **9분**이다(19:28). 태운 시간의 정체가 이 장이다. 아래 각 항목의 소요는 서로 겹친다 — 레이트리밋을 기다리는 동안 다른 걸 돌렸기 때문이라 합계는 53분을 넘는다.
> 12. **시간 배분** — 이 박스의 손절 지점은 명확하다. **메일 가로채기에서 아웃바운드 패킷이 0이면 5분 안에 접는다.** 나는 27분을 썼다. 반대로 로그인만 되면 이후는 9분짜리다.

---

## 검산

구 노트 0/6/7 장 전체 + 8장 없음(4항목 Vulnerability Fix 로 이미 분산 완료) + 9장 없음(관련 섹션으로 이미 이관 완료) 중, 위 14건이 실질 손실 없이 커버함. 유일하게 "완전 이관"이 아니라 "노트에도 요약 남기고 playbook 에도 일반화 반사로 추가"한 것은 항목 2(lxd 그룹)와 항목 3(ICMP) — 실측 상세는 새 노트에 남기고, "다른 박스에도 적용되는 반사"만 playbook 행 대상으로 제안함. 번호는 전부 비워 「신규」로만 표기 — line-manager 배정.
