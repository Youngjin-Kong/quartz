# Detection.md — 문서 검토(문체·공개 적합성·한국어) 2026-08-21

- 대상: `03. PG\Detection.md` (220행 → 220행, 17,977 B → 17,843 B)
- 백업: `03. PG\_backup\Detection.md.bak`
- 범위: 표현만. 사실 검증은 `Detection-audit.md` 소관이며 값·명령·타임스탬프는 재조사하지 않음.
- 코드펜스 대조: 펜스 내부 전량 추출 후 `diff` → **완전 일치(0 바이트 변경)**.

## A. 고친 것

| 위치(문자열) | 분류 | 전 → 후 |
|---|---|---|
| 2장 배경 「…보낸다 / …치환한다」 | 문체 | 「알림(apprise)을 보낸다 … 토큰을 치환한다」 → 「알림(apprise) 발송 … 토큰을 치환」 |
| 2장 인용 규칙 「산출물 실제 형태도 그렇다 / …깨진다」 | 문체 | 「그러함 / …깨짐」 |
| 2장 「### 트리거 지점 — 여기가 브리핑과 달랐다」 | 문체·공개적합성 | 「### 트리거 지점 — 사전 정보와 달랐던 곳」 |
| 2장 「공개 PoC·브리핑은 … **실측은 다르다:**」 | 문체·공개적합성 | 「공개 PoC·사전 정보는 … **실측은 다름:**」 |
| 2장 「…저장 시점의 렌더다.」 | 문체 | 「…저장 시점의 렌더임.」 |
| 3장 「popen 안에서 직접 띄우면 앱이 죽는다(6장)」 | 문체 | 「…앱이 죽음(6장)」 |
| 6장 ① 제목 「진입점을 내 페이로드로 죽였다 (핵심 교훈)」 | 문체 | 「진입점을 내 페이로드로 죽임」 — 「핵심」은 과장 형용사 목록 항목 |
| 6장 ① 「popen 을 절대 붙잡지 마라 / …받는다」 | 문체 | 「popen 을 붙잡지 말 것 / …받을 것」 |
| 6장 ② 「…SSTI 가 안 탄다 / …보여준다」 | 문체 | 「…안 탐 / …보여줌」 |
| 6장 ② 「ⓑ 쪽이 결정적」 | 문체 | 「ⓑ 쪽이 더 강함」 — 「결정적」은 과장 형용사 목록 항목. 「ⓑ가 ⓐ보다 강한 근거」라는 뜻은 유지 |
| 6장 ③ 「우리 443 리스너를 점유」 | 공개적합성 | 「Kali 443 리스너를 점유」 |
| 6장 ③ 「이 진단은 1차 생성물이 …실패했다 / …나왔음」 | 문체 | 「1차 생성물이 …이 진단 자체가 한 번 실패함 / …나옴」 |
| 6장 ③ 「성공한 `GET /diag443-` 만 로그에 있다」 | 문체 | 「…로그에 있음」 |
| 7장 2 「다른 gadget 이 필요」 | 한국어 | 「다른 gadget 필요」 |
| 7장 3 「이게 사실상 유일한 피드백이다」 | 문체·한국어 | 「이것이 사실상 유일한 피드백 채널」 |
| 7장 4 「리버트를 빨리 요청하는 것이 …낫다」 | 문체 | 「…요청하는 편이 …나음」 |
| 7장 5 「`ss` 로 먼저 확인. 계층을 분리하라.」 | 문체 | 「…먼저 확인할 것. 계층 분리가 먼저.」 |
| 남긴 흔적 「세션 시작 이전부터 존재 … 손대지 않고 **보고만** 함」 | 공개적합성 | 「작업 시작 이전부터 존재 … 손대지 않음」 — 「보고」는 내부 절차 언급 |
| 굵게 12곳 | 문체 | 한 문단 2~4개 굵게가 겹친 줄에서 정보 밀도가 가장 높은 하나만 남김(아래 B) |

## B. 굵게 정리 — 남긴 쪽 / 푼 쪽

`**` 토큰 152개 → 114개(굵게 76곳 → 57곳). 값·경로·수치는 한 글자도 건드리지 않았고 마크업만 해제함.

| 줄 | 푼 것 | 남긴 것(그 줄에서 가장 load-bearing) |
|---|---|---|
| 요약 | `**CVE-2024-32651**` | `**changedetection.io v0.45.1**` |
| 1장 nmap 해설 | `**추정**` | `**자동 판정을 서비스 근거로 쓰지 말 것**` |
| 1장 서비스 식별 | `**changedetection.io 0.45.1**`(요약에 이미 굵게) | `**인증 게이트 없음**` |
| 1장 backup zip | `**앱 비밀번호 미설정**` | `**추가 플래그는 없음**` |
| 2장 배경 | `**알림(apprise)**`·`**Jinja2 템플릿**`·`**0.45.20 이하…패치**` | `**샌드박스 없이**`(SSTI 성립의 핵) |
| 2장 트리거 | `**Send test notification**`·`**`POST /settings` 저장 그 자체**` | `**전에**`(선후가 곧 증거) |
| 6장 ① | `**같은 페이로드를 재발사**`·`**블로킹**` | `**5000/tcp 전체 무응답**` |
| 6장 ① 복구 | `**TCP 소켓은 죽였음**` | `**타겟 프로세스 상태는 … 관측할 수 없었음**`(유보) |
| 6장 ① 교훈 | `**SSH 키 심기(전부 비블로킹)**` | `**교훈**` |
| 6장 ② | `**저장값이 아니라 기본 제목·본문**`·`**둘 다 기본 문구**` | `**설정 저장**` |
| 6장 ③ | `**빈 문자열**`·`**같은 종류의 따옴표**` | `**443 아웃바운드 정상**`·`**「stderr…」 자체가 SSTI 디버깅 수단**` |
| 7장 3 | `**반드시 `&`+`setsid` 로 분리**` | `**쓰지 않았고 검증되지 않음**`(유보) |

## C. 삭제한 것

**없음(0건).** 문장·절·수치·출처 캡션·스크린샷 임베드 어느 것도 제거하지 않음. 줄 수 220행 불변.

## D. 넘긴 것 — `writeup-auditor` 소관

**없음.** 표현을 다듬는 과정에서 사실 의심 지점이 나오지 않음. (검토 범위가 표현이므로 사실 대조는 수행하지 않았고, 그것이 없다는 뜻은 아님.)

## E. 손대지 않기로 한 것

| 대상 | 왜 남겼나 |
|---|---|
| `root@detection:~#` · `root@detection:/#` (3곳) | 타겟 pty 실측. 「웹셸이 아닌 대화형 셸에서 플래그를 읽었다」의 유일한 판정 근거 |
| `[가정]` 8건 | 인식론적 표시. 전후 개수 동일(8 → 8) |
| 「미확정」·「관측할 수 없었음」·「검증되지 않음」·「미보존」·「사전 존재 불명」 5건 | 유보 표시. 무변경 |
| `~/PG/Detection/…` 출처 캡션 8건 | 실측 근거. 무변경 |
| 192.168.248.97·192.168.45.207·192.168.248.220·플래그 값·pid·바이트 수 | 랩 환경 값이라 무해하고, 지우면 되살릴 수 없음 |
| 「사전 정보」로 바꾼 「브리핑」 | 「공개 PoC 가 설명한 것 + 사전에 가진 정보가 실측과 달랐다」는 사실은 그대로 유지. 없는 출처를 새로 지어내지 않기 위해 「해설글」류로 바꾸지 않음 |
| 「~하라/~마라」 명령형 잔여 없음 확인 | 개조식 종결(`~할 것`)로 통일 완료 |
| 6장 ①~④ 인과 접속(「…라 …되어」·「…없어 …이었음」·「…쓴 것이 원인」) | 「왜 틀렸는가」의 인과. 압축 대상에서 제외 |
| 8·9장 | 이미 명사 종결형. 무변경 |

## F. 검산

- 코드펜스 내부: `diff` 일치 → **0 바이트 변경**
- 행수: 220 → 220 (증가 0 — 새로 쓴 문장 없음)
- 바이트: 17,977 → 17,843 (-134, 감소 방향 = 개조식 전환의 기대 방향)
- `[가정]` 8 → 8, 출처 캡션 8 → 8, 타겟 프롬프트 3 → 3
- 금칙 문자열 스캔(`C:\Users`·에이전트명·총괄·관리자·웨이브·감사자·정정 이력·TODO·초안): **0건**
- 펜스 밖 서술형 종결 스캔: 잔여 4건 전부 「」 안의 인용문(「저장만으로 실행됐다」·「요청을 사실상 순차 처리한다」·「블로킹 요청 하나 뒤로 accept 가 멈췄다」·「test 는 저장 본문을 아예 안 쓴다」)이라 대상 아님
