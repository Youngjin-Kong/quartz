# Jacko 노트 적대적 검증 — 감사 기록

**감사일** 2026-08-20 · **대상** `03. PG\Jacko.md` (개작본 812행 → 감사 후 868행) · **대조본** `03. PG\Jacko.md.bak`(314행)
**타겟에 패킷을 보내지 않았다.** 순수 문서·산출물 대조 + Kali/Windows 로컬 재현만 했다.

## 0. 최종 판정 — **부분 완료 1/2**

- `local.txt` = `dc9bb9f7d40681ebf2db1589d6ca40f0` — 라인 관리자가 이미 한 글자씩 판독한 건. 재확인 불필요 지시대로 건드리지 않았다.
- `proof.txt` **미획득**. 아래를 전부 훑고 내린 판정이다:
  - `~/PG/Jacko/` 전체 — 파일 **5개**(`49384.txt`·`nmap.log`·`reverse.exe`·`rev.exe`·`whatweb.txt`), 하위 디렉터리 없음
  - 홈 전체 mtime 스윕 `find ~ -xdev -newermt '2026-07-10 00:00' ! -newermt '2026-07-12 00:00'` → **18건 전부 열거함**. Jacko 관련 마지막 쓰기는 `reverse.exe` **17:01:44**. 그 이후 07-11 까지 아무것도 없다
  - 볼트 `파일보관\` 07-10·07-11·07-12 전량 → Jacko 계열 마지막 장이 **17:07:41**(local.txt), 07-11·07-12 스크린샷 **0장**
  - `~/.zsh_history` 이 박스 구간(1792~1810행) 전문 확인. **`~/.bash_history` 는 존재하지 않는다**(로그인 셸이 zsh 뿐) — 히스토리 사료는 이 파일 하나가 전부다
  - Kali 측 `~/.claude/projects/` — 디렉터리 3개 전부 mtime 이 2026-05~06 으로 07-10 이전. `192.168.120.66` 문자열은 `~/PG/Jacko/{nmap.log,whatweb.txt}` 외 어디에도 없다
  - 패키지 캐시·`/tmp`·`~/git` 도 스윕에 포함(홈 전체 `-xdev` 스윕이 `~/.cache` 를 덮는다)

→ **`_STATUS.md` 에 `1/2 부분` 으로 기록.** (지시대로 내가 직접 수정하지 않았다.)

---

## 1. 고친 것

| 위치(문자열) | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| §1 `바이트 사이에 \0 이 끼어 있는 것은 **UTF-16LE**` | **UTF-16BE 다.** 널바이트가 글자 뒤가 아니라 앞에 온다 | "글자마다 앞에 `\0` 이 붙는 것은 UTF-16BE" 로 정정 + 반대로 잡으면 `刀攀洀漀琀攀` 가 나온다는 확인법 한 줄 추가 |
| §1 feroxbuster 코드펜스 | 원본 노트(bak 219~228행)의 **진행 표시 7행을 말없이 삭제**했다. 코드펜스 안 실측의 무단 축약 | bak 원문 그대로 복구(9행) |
| §2 `두 번째 줄이 ... 한 줄 6만4천자 ... 22행짜리 텍스트` | 셋 다 틀렸다. 실측은 **15행 · 124,111자 · 23행** | 정정하고, `searchsploit` 이 찍은 `(64895)` 는 `file` 의 보고값이지 줄 길이가 아니라는 함정을 `[!warning]` 으로 추가 |
| §2 `다섯 개의 '' — 구분자·인용부호·이스케이프 문자·NULL 표기` | 인자 4개를 세고 5개라 했다 | "…같은 서식 인자" 로 완화 (시그니처 원문 미확인 고지는 그대로 유지) |
| §3 `같은 초에 GET 이 두 번 ... 이유는 확정되지 않는다` | **근거부족이 아니라 확정 가능한 사실이었다** | 실측으로 승격 — certutil 은 다운로드 1건당 GET 2회를 낸다 |
| §3 certutil 표 `-split` 행 | "빼면 캐시에만 들어가고 지정 경로에 파일이 안 생긴다" → **반증됨** | 실제 역할(ASN.1 분리)로 교체 + `[!warning]` 으로 통설 반박 |
| §3 certutil 표 `-f` 행 | "빼면 캐시된 옛 파일이 나온다" → 실제는 **아예 받지 않는다** | `**** OFFLINE ****`, 사실상 필수 플래그로 정정 |
| §3 `03a200 은 ... 곧 받은 파일의 크기다` | 근거 출처 미표기(내 Win11 certutil 로는 재현 불가) | "certutil 문서가 아니라 §6 의 크기 일치가 근거" 라고 명시 |
| §3 certutil SQL 코드펜스 | 스크린샷의 `CREATE ALIAS ...` 첫 줄이 빠져 있었다 | 복구 + `(1 row, 1188 ms)` 언급 추가 |
| §4 `local.txt 를 읽을 수 있었으니 최소한 tony 이상` | 더 강한 증거를 놓쳤다 — certutil 로 tony Desktop 에 **썼다** | 읽기+쓰기 두 사실로 재서술, `tony 본인이거나 관리자급` `[가정]` |
| §5 플래그 화면 전사 | 스크린샷의 **명령 에코 행 3개와 빈 줄을 지워 정돈**했다 | 스크린샷 원문대로 복구 + 에코가 나오는 이유 한 줄 |
| §6 `히스토리 전체를 훑어도 msfconsole ... 한 줄도 없고` | **틀렸다.** 211행 `sudo msfconsole` | "이 박스 구간에 없다" + 211행은 `ms17_010`/`192.168.141.10` 이라 별건임을 `~/.msf4/history` 로 확인해 명시 |
| §6 `msfvenom 세 줄은 16:46~17:01 구간에 걸쳐 다시 친 것` | 같은 절의 타임라인표(meterpreter 판 = 16:10 이전)와 **자기모순** | mtime 으로 고정되는 것(16:10 이전 / 17:01:44)만 남기고 가운데 판은 `[가정]` 으로 강등 |
| §6 `zsh 는 세션이 끝날 때 자기 블록을 덧붙인다` | 근거 미제시 | `~/.zshrc` 에서 `share_history` 주석 처리·`EXTENDED_HISTORY` 부재 확인해 근거로 붙이고, 이 박스 구간 자체가 그 증거임을 추가 |
| §6 `16:10 과 16:29 에 타겟이 받아간 reverse.exe 는 meterpreter` | 16:10 분은 직접 증거가 없다 | 16:29 는 단정, 16:10 은 "그 사이 쓰기가 없다" 근거로 `[가정]` |
| §7-7 `권한상승에 3분` | 권한상승 시도 자체가 없었다 | "셸을 잡은 뒤로는 3분 … 권한상승에 쓴 시간은 사실상 0" |
| 관련 노트 `[[Flu]] — 부분 완료(1/2)` | Flu 는 **2026-08-20 재도전으로 2/2** (`Flu.md` §5 에 `/root/proof.txt` 실측 있음) | 2/2 로 정정하고 "Jacko 가 지금 그 상태" 로 재배치 |
| 노트 상단 | 정정 이력 없음 | `[!warning] 적대적 검증 정정 이력` 표 추가 |

## 2. 삭제한 것

**없다.** 반증된 서술도 전부 정정·강등으로 처리했다. 코드펜스 안의 값은 **한 바이트도 바꾸지 않았고**, 오히려 두 곳에서 **삭제돼 있던 실측 행을 복구**했다(feroxbuster 진행 표시 7행, `local.txt` 화면 에코 3행 + 빈 줄).

## 3. 반증한 것 — 지적으로 올랐다가 확인 결과 노트가 옳았던 것

이 목록이 검증 절차 자체의 정확도다.

| 내 초기 의심 | 확인 결과 |
|---|---|
| **`xxd` diff 블록이 복구됐다는 작성자 자기보고가 사실인가** | **사실이다.** `~/PG/Jacko` 에서 `xxd rev.exe`/`xxd reverse.exe` 를 다시 떠 diff 하니 노트의 3-hunk 가 **바이트 단위로 동일**. `f4da`↔`fbd0`, `.mzyj`↔`.bgoe`, `0087`↔`1f92` 전부 일치 |
| **"msfvenom 을 Kali 에서 재실행했다"가 날조 아닌가**(총괄 지시 2번 초점) | 값은 **내가 재실행해 일치 확인**: `Payload size: 230982` / `Final size of exe file: 238080`. 실행 흔적도 남아 있다 — `/tmp/r1.hex`·`/tmp/r2.hex` 가 노트 개작 시각(2026-08-20 13:3x)에 생성돼 있다. `mtr.exe`·`a.exe`·`b.exe` 는 `/tmp` 에 없지만 **부재는 미실행의 증거가 아니고**, 값이 전부 재현됐으므로 날조로 판정하지 않는다 |
| **`0x03a200` = 238,080 산수와 "마지막 오프셋 = 파일 크기" 해석** | 산수 맞다. 해석은 내 Windows 11 certutil 로는 **진행 표시가 아예 안 찍혀 직접 검증 불가**. 다만 그 값이 실제 msfvenom 산출물 크기와 **정확히 일치**하고 대안값(7,680 = `0x1e00`)과는 전혀 안 맞으므로 채택. 노트에 근거 출처를 명시하도록 고쳤다 |
| **총괄 지시 "`rev.exe`/`reverse.exe` 둘 다 7680B = 실패 후 재시도"** | **작성자의 반박이 옳다.** 두 파일은 서로 실패/재시도 관계가 아니다. 16:29 에 타겟이 받아간 것은 238,080B 였고 이는 남아 있는 어느 파일도 아니다 |
| **총괄 지시 "히스토리 1792~1810행"이 맞는 범위인가** | 맞다. 1792 = meterpreter msfvenom, 1810 = `exit` |
| **`searchsploit` 의 `cp -i` 인용(958·959행)** | 원문 일치. `-B1` 이므로 2행이 맞다 |
| **`ls -la /usr/share/exploitdb/.../49384.txt` 블록** | 크기 125140 · mtime `2025-12-17 09:16:42.000000000 +0900` 전부 일치 |
| **`head -16 49382.ps1` 블록** | 16행 전부 원문 일치. `$PayloadFile = "C:\Windows\Temp\UninOldIS.dll"` 도 22행에 실재 |
| **`sysctl net.ipv4.ip_unprivileged_port_start = 0`** | 일치 |
| **"`-o` 를 줬을 때만 `Saved as:` 가 붙는다"** | 직접 재실행해 확인. 배너는 양쪽 다 stderr, `>` 쪽에만 `Saved as:` 가 없다 |
| **"같은 인자로 두 번 빌드하면 md5 가 다르다"** | 재실행해 확인(둘 다 7680B, 해시 상이). 노트에 적힌 특정 해시 두 개는 **원리상 재현 불가**하고 노트도 그 점을 말하고 있다 |
| **`~/PG/Jacko` 산출물 5개** | 정확히 5개 |
| **`~/.msf4/loot` 가 비어 있다** | 비어 있다 |
| **`http.server` 스크린샷 전사** | 프롬프트·`python -m http.server 80`·`Serving HTTP on 0.0.0.0 port 80 (http://0.0.0.0:80/) ...`·GET 2행 시각 `16:10:29` 전부 화면과 일치 |
| **certutil 스크린샷 전사(`**** Online ****`/`000000  ...`/`03a200`/성공 문구)** | 화면과 일치 |
| **`System_load` 응답 `null`, H2 `1.4.199 (2019-03-13)`, `jdbc:h2:~/test`, `sa`+빈 비번, `Generic H2 (Embedded)`, `org.h2.Driver`** | 전부 화면과 일치 |
| **실행 화면(17:04:36) SQL 전사** | 화면과 일치(줄바꿈 위치만 다름) |
| **`03a200` 화면이 §2 3단계 체인의 두 번째 시도라는 §3 의 주장** | 스크린샷 URL 의 `jsessionid` 가 이를 뒷받침한다 — 16:12/16:13/16:28 은 `b5511331…`, 16:29 는 `ba99e591…` 로 세션이 갈린다. 16:10:29 다운로드 시점에 이미 별개 세션이 있었다는 해석과 모순되지 않는다 |
| **작성자가 보고한 "자기 초고 오류 6건"** | 그중 5건은 실물로 확인됨(xxd 복구·49382 헤더 복구·`grep -B1` 2행·`-o` 차이·feroxbuster 시각). **1건은 과잉 자기반증이었다** — "같은 초 GET 2회는 certutil 정상 동작" 을 "근거 없음" 으로 강등했는데, 실측하니 **초고가 옳았다** |

### 총괄 지시 중 틀렸던 것

- **"`rev.exe`/`reverse.exe` 둘 다 7680B = 실패 후 재시도"** — 틀렸다(위 표). 작성자의 반증을 그대로 채택했다.
- 프롬프트가 든 "작성자 자기 반증 6건"의 신뢰성 — 5/6 은 진짜 반증, 1/6 은 **반증이 아니라 옳은 것을 지운 과잉 교정**이었다. 이번 감사에서 되살렸다.

## 4. 대조 불가 터미널 블록

"실측의 표식"이 붙은 블록 = **Kali 프롬프트 블록 15개 + 스크린샷 전사 블록 2개 = 17개.** (나머지 13개 펜스는 익스플로잇 SQL 인용·명령 템플릿·히스토리 발췌라 실측 주장이 아니다.)

| 등급 | 개수 | 내역 |
|---|---|---|
| 보존된 원본과 **일치 확인** | 10 | `cat nmap.log` · `whatweb`(파일 대조) · feroxbuster(bak) · `searchsploit`(bak) · msfvenom 8082 배너(bak) · `http.server`(스크린샷) · `nc -lnvp 8082` 세션(bak) · certutil 반환 문자열(스크린샷) · `local.txt` 화면(스크린샷) · `xxd` diff(**재실행해 바이트 동일**) |
| **재실행으로 값 일치**(원 실행 흔적은 소멸) | 6 | `sysctl` · `python3 -c "print(0x03a200)"` · meterpreter msfvenom(238080) · `grep -B1 searchsploit` · `ls -la 49384.txt` · `head -16 49382.ps1` |
| **대조 불가** | **1** | §6 의 `md5sum a.exe b.exe` 두 해시. msfvenom 이 매 빌드마다 다른 해시를 내므로 **원리상 재현 불가**. 다만 "매번 달라진다"는 주장 자체는 재실행으로 확인했고 노트도 본문에서 그 점을 말한다 → **날조 아님, 검증 불가** |

**날조로 판정한 블록은 0개다.**

## 5. 근거 출처

**Kali 산출물** — `~/PG/Jacko/{nmap.log,whatweb.txt,49384.txt,rev.exe,reverse.exe}` · `~/.zsh_history`(1792~1810행, 전체 2552행) · `~/.zshrc` 히스토리 설정 49~57행 · `~/.msf4/{history,loot,logs/framework.log}` · `/tmp` mtime 목록 · `/usr/bin/searchsploit:958` · `/usr/share/exploitdb/exploits/java/local/49384.txt` · `/usr/share/exploitdb/exploits/windows/local/49382.ps1`

**스크린샷(9장 전량 직접 열람)** — `파일보관\Pasted image 2026071016{1044,1232,1258,1324,2803,2836,2931},20260710170436,20260710170741.png`

**볼트** — `03. PG\Jacko.md.bak` · `03. PG\02. Pentest Essentials.md` §Jacko(493~513행) · `03. PG\Flu.md` · `03. PG\_AUDIT\Jacko-forge.md`

**직접 실행한 명령**
- Kali: `xxd`+`diff` 재현 · `msfvenom -p windows/x64/meterpreter_reverse_tcp …`(238080 확인) · `msfvenom … -o` vs `>` 비교 · `md5sum` 2회 빌드 · `awk '{print length}' 49384.txt` · `file 49384.txt` · `python3` UTF-16 BE/LE 디코드 · `find ~ -newermt` 스윕
- Windows(certutil 실측): Kali 에 `python3 -m http.server 8931` 를 띄우고 1234B·7680B·238080B·5MB 파일로 `certutil -urlcache [-split] [-f]` 조합 8회 실행 → **GET 2회/다운로드 1건** · **`-split` 불필요** · **`-f` 없으면 `**** OFFLINE ****`** 확인. `certutil -urlcache -?` 도움말로 `-split`/`-f` 정의 대조
- 테스트 산출물은 전부 정리했다(Kali `/tmp/audsrv`·tmux 세션 `audsrv`·`/tmp/aud_*` 삭제 확인).

## 6. 총괄 판단이 필요한 것

- **`_STATUS.md` Jacko 를 `1/2 부분` 으로.** 지시대로 내가 손대지 않았다.
- `tech/db/h2` 태그 공백은 보류 지시대로 건드리지 않았다.
- `refresh.ps1` 은 지시대로 돌리지 않았다.
