# Levram · Wheels 문서 검토 (축 ②·③)

범위: 축 ② 공개 적합성(작업 과정 기록·감사 연대기 제거) + 축 ③ 한국어.
축 ① 문체 개조식 전환은 **수행하지 않음**(`_WRITEUP-STANDARD.md:158` 금지, 총괄 판단 미도달).

백업: `03. PG\_backup\Levram.md.pre-docreview-20260821.bak` · `Wheels.md.pre-docreview-20260821.bak`

## 보존 검산

| | 바이트 | 행 | 코드펜스(`^```` `) | `[가정]` | kali 프롬프트 | 타겟 pty 프롬프트 |
|---|---|---|---|---|---|---|
| Levram 전 | 116398 | 1383 | 78 | 20 | 18 | 17 |
| Levram 후 | 113504 | 1382 | 78 | 20 | 18 | 17 |
| Wheels 전 | 12647 | 212 | 20 | 1 | 0 | 8 |
| Wheels 후 | 11938 | 207 | 20 | 1 | 0 | 8 |

플래그·해시·IP·자격증명 전량 무변경(grep 대조 통과). 코드펜스 내부 무변경.

---

## Wheels.md

### 삭제 — 원문 전문

`Wheels.md:29-32` (상단 요약 직후 콜아웃 전체). CLAUDE.md §4 가 ⛔ 로 금지하는 「적대적 검증 정정 이력」 그 자체.

```
> [!warning] 적대적 검증 정정 이력 (2026-08-21)
> 산출물(`~/PG/Wheels/`)·바이너리 재분석으로 대조했다. 터미널 블록 10개 전부 실측과 일치(날조 0). 정정한 것:
> - **`/tmp` 를 `nosuid` 라고 적었던 서술 → 정정.** 실측 마운트는 `/dev/sda2 on / ext4 rw,relatime`(별도 `/tmp` 없음)라 `/tmp` 는 nosuid 가 아니다. nosuid 는 `/dev/shm` 뿐. in-place `chmod +s /bin/bash` 선택 근거를 사실에 맞게 고침(4장·「남긴 흔적」).
> - **`bob may not run sudo` 인용 → 정정.** 실측된 것은 `sudo -n` 의 "a password is required"(NOPASSWD 없음)뿐. `sudo -l` 성공 출력은 산출물에 없어 그 문장을 인용으로 남기지 않음.
```

**내용 손실 없음을 확인한 근거** — 두 정정 결과가 이미 본문에 반영돼 있다:
- `/tmp` nosuid 건 → 4장 말미 「`/dev/shm` 은 tmpfs `nosuid`(마운트 `rw,nosuid,nodev`)라 … `/tmp` 는 별도 마운트가 아니라 루트 `ext4`(`/dev/sda2 on / rw,relatime`) 위라 `nosuid` 가 아니어서 …」
- `sudo -l` 건 → 3장 말미 「`sudo -n` 은 비밀번호를 요구한다(NOPASSWD 엔트리 없음)」

### 고친 것

| 위치 | 분류 | 전 → 후 |
|---|---|---|
| 6장 첫 줄 | 축② 에이전트 언급 | `첫 러너(나)는 **포털에 못 들어가**` → `첫 시도에서는 **포털에 못 들어가**` |
| 남긴 흔적 | 축② 에이전트 언급 | `두 러너가 등록한 계정(초기 열거의 admin·dupx·firstname 계열 + 웹전문의 @wheels.service employee 계정)` → `등록한 더미 계정(초기 열거의 admin·dupx·firstname 계열 + 웹 진입점 규명 때 만든 @wheels.service employee 계정)` |
| 2장 | 축③ 어미 | `계정을 얼마든 찍어낼 수 있다` → `얼마든지` |
| 3장 | 축③ 띄어쓰기 | ``(`groups=1000(bob)`) 뿐이라`` → ``(`groups=1000(bob)`)뿐이라`` (의존명사 「뿐」 붙여쓰기) |

### 손대지 않기로 한 것

- **`03. PG/_AUDIT/Wheels-run.md` · `_AUDIT/Wheels-web-entry.md` 포인터**(남긴 흔적 절). 지시서의 제거 목록(감사 이력 콜아웃·정정 건수·에이전트 이름·로컬 절대경로)에 해당하지 않고, 6장 시행착오의 1차 출처 역할을 한다. 발행 시 이 포인터까지 걷을지는 총괄 판단 사안으로 남긴다.
- 4장 `objdump` 필터 블록의 `strchr(buf, 0x3b);  # ';'` 같은 의사코드 — 코드펜스 안이라 무변경.

---

## Levram.md

핵심 원칙: **일반 원칙으로 진술된 교훈은 남기고, 「누가 언제 무엇을 잘못 판정했는지」의 연대기만 걷어낸다.**
따라서 「부재 증거를 존재 부정으로 승격시키지 마라」·「재현 경로가 없는 관측은 자기 자신을 방어하지 못한다」·「스크립트의 한계를 취약점의 전제조건으로 승격시키지 마라」는 **전부 본문에 남아 있다.** 사라진 것은 감사자·감사 날짜(2026-08-20)·삭제/복원 서사·「이전 판본은 …라고 적었다」 프레이밍뿐이다.

### 고친 것 (전 → 후)

| 위치(원본 행) | 분류 | 전 | 후 |
|---|---|---|---|
| 31 (0장) | 감사 연대기 | `이 노트의 증거 B는 적대적 검증에서 "wheel 을 받은 적이 없다 → 날조"로 판정돼 통째로 삭제됐다가, ~/.cache/pip 에 남아 있던 wheel 로 전항목 복원됐다.` | `이 노트의 증거 B가 그 예다 — ~/PG/ 와 /tmp/gv/ 에 wheel 이 없다고 해서 대조한 적이 없는 것은 아니었다. pip 는 받은 wheel 을 ~/.cache/pip 에도 남긴다.` |
| 106 (1-2) | 감사 연대기 + 날짜 | `증거 B 는 한 번 "날조"로 판정돼 삭제됐다가 철회·복원됐다(2026-08-20). 적대적 검증자가 ~/PG/ 와 /tmp/gv/ 만 뒤져 "wheel 이 없으니 대조한 적도 없다"고 단정한 것인데, pip 는 …` | `증거 B 의 대조본이 ~/PG/ 와 /tmp/gv/ 에는 남아 있지 않다는 점을 먼저 적어 둔다. pip 는 …` (뒤의 「wheel 3종 전수 재대조 → 항목별 일치 → 재현 절차는 이 절 끝」 그대로 보존) |
| 259-260 (1-4) | 초고 대조 서술 | 콜아웃 제목 `— 이 노트가 놓친 대안 경로` / 본문 첫 문장 `이전 판본은 그렇게 단정했다.` | 제목 `— 놓치기 쉬운 대안 경로` / 첫 문장 삭제 |
| 311 (2-1) | 감사 연대기 | `이 노트는 그 혐의를 한 번 받았고, 캐시에 남은 wheel 로 전항목 재현돼 벗었다. 규율 자체는 유효하다 — 확보한 축이 … 이 노트를 구한 것이 그 재현 가능성이었다.` | `확보한 축이 하나면 하나라고 적고, 적었으면 재현 절차를 함께 남긴다. 증거 B를 지켜준 것이 그 재현 가능성이었다 — 캐시에 남은 wheel 로 전항목이 다시 나온다.` |
| 331 (2-1) | 초고 대조 서술 | `이전 판본은 이것을 "브라우저로는 안 보인다"고 뭉뚱그렸다.` | `"브라우저로는 안 보인다"고 뭉뚱그릴 자리가 아니다.` |
| 349 (2-1) | 감사 연대기 | `이 노트의 증거 B가 그 캐시로 복원됐다(1-2 재현 절차)` | `이 노트의 증거 B 대조본이 그 캐시에 남아 있었다(1-2 재현 절차)` |
| 363 (2-2) | 초고 대조 서술 | 제목 `— 이전 판본이 네 번째 행으로 적어뒀던 것` | `— 위 표에 네 번째 행으로 넣고 싶어지는 자리` |
| 393 (2-3) | 초고 대조 서술 | `실행 파일도 scrapy 가 아니라 gerapy 다(이전 판본이 scrapy 로 적어뒀다).` | `실행 파일도 scrapy 가 아니라 gerapy 다 — 제품이 Scrapy 관리 UI 라고 해서 명령줄까지 scrapy 인 것은 아니다.` |
| 432 (2-4) | 초고 대조 서술 | 제목 `… 적으면 틀린다 — 이전 판본의 오류` | `… 적으면 틀린다` |
| 513-514 (2-6) | 초고 대조 서술 | 제목 `이전 판본은 여기서 "프로젝트 생성이 선행조건"이라고 단정했다 — 거짓이다` / 본문 `근거로 "…"고 적었다.` | 제목 `"프로젝트 생성이 선행조건"은 거짓이다` / 본문 `그럴듯한 근거는 "…"는 것이다.` |
| 543 (2-6) | 정정 건수 | `이 노트가 무너진 지점이 여기다. … 만들어 붙였고, 그 허구의 전제조건이 상단 요약·3장·6장 ④·7-1 까지 다섯 군데로 번졌다.` | `이 노트가 미끄러진 지점이 여기다. … 만들어 붙이면, 그 허구의 전제조건이 상단 요약·3장·6장 ④·7-1 까지 서로 멀리 떨어진 절로 함께 번진다.` |
| 608-609 (3장) | 초고 대조 서술 | 제목 `— 이전 판본이 "빼면 400"이라고 단정했던 자리` / 본문 `그 단정은 DRF가 바디를 파싱한다는 전제 위에 서 있었는데,` | 제목 `— "빼면 400"이 통하지 않는 자리` / 본문 `"빼면 400"은 DRF가 바디를 파싱한다는 전제 위에 선다. 그런데` |
| 611 | 축③ 시제 | `반증 사례가 이 노트 안에 있었다` | `… 있다` |
| 706 (4-2) | 초고 대조 서술 | 제목 `… 거짓이다 — 이전 판본의 표에 있던 줄` | `… 거짓이다` |
| 764-767 (4-3) | 초고 대조 서술 | 4행 콜아웃: `linpeas 는 시험 허용 도구다 — 그런데 이전 판본은 도구 목록에서 통째로 지우고 / 그 자리에 실측하지 않은 getcap -r / 6행 출력을 지어 넣었다(snap 리비전 번호까지 구체적으로). / 규정 문제가 아니라 기록 문제다. 실제로 쓴 도구를 지우고 안 쓴 절차를 넣으면 … 오판한다. / linpeas/LinEnum/pspy 는 …` | 3행: `linpeas 는 시험 허용 도구다 — 기록에서 지울 이유가 없다 / linpeas/LinEnum/pspy 는 열거 전용이라 … / 규정이 걸려 실제로 쓴 도구를 목록에서 빼고 그 자리에 돌리지 않은 getcap -r / 출력을 채워 넣으면, 다음 세션이 … 오판한다. 규정 문제가 아니라 기록 문제다.` (교훈 보존, 순서만 압축) |
| 847 (4-4) | 초고 대조 서술 | 제목 `… pty 유무로 갈린다 — 이 노트가 한 번 틀리게 단정한 자리다` | `… pty 유무로 갈린다` |
| 851-855 (4-4) | 감사 연대기 | `[!warning] 정정 — 원 지적("…")은 절반만 맞다` / `이전 판본의 su root 블록에 비밀번호가 찍혀 있던 것은 한때 조작 증거로 읽혔다. 그런데 다른 박스([[plum]]) 검증에서 …` / `즉 이 박스에서 비밀번호가 찍혀 있던 것은` / `① … 덧붙인 것(조작) —` / `[가정] 어느 쪽인지 확정할 수 없다.` | `[!warning] "su 는 에코를 끄므로 화면에 절대 안 나온다"는 절반만 맞다` / `TTY 가 없어도 su 는 stdin 에서 비밀번호를 읽는다([[plum]] 실측) — 그래서 pty 없이 치면 화면에 남는다.` / `즉 su root 블록에 비밀번호가 찍혀 있다면` / `① … 덧붙인 것 —` / `[가정] 이 박스에서 어느 쪽인지는 확정할 수 없다.` |
| 867-868 (4-4) | 감사 날짜 | 제목 `— 반증됨 (2026-08-20, [[plum]] 실측)` / 본문 `이전 판본은 "…"고 단정했다. [[plum]] 검증에서 …` | 제목 `— 반증됨 ([[plum]] 실측)` / 본문 `"…"는 규칙이 흔히 통용되지만, [[plum]]에서 …` |
| 877 (4-4) | 초고 대조 + 정정 건수 | `이전 판본은 여기에 ssh root@… → cat proof.txt 트랜스크립트를 붙여두고, 그것을 근거로 "재접속 비용 0", "6장 ⑤의 소득", "8장 PermitRootLogin 권고"까지 다섯 군데를 지탱했다. SSH 세션 기록이 산출물 어디에도 없다. proof.txt 값은 위 su root 경로에서 얻은 것이다.` | `SSH 세션 기록이 산출물 어디에도 없다. proof.txt 값은 위 su root 경로에서 얻은 것이다.` (제목의 `[가정]` 및 뒤 두 문단의 sshd 기본값 논증은 전량 보존) |
| 946-948 (6장 ①) | 정정 건수 | 제목 `이 항목이 이 노트를 통째로 오염시킨 발원지다` / `문제는 이전 판본이 한 걸음 더 나가 … 승격시킨 것이다.` / `… 옮겨 적은 것이고, 그 한 번의 승격이 … 다섯 군데로 번졌다.` | 제목 `여기서 한 걸음 더 나가면 노트가 통째로 오염된다` / `위험한 것은 "…"으로 승격시키는 것이다.` / `… 옮겨 적는 것이고, 그런 승격은 한 번이면 … 멀리 떨어진 절까지 함께 번진다.` |
| 971-972 (6장 ②) | 감사 연대기 + 날짜 | 제목 `이 관측은 한 번 "날조"로 판정돼 삭제됐다가 철회·복원됐다 (2026-08-20)` / `적대적 검증자가 ~/PG/ 와 /tmp/gv/ 만 보고 "정품 wheel 을 받은 기록이 없다"며 절 전체를 지웠다. pip 의 HTTP 캐시(…)에 wheel 이 그대로 있었고,` | 제목 `이 관측의 대조본은 pip 캐시에 남아 있다` / `~/PG/ 와 /tmp/gv/ 에는 정품 wheel 이 남아 있지 않지만, pip 의 HTTP 캐시(…)에 그대로 있다.` (수치 재대조 결과·비용 서술 보존) |
| 975 (6장 ②) | 감사 연대기 | `이 노트는 대조는 했는데 경로를 안 적어서 한 번 통째로 지워졌다.` | `대조를 해놓고 경로를 안 적으면 나중에 스스로도 그 대조를 증명하지 못한다.` (뒤의 「결론만 적힌 관측은 자기 자신을 방어하지 못한다」 보존) |
| 985 (6장 ③) | 초고 대조 서술 | `**순서 주의**: setuid 가 먼저다(이전 판본은 "setgid 를 앞에 넣었다"고 적었는데 산출물과 어긋난다).` | `**순서 주의**: 산출물의 호출 순서는 setuid 가 먼저다.` |
| 1011 (6장 ⑤) | 초고 대조 서술 | `**소득이 아니었던 것**: 이전 판본은 여기에 "+ SSH 재접속 경로"를 덧붙였다.` | `**소득이 아니었던 것**: "SSH 재접속 경로"는 여기 넣을 수 없다.` (`[가정]` 보존) |
| 1047 (6장 ⑦ 표) | 초고 대조 서술 | `… 모두 4444 하나뿐이고, "4445"는 이전 판본이 자기 본문에서 만들어낸 값이다` | `… 모두 4444 하나뿐이다` (`[가정]` 및 「이 박스에서 겪은 일이 아니다」 보존) |
| 1102 (7-1) | 초고 대조 서술 | `이전 판본은 이 목록에서 linpeas 를 통째로 빼고 프론트매터의 tech/enum/peas 태그까지 지웠는데, 실제로 privesc를 짚어준 것이 그것이었다(4-3).` | `실제로 privesc를 짚어준 것이 linpeas 이므로(4-3) 도구 목록에서도 프론트매터의 tech/enum/peas 태그에서도 뺄 이유가 없다.` |
| 1129 (7-1) | 초고 대조 서술 | 제목 `이전 판본은 여기에 "② 프로젝트 생성 (선행조건)"을 넣어 4개로 적어뒀다` | `"② 프로젝트 생성 (선행조건)"을 끼워 넣어 4개로 적고 싶어지는 자리다` |
| 1143 (7-2) | 감사 연대기 | `증거 B는 결론만 적혀 있었던 탓에 적대적 검증에서 "날조"로 판정돼 통째로 삭제됐다가 ~/.cache/pip 에 남은 wheel 로 복원됐다.` | `증거 B는 결론만 적혀 있어서 나중에 되짚을 때 ~/.cache/pip 에 남은 wheel 을 찾아내기 전까지는 스스로를 증명하지 못했다.` |
| 1146 (7-2) | 초고 대조 서술 | `이전 판본은 확인 없이 "비인증은 /api/user/auth 하나뿐"이라고 단정했다.` | `확인 없이 "비인증은 /api/user/auth 하나뿐"이라고 단정하기 딱 좋은 자리였다.` |
| 1160 (7-2) | 감사 연대기 | `이 노트의 증거 B가 그 오류로 통째로 삭제됐다 — 검증자가 … "wheel 을 받은 적이 없다 → 대조한 적이 없다 → 날조다"로 세 단계를 뛰었고,` | `이 노트의 증거 B가 그 함정 위에 놓여 있다 — … "wheel 을 받은 적이 없다 → 대조한 적이 없다"로 두 단계를 뛰게 되는데,` |
| 1162 (7-2) | 감사 어휘 | `검증자에게만 해당하는 규율이 아니다` | `기록을 되짚을 때만의 규율이 아니다` |
| 1218 (관련 노트) | 감사 연대기 | `… "출제자가 표식을 조작한다"는 교훈의 실제 근거지 — 이 노트가 한때 자기 사례인 것처럼 적었던 것` | `… 교훈의 실제 근거지` |
| 1220 (관련 노트) | 초고 대조 서술 | `이 노트 4-4 의 su 서술이 그 결과로 정정됐다` | `… 그 실측을 따른다` |

### 삭제 — 원문 전문 (되살릴 수 있게)

`Levram.md:1219` 관련 노트 항목. **유일하게 인식론적 표시(`[가정 — 아직 재검증하지 않았다]`)를 함께 잃은 편집**이므로 원문을 그대로 남긴다.

```
- [[RubyDome]] · [[Astronaut]] — 같은 "버전 판정은 독립 근거 2개" 패턴. [[RubyDome]]은 같은 날 같은 적대적 검증을 받은 짝 노트다(소스 인용은 정확, 터미널 출력은 날조). 단 Levram 에서는 그 패턴이 그대로 성립하지 않는다 — "실행했어야 하는 것"으로 분류돼 삭제된 증거 B가 실제로는 수행된 작업이었다. 같은 배치에서 나온 판정이라도 개별로 재확인해야 한다는 뜻이고, [[RubyDome]] 쪽 판정도 같은 기준으로 재검증할 가치가 있다 `[가정 — 아직 재검증하지 않았다]`
```

대체문:

```
- [[RubyDome]] · [[Astronaut]] — 같은 "버전 판정은 독립 근거 2개" 패턴. 단 "소스 인용은 정확하고 터미널 출력은 지어냈다"는 이 유형의 흔한 실패 모드를 Levram 에 그대로 대입하면 틀린다 — 여기서 증거 B는 "실행했어야 하는 것"이 아니라 실제로 수행된 대조였다
```

**판단 근거**: 잃은 `[가정 — …]` 이 수식하던 명제는 「[[RubyDome]] 쪽 **감사 판정**을 재검증할 가치가 있다」로, 노트 내용이 아니라 **감사 작업 큐**다. 지시서의 남길 목록(「내용에 붙은 인식론적 표시」)에 해당하지 않는다고 보아 문장째 걷었다. 검산 지표인 `[가정]` 20개는 그대로다. 이 판정을 뒤집으려면 위 원문을 그대로 되돌리면 된다.

### 손대지 않기로 한 것과 이유

| 대상 | 이유 |
|---|---|
| `Levram.md:35` 「이 구분이 이 노트에서 한 번 무너졌다」 | 감사 연대기가 아니라 **노트 자신의 시행착오 자기진단**(6장 계열). 0장 학습 항목의 골자를 이룬다 |
| `2-3` 「코드 감사에서 shell=True … 를 grep하는 이유」 | 「감사」가 **코드 감사**를 가리키는 정상 산문. 기계적 치환 대상 아님 |
| 코드펜스 78개 전량 | 지시서 ⛔. 내부 오탈자로 보이는 것도 실측 가능성이 있어 무변경 |
| `┌──(kali㉿kali)` 18개 · 타겟 pty 프롬프트 17개 | 레거시 실측 표식 및 「대화형 셸에서 읽었는가」 판정 근거 |
| 부록 A(2026-06-26 세션) 전체 | 다른 랩 인스턴스의 보존본. 플래그·IP가 본문과 다른 것은 의도된 대조 자료 |
| `1188` 8장 표 행의 `[가정 — 이 박스에서 확인하지 않았다]` | 내용에 붙은 인식론적 표시 |
| `1229-1230` 부록 A 제목 뒤 빈 줄 2개 | 구조 변경 회피 |

### 넘긴 것 — 사실 의심 (수정하지 않음, `writeup-auditor` 영역)

1. **`Levram.md:106`·`971` 의 「캐시 wheel 3종 재대조」** — 이 검토에서 파일시스템 확인을 하지 않았다(CLAUDE.md §3 ⛔ 「파일시스템을 헤매지 마라」). 서술 자체는 재현 절차(1-2 콜아웃)를 동반하므로 표현상 문제 없음. 사실 확인이 필요하면 감사자 몫.
2. **`Levram.md:1077` 의 mtime 타임라인 코드펜스** — 「17:29:45 50640.py … 17:37:06 pathB.sh」. 코드펜스라 무변경. 「8분」 총계와 개별 mtime 의 정합은 검증하지 않았다.
3. **`Wheels.md:167` 의 마운트 서술** — 삭제한 감사 콜아웃이 근거로 든 실측(`/dev/sda2 on / ext4 rw,relatime`)이 본문 코드펜스로는 남아 있지 않다(산문으로만 인용). 실측 원문이 `~/PG/Wheels/` 에 있는지는 확인하지 않았다.
4. **`Levram.md:852` 계열의 「[[plum]] 실측」** — 다른 노트를 출처로 삼은 서술. 교차 확인은 하지 않았다.
