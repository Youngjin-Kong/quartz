# GLPI.md — 적대적 검증 + 정정 (2026-08-21)

대상: `03. PG\GLPI.md` (23,829B → 23,362B)
초고: `03. PG\_backup\GLPI.md.bak` (15,929B, 08:23)
증거원: `~/PG/GLPI/` 52개 · 볼트 `파일보관\PG-GLPI-*.png` 2장 · `03. PG\_AUDIT\GLPI-run.md` · NVD

⚠️ **정정 전 forge 판(23,829B)은 git 에 없다**(노트가 untracked). 아래 「삭제·강등」 절의 원문 인용이 유일한 복원 근거다.

---

## 1. 고친 것

| 위치(문자열) | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| §1 nmap 코드펜스 `\|_http-title: Did not follow redirect to http://glpi/` | **날조.** `nmap.log`·`nmap_stdout.log` 어디에도 이 줄이 없다. 실제 값은 `\|_http-title: Authentication - GLPI` | 실제 두 줄(`http-server-header`·`http-title`)로 교체 |
| §1 산문 「80 이 `http://glpi/` 로 리다이렉트하므로 `/etc/hosts` 에 … 넣거나 `Host: glpi` 로 접근한다」 | **반증됨.** `head_80.txt` 가 IP 직접 요청에 `HTTP/1.1 200 OK` + GLPI 세션쿠키를 반환. 리다이렉트 자체가 없다 | 「리다이렉트도 vhost 요구도 없음 — 200 OK 로 로그인 화면이 그대로 옴(출처 `head_80.txt`)」로 교체 |
| §1 「`/CHANGELOG.md` **최상단**」 | 최상단은 `# GLPI changes`. `## [10.0.2] unreleased` 는 6행(첫 «버전» 항목) | 「첫 버전 항목」 + 행번호·바로 아래 항목(`## [10.0.1] 2022-06-02`) 근거 명시 |
| §1 「GLPI **9.5.x~10.0.2** 는 …」 | 하한 `9.5.x` 의 근거가 없다. NVD 표기는 "GLPI **through** 10.0.2" | 「NVD 의 영향 범위 표기는 "GLPI through 10.0.2"」로 교체 |
| §2 「응답은 **200 으로 오고**」 / §6 「응답은 **전부 200 이었지만**」 | curl 호출에 상태코드가 기록되지 않았다. 관측된 것은 «정상 페이지 본문이 통째로 왔다» 뿐 | 「응답 본문은 정상 페이지가 통째로 돌아오고」 / 「응답은 전부 정상 페이지였지만」 |
| §6 「이전 러너가 bcrypt 를 rockyou 로 깨려고 **30분 CPU 를 태우며** 대기하다 멈춰 있었다」 | `30분` 의 출처가 어디에도 없다. `GLPI-run.md` 는 "in progress" 만 적었고 john 산출물(`.pot`·로그)도 없다 | 「rockyou 로 돌린 채 그 앞에서 대기하다 멈춰 있었음(경과 시간은 기록에 없음 [관측 없음])」 |
| §6 「**9분 40초**」 | `rce_a.html` 22:52:00 → `am_id.html` 23:01:41 = 9분 **41**초 | 9분 41초 |
| §8 「GLPI 는 CVE-2022-35914 패치(10.0.3)에서 **이 파일을 삭제했다**」 | 10.0.3 이 fixed version 인 것은 NVD 로 확인. 그러나 «파일 삭제»가 수정 내용이라는 것은 `10.0.3` 태그 CHANGELOG 로 확인 불가(해당 절이 비어 있음) | 「GLPI 는 10.0.3 에서 수정(NVD 기준 fixed version)」 — 삭제 여부 주장 제거 |
| §3 `./rce.sh 'id'` 블록의 `# uid=33…` | 출력 자체는 `am_id.html` 로 실측 확인됨. 다만 출처 표기가 없어 재구성인지 캡처인지 불분명 | 주석에 `(응답 원문: am_id.html)` 부기 |
| §6 SSH 재사용·홈파일 항목 | 산출물 출처 미표기 | `ssh_reuse.txt`·`sweep1.txt` 출처 부기 |
| 전 구간 산문 | 서술형 종결 | 명사 종결형 개조식으로 전환. **코드펜스·표·플래그값·프롬프트는 한 바이트도 안 건드림** |
| §5 dash 소절 | — | 관리자 요청대로 Kali `/tmp` **nosuid** 함정 한 줄 추가 |

## 2. 삭제·강등한 것 — 원문 인용

**(a) §2 disable_functions 뒤 mtime 고고학 문단 — 축약(강등 아님, 유보는 유지)**

> 이 파일은 러너가 한 줄로 줄여 적어둔 것이고 `php.ini` 원문 출력은 남기지 않았다 — 목록의 나머지(`...` 부분)는 복원할 수 없다 [관측 없음]. **mtime 은 `23:20:57 UTC`, 즉 root 를 잡고 정리에 들어간 뒤다. 판정 자체는 그보다 훨씬 앞이었겠지만(`array_map` 체인이 통한 게 `23:01`) 파일로 떨어뜨린 건 마지막이었다. 같은 초에 생긴 0바이트 파일 `nothing` 도 이 명령의 부산물로 보인다 [가정].**

굵게 친 부분을 삭제. 사실 자체는 맞다(`disable_functions.txt`·`nothing` 둘 다 mtime `08:20:57 +0900` = `23:20:57 UTC`). **삭제 사유는 깊이 기준** — 「이 문단을 지우면 심사관이 재현하지 못하거나 납득하지 못하는가?」에 아니오. 파일 생성 순서 고고학이지 재현 정보가 아니다. 앞 문장의 `[관측 없음]` 유보는 그대로 남겼다. (이 삭제로 노트의 `[가정]` 표기가 0개가 됨 — 「관측 없음」은 8개 유지.)

**(b) §5 harvest 부재 문단 — 두 문단을 한 문단으로 병합**

> 그래서 이 박스에는 **root 권한 열거 기록이 하나도 없다** [관측 없음]. `/etc/shadow` 는 harvest 의 `-- shadow (읽히면) --` 아래가 비어 있고, `===== FLAGS =====` 에는 `/home/betty/local.txt` 만 있으며 **`/root/proof.txt` 는 안 잡혔다.** root 크론·root 프로세스 환경도 마찬가지다. 박스는 이미 정지돼 다시 걷을 수 없다.
>
> 셸 획득 직후에 돌렸어야 할 **user 단계 harvest 도 없다.** 남은 harvest 는 이 한 개뿐이다.

「`/root/proof.txt` 는 안 잡혔다」만 삭제(앞 절의 「`local.txt` 만 있다」와 동어반복). 나머지는 병합만 하고 내용·유보 전부 유지.

**(c) §2 RCE 스크린샷 부재 문단 — 표현만 축약**

> …`shot_rce_success.png` 라는 이름의 파일이 있지만 `shot_htmlawed.png` 와 md5 가 같은 **동일 파일**이다… — **위 §1 의** htmLawed 테스트 페이지 화면이…

「Kali 에」를 추가하고 굵게·「위」를 뺀 것뿐. **판정(「관측 없음」·md5 근거·62837바이트)은 전부 유지.**

**삭제한 절은 없다.** 강등(등급 하향)은 (a) 1건.

## 3. 반증한 것 — 지적으로 올랐다가 노트가 옳았던 것

1. **`shot_rce_success.png` 중복 판정 — 노트가 정확했다.** 지시받은 md5 두 개를 실측 재확인:
   ```
   5b8b79b333d505816c05b501ef6f563c  shot_htmlawed.png
   5b8b79b333d505816c05b501ef6f563c  shot_rce_success.png     ← 동일
   1ce5458d67e7622567e07d361127666f  shot_80_glpi_login.png
   ```
   볼트 `파일보관\` 도 `PG-GLPI-htmlawed-testpage.png`(5b8b79b3…) · `PG-GLPI-login.png`(1ce5458d…) 2장뿐, 서로 다른 md5. **다른 곳에 같은 파일이 두 이름으로 인용된 사례는 없다.** `![[...]]` 링크 2개 모두 실재 파일을 가리킨다. 노트 322행의 경위 서술도 정확 — 손대지 않았다.
2. **rootbash 소절은 실측이다 — 재확인 완료.** `harvest_betty.txt:367` 에 총괄이 준 것과 **한 글자도 다르지 않은** `ps` 줄이 있다. 날조 아님. 그대로 뒀다.
3. **`harvest_root.txt` → `harvest_betty.txt` 개명이 옳다.** `GLPI-run.md:61` 이 "harvest.sh run as root -> `harvest_root.txt` (1338 lines)" 라고 적었지만, 실제 파일은 `harvest_betty.txt` 이고 **행수는 1338 로 일치**하며 `===== WHOAMI =====` 는 betty 다. 러너의 자기 보고가 틀렸고 forge 의 정정이 맞다.
4. **`webapps` 소유자를 `[관측 없음]` 으로 강등한 forge 판단이 옳다 — 다만 반대 증거가 있다.** `GLPI-run.md:36,55` 는 "owned **betty:betty**, Confirmed live" 라고 두 번 단언한다. 그러나 `~/PG/GLPI/` 어느 파일에도 `ls -la /opt/jetty/…` 출력이 없고, harvest 에 남은 것은 `find / -writable -type d` 결과뿐이다. **러너 보고서는 raw 산출물이 아니므로** 강등 유지가 맞다. 총괄이 뒤집고 싶으면 이 줄이 근거다.
5. **`~/.zsh_history` 에 GLPI 흔적 0건.** 즉 이 박스는 전부 비대화형 `ssh kali "..."` 로 돌았다. §4 `sshpass`·§5 `scp` 블록은 «재구성된 명령»이다. 다만 **출력이 붙어 있지 않고 Kali 프롬프트도 없어** 정직성 규율 위반이 아니다. 손대지 않았다.
6. **`disable_functions` 판정 자체는 정확하다.** `disable_functions.txt` 원문이 `exec disabled; system/passthru/shell_exec NOT disabled`. 공개 PoC 불발의 근거로 성립.
7. **§2 「입력 hexdump 를 보여줌」·「출력 구간이 비어 있다」 둘 다 실측이다.** `m_id.html` 에 `hexdump` 문자열 존재. `poc_id.html` 을 `rce.sh` 와 동일한 정규식으로 파싱하면 결과가 빈 문자열(`''`). 「`sleep` 에 타이밍 지연 없음」도 `m_sleep.html`(07:58:24.135) → `m_id.html`(07:58:24.867) 0.7초 간격으로 뒷받침된다.
8. **타임존 함정 없음.** 노트의 UTC 표기가 전부 정확하다 — `ls` mtime 은 `+0900`, 노트는 `-9h` 한 UTC 로 적었다(`08:01:41 +0900` → `23:01:41 UTC` 등). 미환산 오류 0건.
9. **산출물 개수 52개 정확.** `.`·`..` 제외 실측 52.
10. **`0바이트 htmLawed_src.php`·`nothing` 은 그대로 뒀다.** 「빈 응답을 받았다는 기록」이라는 §6 서술 유지.

## 4. 근거부족으로 남긴 것 (정정하지 않음)

- **「Fundamental 난이도(860명 풀이)」** — 포털에서만 보이는 값이고 박스는 정지됐다. 고정 출처 목록에 없다. 「포털 표시」를 부기만 하고 값은 유지했다. 논지(「대중적 난이도 + 브루트포스 = 신호」)가 정확한 숫자에 의존하지 않는다.
- **§2 페이로드 해부의 `$C` = `['array_map','system']`** — htmLawedTest.php 실제 소스에서 `$cfg` 가 어떻게 조립되는지는 확인하지 않았다. 확인하려면 소스 고고학이 되고, 새 깊이 기준상 과잉이다. 서술은 관측된 페이로드·결과와 모순되지 않는다.
- **「이전 러너의 nc:443 리스너 / 좀비 watcher」** — `GLPI-run.md:68` 의 PID 기록이 유일한 근거. Kali 산출물에는 없다.

## 5. 총괄에 넘기는 것

- **색인 갱신 필요.** `GLPI.md` 는 신규 노트라 `ports` 프론트매터가 없고 `_INDEX` 에 미반영이다. `refresh.ps1` 은 공유 인프라라 돌리지 않았다. `manual_tags`/`manual_cves` 선언은 정상이고 값 줄에 주석 없음(§5 함정 회피 확인). `tech/*` 5종·`CVE-2022-35914` 전부 실제 사용한 것.
- **`_STATUS.md` 판정: 완료 2/2** (근거 `proof_user.txt`·`proof_root.txt`, `euid=0(root)`). 직접 편집하지 않았다.
