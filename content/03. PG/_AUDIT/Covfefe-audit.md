---
type: audit
target: "03. PG/Covfefe.md"
box: Covfefe
date: 2026-09-09
manual_tags: true
manual_cves: true
---

# Covfefe writeup 적대적 검증

대상 `03. PG\Covfefe.md` (485행 → 491행). 검증과 정정을 한 사이클로 수행.

## 0. 증거원

| 출처 | 내용 |
|---|---|
| git 원본 | **baseline 부재.** 노트가 커밋되지 않은 신규 파일(`git status` 에서 `?? "03. PG/Covfefe.md"`). 개작 전후 대조 불가 |
| Kali 산출물 | `~/PG/Covfefe/` 42개 + `p31337/`·`svc/`·`web-31337/`·`web-80/`. mtime 은 `ls -la --time-style=full-iso` 로 전량 확보 |
| 볼트 스크린샷 | `파일보관\PG-Covfefe-{31337-404,nginx-default,robots-txt,ssh-listing,taxes}.png` 5장 — 전부 실재. 크기가 Kali 원본 `shot_*.png` 와 일치 |
| `~/.zsh_history` | Covfefe·`192.168.103.10` 검색 결과 **0건**. Kali 쪽 작업이 비대화형이었다는 노트의 전제 확인 |
| 직접 실행 | Kali 에서 `printf` 페이로드 두 형태를 `xxd` 로 대조 |

## 1. 고친 것

| 위치(문자열) | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| `— 출처: ~/PG/Covfefe/p31337/.ssh` | **인용 경로가 존재하지 않는 파일.** `p31337/` 전량 목록(점 파일 포함)에 `.ssh` 부재 — 저장된 것은 `.ssh_id_rsa`·`.ssh_id_rsa.pub`·`.ssh_authorized_keys`·`.ssh_known_hosts` 뿐 | 본문 값(`['id_rsa', 'authorized_keys', 'id_rsa.pub']`)은 유지. 출처를 「원문 미보존 + 스크린샷에서 전사」로 교체하고, 43바이트라는 길이가 gobuster `[Size: 43]` 과 일치한다는 교차 근거를 캡션에 부착 |
| `/.ssh 디렉터리를 직접 요청, 목록 확보` → 그 뒤 `목록의 id_rsa 를 다운로드` | **시간 서사 역순.** `p31337/.ssh_id_rsa`(1766바이트) mtime 09:58:26 은 `.bashrc`·`.profile`·`robots.txt`·`taxes` 와 같은 첫 배치. 목록을 본 뒤 받은 것이 아니라 표준 경로 추측이 먼저 맞은 것 | 실제 순서로 재서술. 근거로 두 번째 배치(09:58:47~48)가 `/.ssh/known_hosts` 까지 요청해 404(233바이트)를 받은 사실을 명시 — 목록을 쥐고 있었으면 없는 이름을 요청할 이유 부재 |
| `root 획득 후, 10:00:41 http_server.py 소스를 직접 열람` | **틀린 인과·시간.** `read_message_src.txt` mtime 10:00:41 < `root.sh` 10:01:31 < `proof_root.txt` 10:01:57. root 이전이고, `/home/simon/http_server.py` 는 simon 소유 755 | 「SSH 로그인 후 simon 권한으로 10:00:41 열람」으로 정정 |
| `루트 획득 후 dpkg -l 결과` | `version_evidence.txt` 의 `ps auxf` 가 그 명령을 실행한 프로세스를 `simon 1616` 으로 기록. root 컨텍스트 아님 | 「SSH 셸에서 받은 `dpkg -l`」로 정정 |
| ```` ```bash / python3 -c … > /tmp/pl.bin ```` (root.sh 인용) | **펜스가 4행 중 2행만 인용.** `#!/bin/bash`·`cd /tmp` 누락, 생략 사실 미기재(A-1) | 원문 4행 전량으로 복원 |
| proof_root.txt 인용 2블록 | **줄바꿈 변조 + 무고지 절단.** ①`pty.spawn(\"/bin/bas` / `h\")"` 를 한 줄로 합침 ②`root@covfefe:/tmp# whoami; …` / ` cat /root/flag.txt` / `<e -I; date; …` 3행 삭제 ③`uid=0(...)` 줄바꿈 합침. 두 블록으로 쪼개면서 중간이 사라진 것 | 한 펜스로 합쳐 **원문 바이트 그대로** 복원(80칸 접힘·프롬프트 재출력 잔재 포함). 스크립트로 `proof_root.txt` 와 완전 일치 확인 |
| `~/PG/Covfefe/s.sh "find / -perm -4000 -type f 2>/dev/null"` | **실행 불가능한 귀속.** `harvest_simon.txt` mtime 10:00:13 이 `s.sh` 생성 10:00:29 보다 앞섬. 출력은 `harvest.sh` 의 SUID 절(`~/PG/_lib/harvest.sh:39`) | 래퍼 호출을 걷어내고 `find / -perm -4000 -type f 2>/dev/null` 만 남김. 열거 스크립트 일괄 실행이라는 사실을 앞 문장에 기재 |
| `~/PG/Covfefe/s.sh "cat /root/read_message.c"` | 산출물은 C 소스 + `===HTTPSERVER===` + `===STRINGS===` 를 담은 복합 명령의 결과. 단일 `cat` 이 만든 파일 아님 | 명령을 `cat /root/read_message.c` 로 두고, 출처 캡션에 「SSH 클라이언트 경고 배너 4행과 뒤따르는 두 구간 생략」 명시. 펜스 태그도 `text` → `c` |
| `~/PG/Covfefe/s.sh "whoami; id; … cat /home/simon/local.txt"` | **캡처 방식과 모순.** `proof_user.txt` 에 `/etc/motd` 꼬리·`Last login:`·셸 프롬프트·80칸 하드 랩이 있어 pty 세션 산물. `ssh host "cmd"` 는 pty 도 motd 도 없음 | 래퍼를 인자 없이 띄운 대화형 pty 세션이라고 서술하고, 명령 줄은 캡처 안의 프롬프트 뒤 원문에 맡김. 펜스 태그 `text` → `bash`(A-1 표의 「셸 세션 캡처」) |
| `printf 'Simon%.0sA' {1..15}` (수동 대안) | **틀린 명령.** Kali 에서 실행 시 `SimonASimonA…` 출력 — 페이로드 파손. `xxd` 로 확인 | 검증된 형태 `printf 'Simon'; printf 'A%.0s' {1..15}; printf '/bin/sh\0'` 로 교체하고, 틀린 형태를 함정으로 함께 기재 |
| `mtime 2026-09-09 09:57~10:05, 총 9분 12초` | **수치 오류.** 산출물 mtime 범위는 09:57:15.973(`recon-preflight.txt`) ~ 10:05:27.865(`version_evidence.txt`) = **8분 12초** | 해당 콜아웃 자체를 삭제(아래 2절) |
| 상단 `[!warning]` 메타 콜아웃 | 문서가 자기 자신·작성 방식을 서술(A-2「문서가 자기 자신을 서술하지 말 것」·A-5「작업 조직·작성 도구를 본문에 쓰지 말 것」). 「Kali 쪽 명령은 전부 비대화형 `ssh kali …` 로 돌았다」는 전형적인 산출 방식 표시 | 삭제. 인식론 표시는 유지 — 타겟 프롬프트가 실측이라는 근거는 239·458행 출처 캡션이, `[가정]`·「원문 미보존」은 본문 각 지점이 이미 담당 |
| `그림 1 — … gobuster-80.txt 도 / 리다이렉트 외` | 본문(108행)은 `/.`, 캡션은 `/`. `gobuster-80.txt` 원문은 `/.` | 캡션을 `/.` 로 통일 |
| `@app.route('/.ssh')` | 소스 원문은 `@app.route(sauce)` + `sauce = '/.ssh'` | 원문 형태로 정정 |
| `/root/flag.txt(32바이트, …)` | 수치 자체는 정확하나 근거 미기재(A-3) | `harvest_root.txt` FLAGS 절의 `-rw------- 1 root root 32 Jul 14 2020` 행을 근거로 병기 |
| `수행됨을 확인`·`덮어씀`·`필수는 아님`·`아님`·`드러냄`·`읽음` 등 | A-4 명사형 어미(`~함`·`~임`·`~됨`·`~음`) 금지 위반 | 파생명사·상태어로 교체. 정정 후 코드펜스 밖 위반 **0건**(잔여 검출은 전부 「없음」·「해당 없음」 등 A-2 정의 토큰과 「처음」·「다음」 같은 일반 명사) |

### 추가한 것 — 산출물에 있으나 노트에 없던 것

| 추가 | 근거 |
|---|---|
| UDP top-100 결과 한 줄 | `nmap-udp-top100.txt` — 9개 `closed`·91개 `open\|filtered`. 「배제했다」와 「안 해봤다」를 가르는 자리 |
| 31337 앱이 simon 권한으로 구동 | `version_evidence.txt` 의 `ps auxf`: `simon 415 … /bin/sh -c /home/simon/http_server.py`. `environ['HOME']` 이 `/home/simon` 으로 풀린 인과의 실측 근거 |
| `/root` 가 `drwxr-xr-x`, `read_message.c` 가 `-rw-r--r--` | `lsdump.txt` — 바이너리 역분석 없이 소스로 취약점을 확인할 수 있었던 이유 |
| `cat /tmp/pl.bin /dev/stdin` 의 역할 | `root.sh` 원문. 페이로드만 파이프하면 셸이 EOF 로 즉사하는 지점이라 재현에 필수 |
| `/local.txt` 웹 노출의 직접 증거 | `p31337/local.txt`(33바이트, 09:58:46)에 플래그 값 존재. 웹 열람 값이 시험 규정상 0점이라는 판정과 함께 기재 |
| 원문 미보존 항목 4건 목록 | 아래 3절 |
| `nmap` 명령을 tmux 배경 실행으로 명시 | `.bg-fullscan.sh`·`_SUMMARY.txt` 의 `rc-Covfefe-full` 세션 기록 |

## 2. 삭제한 것 — 원문 인용

삭제는 1건. 아래 전문을 남기므로 되살릴 수 있음.

```markdown
> [!warning] 이 노트를 읽는 규약 — 관측과 재구성을 구분할 것
> 코드펜스는 Kali 산출물 `~/PG/Covfefe/`(mtime 2026-09-09 09:57~10:05, 총 9분 12초) 원문만 수록. 블록마다 출처 캡션 부착.
> - **타겟 대화형 셸 프롬프트(`simon@covfefe:~$`·`root@covfefe:/tmp#`)는 `proof_user.txt`·`proof_root.txt` 에서 그대로 인용한 실측** — 창작이 아님. Kali 쪽 명령은 전부 비대화형 `ssh kali@10.44.44.128 "..."` 로 돌았고 그쪽에는 `┌──(kali㉿kali)` 류 프롬프트 미부착
> - `/.ssh` 경로를 처음 어떤 근거로 직접 요청했는지는 비대화형 세션이라 `~/.zsh_history` 미기록 — 아래 서술의 「robots.txt 로 추정」은 관측된 산출물(로봇 배제 목록·gobuster 결과)로부터의 재구성, `[가정]` 표기
> - `[가정]` — 이 박스에서 직접 실행·관측하지 않은 추론
```

**근거** — ①A-5「작성 도구·작업 방식을 드러내지 말 것」: 「Kali 쪽 명령은 전부 비대화형 `ssh kali …` 로 돌았고」는 산출 방식 표시 그 자체. 이 노트는 공개 발행 대상. ②A-2「문서가 자기 자신을 서술하지 말 것」: 「코드펜스는 … 원문만 수록」은 문서에 대한 메타 서술. ③「총 9분 12초」는 실측(8분 12초)과 불일치. ④두 번째 불릿의 인식론 내용은 본문 152~156행에 더 구체적인 형태로 이미 존재하고, 타겟 프롬프트가 실측이라는 근거는 239·458행 출처 캡션이 담당하므로 손실 부재.

**강등 0건** — `[가정]`·「원문 미보존」 표기는 전부 유지했고, 오히려 4건을 「관련」절에 새로 명시.

**타겟 pty 프롬프트는 한 글자도 건드리지 않음.** `simon@covfefe:~$`·`root@covfefe:/tmp#` 는 원문 대조로 오히려 **누락분을 되살렸다**(위 proof_root 항목). `┌──(kali㉿kali)` 류 창작 프롬프트는 노트 전체에 부재 확인.

## 3. 반증한 것 — 지적으로 올랐다가 노트가 옳았던 것

| 의심 | 확인 결과 |
|---|---|
| `/root/flag.txt` **32바이트** 가 산출물로 뒷받침되는가 (총괄 지시의 중점 3) | **노트가 옳음.** `lsdump.txt` 의 `/root` 목록 `-rw------- 1 root root 32 Jul 14 2020 flag.txt` 와 `harvest_root.txt:707` 이 독립적으로 32바이트 기록. 문자열 `Your flag is in another file...`(31자 + 개행)과도 일치. 수치를 빼거나 `[가정]` 강등할 필요 부재 — 근거만 병기 |
| `read_message.c` 주석의 `flag2{use_the_source_luke}` 가 실재하는가 | **실재.** `read_message_src.txt` 의 C 소스 구간에 `// You're getting close! Here's another flag:` / `// flag2{use_the_source_luke}` 두 줄 그대로. 노트 인용이 바이트 단위로 일치 |
| 「남긴 흔적」표의 `/tmp/h.sh`·`/tmp/hr.sh`·`/tmp/.h/` 가 지어낸 경로인가 | **전부 실재.** `harvest_root.txt:660~674` 의 `/tmp` 목록에 `.h/`·`h.sh`·`hr.sh`·`pl.bin`·`root.sh`·`sweep.sh` 6개 모두 존재. 표의 「잔존」판정이 정확 |
| `sweep.sh`·`s.sh`·`john_out.txt` 인용이 변조됐는가 | **무변조.** 스크립트로 펜스와 산출물을 바이트 비교해 완전 일치 확인. `proof_user.txt` 도 80칸 접힘(`cat /home/simon/local.` / `txt`)까지 원문 그대로였음 |
| `~/PG/Covfefe/p31337/.ssh` 부재가 곧 「목록을 본 적 없다 = 날조」인가 | **날조 아님.** 부재 증거로 판정을 내리지 않았음. 스크린샷 `PG-Covfefe-ssh-listing.png` 를 직접 열어 `['id_rsa', 'authorized_keys', 'id_rsa.pub']` 를 눈으로 확인했고, gobuster `[Size: 43]` 이 문자열 길이 43과 정확히 일치. **내용은 실측, 인용 경로만 오기** |
| 총괄 지시 「본문 228·237·425·430·454행 등의 타겟 프롬프트가 인용돼 있다」 | **행 번호는 맞으나 인용이 완전하지 않았음.** 425·430·454행 계열의 root 세션은 중간 3행(프롬프트 뒤 명령 에코와 `<e -I; …` 잔재)이 빠진 절단본이었다. 「지우지 마라」 지시는 옳았고, 실제로 필요했던 조치는 **되살리기** |
| 노트 프론트매터가 템플릿(`tier`·`method`·`session_logged` 등)과 다른 것이 결손인가 | **결손 아님.** 볼트 다른 노트(`Nagoya.md`·`Flu.md`)가 같은 스키마(`type`/`platform: pg`/`ports`/`manual_tags`)를 쓰고, 템플릿 자체가 「색인 파서 소관 — 값 형식은 볼트 파이프라인이 정함」이라 명시. 건드리지 않음 |

## 4. 판정 유보 — 근거부족으로 남긴 것

- **`guess.txt`**(09:59:47, `covfefe`·`covefe`·`simon`·`simon123`·`covfefe123`·`password` 6개) — john 크랙(09:59:20) 뒤에 만들어졌는데 `starwars` 를 담고 있지 않음. 무엇에 쓰려던 후보군인지 근거부족. **박스 노트가 아니라 `_PLAYBOOK` 시행착오 재료**이므로 노트에는 미기재
- **`read_message.strings`** — 내용이 `zsh:1: no such file or directory: ssh -i simon_id_rsa_nopass …` 라 `strings` 결과가 아닌 실패 로그. 타겟에도 `strings` 부재(`read_message_src.txt` 의 `===STRINGS===` 구간이 `bash: strings: command not found`). 노트가 인용하지 않았으므로 정정 대상 아님
- **BOF 오프셋 21~43 무응답, 44 의 다른 오류** — 노트가 「근거부족」으로 표기한 것이 정확. 박스 정지로 추가 계측 불가
- **`shot_31337_id_rsa.png`·`shot_31337_bash_history.png`** — `shots.sh` 가 요청했으나 PNG 미생성. 원인 미상, 재촬영 불가. 「관련」절 원문 미보존 항목에 기재

## 5. 총괄 판단이 필요한 것

- **색인 갱신** — 노트가 git 미추적 신규 파일이라 `_INDEX` 반영 여부 미확인. 프론트매터(`ports`·`tags`·`services`)와 nmap raw 출력 3행은 정정 전후로 변경 부재이므로 파서 입력은 동일. `refresh.ps1` 은 공유 인프라라 실행하지 않음
- **`_PLAYBOOK` 신규 카드 2건 제안** — ①「Flask `static_folder=$HOME` 로 홈 전체가 웹 루트가 되는 구성」 ②「SUID 바이너리의 인접 스택 변수를 오버플로로 치환해 `execve` 대상 경로를 바꾸는 기법」. 노트 「관련」절이 두 패턴을 가리키고 있으나 카드 실체는 아직 부재
- **문체 이관 0건** — `pg-doc-reviewer` 로 넘길 건 없음. 서술형 종결(`~다.`) 검출 0건, A-4 명사형 어미 위반은 이 감사에서 전량 정정

## 6. 근거 출처

| 판정 | 출처 |
|---|---|
| 파일 실재·시간 서사 | `ssh kali "ls -la --time-style=full-iso ~/PG/Covfefe/ ~/PG/Covfefe/{p31337,svc,web-31337,web-80}"` |
| 펜스 바이트 일치 | `proof_user.txt`·`proof_root.txt`·`root.sh`·`sweep.sh`·`s.sh`·`john_out.txt`·`bof_offset_sweep.txt` 를 노트 펜스와 프로그램 비교 |
| SUID·sshd·플래그 | `harvest_simon.txt:30-44`(SUID)·`:671`(`PasswordAuthentication no`) · `harvest_root.txt:702-711`(FLAGS)·`:660-674`(`/tmp`) |
| 퍼미션·바이너리 | `lsdump.txt` — `/usr/local/bin` · `/home/simon` · `/root` 목록, `file` 출력 |
| 버전 | `version_evidence.txt` · `svc/ssh-banner.txt` · `web-31337/whatweb.txt` · `web-80/whatweb.txt` |
| 소스 | `read_message_src.txt`(C 소스 · `===HTTPSERVER===`) |
| 웹 응답 | `web-31337/probe_robots.txt` · `p31337/{robots.txt,local.txt,taxes_,.ssh_id_rsa}` · `gobuster-31337.txt` · `gobuster-80.txt` |
| 스캔 | `nmap-full.txt` · `nmap-quick.txt` · `nmap-quick-ports.txt` · `nmap-udp-top100.txt` · `.bg-fullscan.sh` · `.bg-gobuster.sh` · `recon-preflight.txt` · `_SUMMARY.txt` |
| 도구 동작 | `~/PG/_lib/recon.sh:210,266-275` · `~/PG/_lib/harvest.sh:3,7,38-39` |
| 화면 | `파일보관\PG-Covfefe-ssh-listing.png` 직접 열람 |
| 명령 검증 | Kali 에서 `printf 'Simon%.0sA' {1..15} \| xxd` 및 `printf 'Simon'; printf 'A%.0s' {1..15}; printf '/bin/sh\0' \| xxd` 실행 |
| 이력 | `git log -- "03. PG/Covfefe.md"` → 출력 부재(미추적) · `grep -in "covfefe\|103\.10" ~/.zsh_history` → 0건 |

백업 — `03. PG\_backup\Covfefe.md.bak`(정정 전 원본 485행).

---

## A-2-1 이관 — 본문에서 내린 추정 (2026-09-09)

규격 7차 개정(A-2 / A-2-1)에 따라 `[가정]` 3건·「근거부족」 1건을 본문 밖으로 이관. 「관측 없음」·「원문 미보존」은 유지 토큰이라 본문에 존치. 이관 시점의 본문 행번호 기준이며, 편집으로 이후 행번호는 이동.

### 1. 키 복호화 명령 (본문 214행)

원문:

> 패스프레이즈로 키 복호화 — `simon_id_rsa`(암호화, 09:59:17)와 `simon_id_rsa_nopass`(평문, 09:59:57) 두 파일이 실재하므로 복호화 자체는 확정이나, 정확한 명령(`openssl rsa` 계열로 추정, `[가정]`)은 원문 미보존.

- **내려온 추정** — 「`openssl rsa` 계열로 추정」. 산출물에 명령 줄이 남아 있지 않고 Kali 접속 불가로 확인 불가
- **본문에 남긴 것** — 두 파일 실재로 복호화 확정, 「사용한 명령 줄은 원문 미보존」
- **결론 동반 하차** — 없음. 복호화 성립의 근거는 두 파일의 실재뿐이라 추정에 얹힌 결론 부재

### 2. BOF 스윕 21~43·44 구간 (본문 410행)

원문:

> **오프셋 20 에서만** `euid=0` 응답 관측. 21~43 은 무응답(크래시 추정, 원인 미상 — 관측 없음), 44 는 다른 오류 메시지 — 이 두 구간을 더 파고들지 않았고 왜 그런지는 근거부족

- **내려온 추정** — 「크래시 추정」, 「왜 그런지는 근거부족」
- **내려온 자기 서술** — 「이 두 구간을 더 파고들지 않았고」. A-2-1 이 금지한 작성자 행위 서술
- **본문에 남긴 것** — 「21~43 은 무응답, 44 는 다른 오류 메시지 — 두 구간 모두 원인 관측 없음. 인스턴스 정지로 추가 계측 불가」. 「관측 없음」은 유지 토큰이라 존치
- **결론 동반 하차** — 없음. 무응답·다른 오류 메시지는 `bof_offset_sweep.txt` 원문에 그대로 있는 관측
- **판정 근거** — 확인 「불가」. 인스턴스 정지 상태이고 Kali(`10.44.44.128`)도 ping·SSH 둘 다 timeout 이라 재계측 수단 부재. 확인 「안」 한 미완이 아니므로 `<!-- 확인 필요 -->` 주석 미부착

### 3. `p31337/` 배치 요청 도구 (본문 486행, 「원문 미보존 항목」 목록)

원문:

> - `p31337/` 배치를 만든 요청 스크립트·명령 줄 — 정찰 스크립트의 `probe_` 규칙과 파일명 규칙이 달라 별도 즉석 도구로 판단(`[가정]`)

- **내려온 추정** — 「별도 즉석 도구로 판단」. 도구의 실체가 산출물에 부재
- **본문에 남긴 것** — 「저장 파일명 규칙이 정찰 스크립트의 `probe_` 규칙과 불일치」. 관측 사실이며 항목 자체는 「원문 미보존」 목록으로 유효
- **결론 동반 하차** — 도구 정체 판단만 하차. 파일명 규칙 불일치는 `p31337/` 과 `web-31337/probe_robots.txt` 의 이름을 비교한 관측

### 4. 복호화 명령의 `[가정]` 표기 (본문 487행, 「원문 미보존 항목」 목록)

원문:

> - `simon_id_rsa` → `simon_id_rsa_nopass` 복호화 명령 — 두 파일이 실재하므로 복호화 자체는 확정, 명령은 `[가정]`

- **처리** — 추정 내용 자체는 1번(본문 214행)에 있었고 이 줄의 `[가정]` 은 그 표시의 반복. 실제 상태는 추론이 아니라 「원문 미보존」이므로 토큰 교체
- **본문에 남긴 것** — 「두 파일 실재로 복호화 확정, 명령 줄만 원문 미보존」
- **결론 동반 하차** — 없음

### 대조 산출물

Kali `10.44.44.128` ping·SSH 모두 timeout 으로 `~/PG/Covfefe/` 재대조 불가. 이 이관은 본문·백업본·이 감사 파일만으로 수행. 인용한 원문 출처는 `03. PG\_backup\Covfefe.md.pre-a2-20260909.bak`(491행).

---

## 적대적 검증 2차 — A-2 / A-4L 개작분 (2026-09-09)

대상 `03. PG\Covfefe.md`(513행 → 515행). 대조 기준은 `03. PG\_backup\Covfefe.md.pre-a2-20260909.bak`(491행).

### 0. 이번 회차의 증거원

| 출처 | 상태 |
|---|---|
| Kali `~/PG/Covfefe/` | **미검증(대조 불가)** — `10.44.44.128` 접속 불가. 산출물 재대조가 필요한 항목은 전부 이 등급 |
| `_backup\Covfefe.md.pre-a2-20260909.bak` | 행 단위 `diff` 로 개작분 11개 hunk 전량 대조 |
| 볼트 `파일보관\PG-Covfefe-*.png` | 5장 실재. `ssh-listing`·`31337-404` 두 장 직접 열람 |
| 로컬 직접 실행 | `printf` 페이로드 두 형태를 `od -c` 로 대조 |
| 검사기 | `style-check.ps1` — `Covfefe.md` 위반 0건(정정 전후 동일) |

### 1. A-4L 분할이 새 사실을 만들었는가 — 최대 위험 항목

11개 hunk 전량 대조 결과 **정보량 초과 0건.** 분할·불릿화만 일어났고 인과·수치·판정의 신설 부재.

| 분할 지점 | 판정 |
|---|---|
| nmap TCP/UDP 2불릿 분할 | 원문과 사실 동일. 「3개 포트를 먼저 잡았고」→「3개 포트 선점」은 동의어 치환 |
| 버전 판정 근거 3불릿 | 「배너·패키지·런타임 세 출처가 독립적으로 일치」 — 원문은 앞의 둘만 「독립」이라 표현. 측정 채널이 네트워크·패키지DB·인터프리터로 실제 셋이라 과장 아님 |
| `p31337` 배치 3불릿 | 원문의 시각·바이트 수·순서 판정이 전량 보존 |
| BOF 성립 지점 3불릿 | 「20바이트를 채운 뒤」→「성립 지점은 20바이트」. 스윕의 `$n` 이 곧 접두 길이라 등가 |
| 수동 대안 2불릿 | 원문과 동일. 함정 형태·근거(`xxd`) 보존 |
| 미끼 플래그 3불릿 | 원문 한 불릿을 셋으로 가른 것뿐 |

**`robots.txt` 서빙 「추정」→「단서」 치환** — robots.txt 가 홈 아래 점 파일을 나열한 것은 관측 사실이라 「단서」 쪽이 오히려 정확. 요약(25행)은 여전히 「추정」으로 남아 서사 충돌 부재.

### 2. 고친 것

| 위치(문자열) | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| `Steps to reproduce` 2·3 단계 | **내부 모순.** 본문 156~160행이 「개인키는 목록 열람 뒤가 아니라 표준 경로 추측으로 먼저 확보」로 정정됐는데 재현 절차는 「목록 확보 → 다운로드」 옛 순서 잔존. 요약(25행)의 「`/.ssh/id_rsa` 직접 요청」과도 충돌 | 실제 실행 경로 순서로 교체 — 2단계 `/.ssh/id_rsa` 직접 요청, 3단계 목록 확인 |
| `21~43 은 무응답` | **과잉 판정.** `sweep.sh` 가 출력을 `grep -a -E "uid=\|MARKER_\|Segmentation\|Sorry\|No such\|not found"` 로 거른다. 빈 구간은 「응답 부재」가 아니라 「필터 매칭 부재」 | 「`sweep.sh` 의 grep 필터에 걸린 출력 부재」로 강등. 관측 자체는 그대로 |
| Post-Exploitation 첫 문단 | **인과 결손.** 스윕 출력은 `uid=1000 … euid=0`, `proof_root.txt` 는 `uid=0 gid=0`. 이 전환의 근거가 본문에 부재 | 펜스 안에 이미 있는 `os.setreuid(0,0)`·`os.setregid(0,0)` 이 실uid·실gid 를 0 으로 고정한다는 한 줄 추가 |
| `스크린샷 6장` | **자기모순 수치.** 노트가 임베드한 것은 5장이고 별도로 2장을 「PNG 미생성」으로 기재 — 6장이 성립하려면 요청 8건에 미생성 2·미반입 1 이 동시에 필요. Kali 대조 불가라 6의 근거 미검증 | 검증 가능한 형태로 교체 — Kali 쪽 수치를 빼고 「볼트 반입분은 `파일보관\PG-Covfefe-*.png` 5장」 병기 |
| `패턴 — 이 노트, [[_PLAYBOOK]] 참조` 2행 | A-2-1「문서가 자기 자신을 서술하지 말 것」. 이 노트를 읽는 독자에게 「이 노트 참조」는 무정보 | 「이 노트, 」 제거. `[[_PLAYBOOK]]` 지시는 보존 |

### 3. 삭제한 것 — 원문 인용

문장 단위 삭제 부재. 낱말 단위 제거 3건의 원문:

```markdown
2. `/.ssh` 요청으로 디렉터리 목록(`id_rsa`·`authorized_keys`·`id_rsa.pub`) 확보
3. `/.ssh/id_rsa` 다운로드
- 21~43 은 무응답, 44 는 다른 오류 메시지 — 두 구간 모두 원인 관측 없음. 인스턴스 정지로 추가 계측 불가
… `p31337/` 경로별 응답 본문·스크린샷 6장). `simon_id_rsa_nopass` 는 …
- **"Flask 정적 서버가 홈 디렉터리를 그대로 서빙"** 패턴 — 이 노트, [[_PLAYBOOK]] 참조
```

**유지 토큰 무변경** — 「관측 없음」1 · 「해당 없음」4 · 「원문 미보존」6 · 「미계측」0. `[가정]`·「근거부족」 잔여 0건.
**코드펜스 무변경** — 펜스 내부 223행을 백업본과 프로그램 비교해 완전 일치 확인.
**타겟 pty 프롬프트 무변경** — `simon@covfefe` 6 · `root@covfefe` 4 로 백업본과 동수. `┌──(kali㉿kali)` 류 창작 프롬프트는 노트 전체에 부재.

### 4. 반증한 것 — 지적으로 올랐다가 노트·작성자가 옳았던 것

| 의심 | 확인 결과 |
|---|---|
| 487행 「원문 미보존」 토큰 교체가 추정을 사실로 승격시킨 것인가 (총괄 지시 중점 2) | **작성자가 옳음.** 「원문 미보존」은 「있었으나 저장하지 않음」. 암호화 키에서 평문 키가 나오려면 어떤 조작이든 실행이 필수라 **명령의 존재 자체는 추정이 아니라 사실**. 백업본의 `[가정]` 이 걸려 있던 대상은 「존재 여부」가 아니라 「`openssl rsa` 계열이라는 정체」이고 그 추정은 이관으로 하차 |
| 214행이 결론을 안 내렸다는 보고가 사실인가 (총괄 지시 중점 4) | **작성자가 옳음.** 후속 서술 「`simon_id_rsa_nopass` 로 SSH 접속」의 근거는 `s.sh` 원문(`ssh -i simon_id_rsa_nopass …`)이라는 산출물이지 복호화 명령 추정이 아님. 추정에 얹힌 결론 부재 |
| 410행 정정이 원 관측까지 지웠는가 (총괄 지시 중점 3) | **지우지 않음.** 「오프셋 20 에서만 `euid=0`」·「21~43」·「44 는 다른 오류 메시지」 셋 다 보존. 하차한 것은 「크래시 추정」과 자기 행위 서술(「더 파고들지 않았고」)뿐. 다만 「무응답」 표현은 별건으로 강등(2절) |
| 「인스턴스 정지로 추가 계측 불가」가 미완을 규율 준수로 위장한 것인가 | **위장 아님.** 스윕 시점에는 확인 「안」 한 미완이 맞으나 현재는 대상·Kali 양쪽이 소실된 확인 「불가」. `<!-- 확인 필요 -->` 주석은 검토 때 채울 수 있어야 성립하므로 부착 대상 아님 |
| `printf 'Simon%.0sA' {1..15}` 가 페이로드를 깬다는 단정이 맞는가 | **노트가 옳음.** 로컬 bash 실행 결과 `SimonASimonA…`. 정정 형태는 `Simon`+`A`×15+`/bin/sh`+NUL = 28바이트로 정확 |
| 스크린샷에서 전사한 `['id_rsa', 'authorized_keys', 'id_rsa.pub']` 가 화면과 일치하는가 | **일치.** `PG-Covfefe-ssh-listing.png` 직접 열람으로 문자열 확인. 길이 43이 gobuster `[Size: 43]` 과 일치해 교차 검증 성립 |

### 5. 총괄 판단이 필요한 것

- **여러 노트가 상속한 표현** — 「이 절은 이 박스의 실제 재현」
  - 소재 — `BlackGate.md:107` · `Covfefe.md:154` · `Interface.md:196` · `Nagoya.md:202` 넷
  - A-2-1「문서가 자기 자신을 서술하지 말 것」에 저촉. 단일 노트 판단으로 고칠 사안 밖이라 미수정
  - 표기도 갈림 — Covfefe·Interface 두 곳만 `<…>` 꺾쇠 부착
- **색인 갱신 필요** — 프론트매터·nmap raw 출력은 무변경이라 파서 입력 동일. `refresh.ps1` 은 공유 인프라라 미실행
- **문체 이관 0건** — `pg-doc-reviewer` 로 넘길 건 부재
