# Cockpit — 노트 개작 감사 기록

작업일 2026-08-20 · 대상 `03. PG\Cockpit.md` (148행 → 655행) · 백업 `03. PG\Cockpit.md.bak`

## 1. 판정 — 완료 2/2

`_STATUS.md` 는 Cockpit 을 **미착수 `0/2` (노트O)** 로 기록하고 있었다. **틀렸다.** 두 플래그 모두 스크린샷 실측이 있다.

| 플래그 | 값 | 증거 |
|---|---|---|
| `/home/james/local.txt` | `e4461b92c770db29bb95ae433e3f73ac` | `파일보관/Pasted image 20260626111320.png` |
| `/root/proof.txt` | `fc0dcc22a87ad79ebf61dcece9b4ce0c` | `파일보관/Pasted image 20260626135233.png` |

두 번째 화면에는 `whoami` → `root`, `cd /root`, `ls` → `flag2.txt proof.txt snap`, `cat proof.txt` 가 한 화면에 있다. `/root/flag2.txt` 의 `RWFzdGdVVyRWdn` 는 제출 대상이 아닌 별도 파일이고 정체는 확인하지 않았다.

원본 노트(6.5KB)도 이미 두 플래그 경로를 담고 있었다 — 노트가 얇았을 뿐 미착수였던 적이 없다.

## 2. 증거원별 수확

### Kali `~/PG/Cockpit/` (6개 파일)

| 파일 | mtime | 무엇을 알려줬나 |
|---|---|---|
| `nmap.log` | 06-25 10:23:32 | 22/80/9090. 9090 = `Cockpit web service 198 - 220` |
| `49390.py` | 06-25 10:41:02 | **Cockpit CMS 0.6.1 RCE** — 이름만 같은 다른 제품. 내용은 스크립트가 아니라 산문 어드바이저리 |
| `ferox-…150_10_9090….state` | 06-25 11:12:28 | 요청 66,771 / 예정 1,323,276(5%) · `wildcards_filtered: 22778` · `resources_discovered: 149` |
| `ferox.txt` | 06-25 13:36:29 | 6,997행 중 **6,833행이 200**. 9090 catch-all |
| `ferox_80.txt` | 06-25 15:32:26 | tcp/80 결과 **7건**, `login.php` 없음 |
| `ferox-…161_10….state` | 06-26 10:51:35 | 요청 41,154 / 예정 1,245,774 · `status_403s: 14` · `login.php` 발견 |

`ferox.txt` 594KB 를 통째로 읽지 않고 크기 분포·경로 접두사로 축약해 두 가지를 뽑았다.

- 200 응답의 바이트 수가 소수의 값으로 뭉친다(438×40222c, 350×30506c, …) → catch-all
- **6,833건 중 5,877건(86%)이 `/text/` 와 `/shell/` 두 디렉터리 밑**이다. `text`(워드리스트 341번), `shell`(1688번)이 200 을 받아 재귀 대상이 됐고, 그 아래에 워드리스트가 통째로 다시 뿌려졌다. 재귀 1회당 예정 요청 +132만(`total_expected: 3970062` / `total_scans: 3`)

### `~/.zsh_history` (타임스탬프 없음)

Cockpit 구간 22줄을 복원했다. `searchsploit Cockpit` → `-m 49390` → `vi 49390.txt` → `mv .txt .py` → `python` → `python2` → 인자 → `vi 49390.py` 순서, ferox 3회 명령 원문, 그리고 2일차의 유일한 Kali 명령 `base64 -d 'Y2FudHRvdWNoaGh0aGlzc0A0NTUxNTI='`.

`~/.msf4/loot` 는 비어 있고 `history` 는 오늘 날짜다 — **이 박스에 metasploit 은 쓰이지 않았다.**

`~/PG/` 전체를 `blaze`·두 IP 로 훑어 Cockpit 디렉터리 밖에 흘러간 산출물이 없는지 확인했다. 유일한 히트인 `~/PG/Astronaut/linpeas.sh` 는 **오탐**이다 — linpeas 내장 약한 비밀번호 목록의 `blazer` 에 걸렸다. **Cockpit 산출물은 자기 디렉터리 밖에 없다.**

### 볼트 `파일보관\` 스크린샷

작업일 후보 12장을 전부 열었다.

| 파일 | 판정 |
|---|---|
| `20260625135833` | **Cockpit 아님** — boroCTF 참가 증명서 |
| `20260626105312` · `105320` · `105549` · `110746` · `110833` · `111116` · `111320` · `130129` · `133613` · `135233` | Cockpit (10장) |
| `20260626142047` | **Cockpit 아님** — Gerapy 로그인 화면(다른 박스) |
| `PG-*.png` 15장 | Cockpit 것 없음 |

**1일차 0장 / 2일차 10장.** 총괄 가설대로다 — 1일차는 스캔, 2일차는 화면을 보며 손으로 파고든 날이다. 그리고 Cockpit 브라우저 터미널 안에서 친 명령은 `~/.zsh_history` 에 남지 않으므로 **셸 이후 구간은 스크린샷이 유일한 실측**이었다.

원본 노트에 없던 2장이 실질 수확이다.

- `20260626133613` — **폐기된 1차 권한상승 페이로드.** 6장 ④ 전체가 여기서 나왔다
- `20260626110746` — base64 디코딩 중간 화면(110833 과 중복이라 노트에는 넣지 않았다)

이미지 확대로 확인한 것:
- `105549` 의 사용자명 입력칸 글자는 **작은따옴표**(캐럿과 나란히 찍힌 얇은 틱). 비밀번호 칸은 비어 있다
- `105312` 의 입력은 사용자명 한 글자(맞춤법 밑줄 있음, `a` 로 보이나 단정 불가) + 비밀번호 한 글자
- `105549` 의 에러 문구 끝 `'%%"` 은 확대해도 `''` 인지 `"` 인지 구분되지 않는다 → 노트에 그대로 적고 미확정임을 명시

## 3. 노트에 새로 쓴 단정은 전부 때려봤다

| 주장 | 검증 방법 | 결과 |
|---|---|---|
| sudoers 의 `*` 가 인자 자리에서 임의 옵션을 허용한다 | Kali `/etc/sudoers.d/ztest` 에 동일 규칙 복제, `nobody` 로 `sudo -l` 매칭 + 실제 실행 | **성립.** `--checkpoint-action=exec=id` 가 `uid=0(root)` 출력. 대조군(아카이브 경로 변경)은 매칭 안 됨 |
| `--checkpoint-action=exec=` 가 기본 echo 동작을 대체한다 | 위 실행 출력에 `Write checkpoint` 줄 없음 | **성립** |
| `bash -p` 가 SUID 시 euid 를 유지한다 | `/opt/zbash` 를 4755 로 두고 `id -u; id -ru` 비교 | **성립.** `-p` 없으면 1000/1000, 있으면 **0**/1000 |
| `touch` 는 `--` 없이 `--checkpoint=1` 을 만들지 못한다 | Kali 실행 | **성립.** `touch: unrecognized option '--checkpoint=1'` |
| `login` 이 워드리스트 앞쪽에 있다 | 두 워드리스트 `grep -n -x` | **성립.** 둘 다 **53행**. `dirb/common.txt` 는 2347행 |
| `49390.txt` 는 스크립트가 아니다 | `searchsploit -p 49390` | **성립.** `File Type: ASCII text` |
| `49397.txt` 는 확장자와 달리 스크립트다 | `head -20` | **성립.** `#!/usr/bin/python3` |
| feroxbuster 필터 플래그 이름 | `feroxbuster --help` (2.13.1) | `-S`·`-N`·`-C`·`--filter-similar-to`·`-n/--no-recursion`·`-d/--depth` 확인 |
| sudoers 와일드카드 문서 근거 | `man 5 sudoers` Wildcards 절 | "zero or more characters (including white space)" · 인자에서는 슬래시도 매칭 |

⚠️ 검증용으로 Kali 에 `/etc/sudoers.d/ztest`·`/opt/zbash`·`/tmp/backup.tar.gz` 를 잠시 만들었다가 **전부 제거하고 `ls /etc/sudoers.d/` 로 원상복구를 확인**했다(원본 4개만 남음).

## 4. 반증한 것

### (a) `_STATUS.md` 의 판정

**미착수 0/2 → 완료 2/2.** 근거는 1절.

### (b) 원본 노트의 frontmatter

`tech/lin/container-escape` 태그가 붙어 있었다. **이 박스에 컨테이너는 없다.** 자동 태거가 `tar` 아카이브 관련 서술을 오인한 것으로 보인다. 제거했다.

`status: unsolved` 도 스크린샷 증거와 어긋나 `solved` 로 정정.

### (c) 내 초고가 틀렸던 것 셋

**① "세 번 실행을 시도한 뒤에야 `vi` 로 열었다"** — 내가 인용한 history 블록 3번째 줄이 `vi 49390.txt` 였다. **먼저 열어봤다.** 열고도 확장자를 바꿔 실행한 것이고, 이게 원래 초고보다 나은 교훈이다("열었다는 사실만으로는 부족하다 — 무엇을 확인할지 정해두지 않으면 눈만 스친다"). 6장 ①과 7장 6번을 다시 썼다.

**② "`/text/css` 와 `@localhost` 는 워드리스트에 없는 문자열"** — `@localhost` 는 맞지만 `css` 는 워드리스트 550번째 단어다. 링크 추출 탓으로 돌린 것이 틀렸고, 진짜 증폭기는 **`/text/`·`/shell/` 로의 재귀**였다(86%). 경로 접두사 집계로 반증했고, 이 편이 훨씬 실용적인 교훈이라 절을 통째로 교체했다.

**③ "한 글자만으로 두 행이 다 매칭되고 인증이 통과한다"** — cameron 의 저장값 `dGhpc3NjYW50dGJldG91Y2hlZGRANDU1MTUy` 에는 **소문자 `a` 가 없다**(대문자 `A` 만). 인증에는 james 행 하나면 충분하므로 주장 자체가 불필요하게 넓었다. 화면이 두 계정을 다 뿌린 것은 대시보드의 별도 조회로 보이며 `[가정]` 표시했다.

### (d) 총괄 지시 중 정정할 것

프롬프트는 2일차 산출물을 "ferox state(10:51, 8.6KB — 1일차보다 훨씬 작다. **중단됐거나 다른 워드리스트다**)" 로 추정했다. **둘 다 맞지만 인과가 반대다.** 상태파일이 작은 이유는 워드리스트 차이가 아니라 **일찍 끊었기 때문**이고, 일찍 끊은 이유는 **`login.php` 를 찾아서**다(상태파일의 `scans` 배열에 `http://192.168.161.10/login.php` 가 들어 있다). 실패의 흔적이 아니라 성공의 흔적이다.

또 프롬프트는 "9090 은 Cockpit 웹콘솔의 기본 포트다. 박스 이름과 맞는다" 를 단서로 제시했는데, **실제 침투 경로는 9090 이 아니라 tcp/80** 이었다. 9090 은 자격증명을 얻은 뒤 로그인만 한 곳이고, 9090 을 파고든 1일차는 통째로 헛수고였다.

## 5. 미확정으로 남긴 것 (`[가정]` 표시)

- **1일차 tcp/80 스캔이 `login.php` 를 놓친 이유.** 정황: 9090 을 2시간 반 두들긴 직후였고, 결과 7건이 전부 `index.html` 링크 추출분이며, 웹루트에 `blocked.html`(233바이트)이 있다 → **스캐너 IP 차단** 가설. `blocked.html` 내용을 확인한 기록이 없고 타겟도 회수돼 확정 불가
- **폐기된 1차 페이로드가 실행됐는지.** 실행됐다면 `> /etc/sudoers` 가 james 의 tar 규칙을 지웠을 텐데 16분 뒤 `sudo tar` 가 동작했다 → **실행 전에 갈아엎었다**로 추정
- **11:13→13:01 의 108분.** 산출물·스크린샷 전무. 점심 시간대와 겹쳐 자리를 비운 것으로 보이나 근거 없음
- **SSH 대체 경로.** Cockpit 이 PAM 인증이므로 같은 비밀번호로 `ssh james@` 가 됐을 것이나, **실제 시도 기록은 없다**
- **로그인 화면 입력 문자.** `a` 로 보이지만 이미지에서 단정 불가
- **에러 문구 끝 `'%%"`.** `''` 인지 `"` 인지 구분 불가

## 6. 파이프라인

- `tech/lin/container-escape` 제거, 실제 사용 기법 9개로 재구성 + `manual_tags: true`
- CVE 번호를 본문에서 일절 쓰지 않았다(EDB ID 만 사용) → `manual_cves` 불필요. `_INDEX/04. CVE 색인.md` 에 Cockpit 유입 **0건** 확인
- `python extract.py` 정상 → `refresh.ps1` 실행. **갱신 337 / 건너뜀 3 / 총 340** (정상 범위)
- 상호 링크: `Crane.md` 관련 노트에 `[[Cockpit]]` 1행 추가(`Zipper.md` 는 이미 링크 보유). 그 외 스코프 밖 파일 미수정
- `_STATUS.md` **미수정** (지시대로)
