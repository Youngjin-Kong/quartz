---
tags:
  - type/theory
  - platform/pen200
type: theory
platform: pen200
source: pen-200.pdf
section: "18.1.3"
---

# 18.1.3 자동화 열거

> **Automated Enumeration** · PEN-200 §18.1.3

수동으로 모을 정보량이 많아 시간이 오래 걸리므로 스크립트로 자동화함. 초기 기준선(baseline)을 잡는 용도로 UNIX 계열에는 `unix-privesc-check` 를 씀 — Kali 에 `/usr/bin/unix-privesc-check` 로 기본 설치돼 있는 Bash 스크립트이며, 로컬 권한 상승에 악용 가능한 시스템 오설정을 여러 항목으로 점검함. 인자 없이 실행하면 사용법이 나옴.

```
kali@kali:~$ unix-privesc-check
unix-privesc-check v1.4 ( http://pentestmonkey.net/tools/unix-privesc-check )
Usage: unix-privesc-check { standard | detailed }
"standard" mode: Speed-optimised check of lots of security settings.
"detailed" mode: Same as standard mode, but also checks perms of open file
                 handles and called files (e.g. parsed from shell scripts,
                 linked .so files).  This mode is slow and prone to false
                 positives but might help you find more subtle flaws in 3rd
                 party programs.
This script checks file permissions and other settings that could allow
local users to escalate privileges.
...
```

모드는 둘임:

- **standard** — 속도 최적화. 다수의 보안 설정을 점검하며 오탐이 적음
- **detailed** — standard 에 더해 열린 파일 핸들과 호출되는 파일(셸 스크립트에서 파싱한 것, 링크된 `.so`)의 권한까지 확인. **느리고 오탐이 많지만** 서드파티 프로그램의 미묘한 결함을 더 찾을 수 있음

대상에 스크립트를 전송한 뒤 standard 모드로 돌리고 출력 전체를 파일로 리다이렉트함.

```
joe@debian-privesc:~$ ./unix-privesc-check standard > output.txt
```

점검 항목 중 하나가 쓰기 가능한 설정 파일 탐지임:

```
Checking for writable config files
############################################
    Checking if anyone except root can change /etc/passwd
WARNING: /etc/passwd is a critical config file. World write is set for /etc/passwd
    Checking if anyone except root can change /etc/group
    Checking if anyone except root can change /etc/fstab
    Checking if anyone except root can change /etc/profile
    Checking if anyone except root can change /etc/sudoers
    Checking if anyone except root can change /etc/shadow
```

`/etc/passwd` 가 world-writable 이라는 뜻 — **누구나 계정을 추가하거나 권한을 올릴 수 있어** 그대로 권한 상승으로 이어지는 중대 결함임.

Linux 권한 상승 정보 수집용 도구로는 이 외에 LinEnum, LinPeas 가 꾸준히 개발돼 옴.

⚠️ **자동화가 수동 점검을 대체하지 못함.** 시스템마다 사정이 다르고, 일회성으로 손댄 고유한 변경은 이런 도구의 체크리스트에 없어 그냥 지나감. 앞 절의 수동 확인을 함께 돌려야 함.
