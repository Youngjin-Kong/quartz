---
tags:
  - type/theory
  - platform/pen200
type: theory
platform: pen200
source: pen-200.pdf
section: "7.3.2"
---

# 7.3.2 NSE 스크립트 다루기

> **Working with NSE Scripts** · PEN-200 §7.3.2

vulners 스크립트는 탐지된 버전에 매핑된 CVE 를 전부 훑어주므로, 특정 CVE 하나의 존재 여부만 네트워크 범위로 확인하려는 상황에는 검토할 정보량이 과함. 최신 취약점 대부분은 전용 NSE 스크립트를 직접 들여와 쓰는 편이 나음. 스크립트를 찾을 때는 검색 엔진에 CVE 번호와 nse 를 함께 넣음(예: `CVE-2021-41773 nse`) — 점검 스크립트를 올려둔 GitHub 페이지가 상위 결과로 나옴.

내려받은 스크립트는 다른 NSE 스크립트의 명명 규칙에 맞춰 `/usr/share/nmap/scripts/` 아래에 저장하고, 쓰기 전에 `--script-updatedb` 로 script.db 를 갱신해야 함.

```
kali@kali:~$ sudo cp /home/kali/Downloads/http-vuln-cve-2021-41773.nse
/usr/share/nmap/scripts/http-vuln-cve2021-41773.nse
kali@kali:~$ sudo nmap --script-updatedb
[sudo] password for kali:
Starting Nmap 7.92 ( https://nmap.org )
NSE: Updating rule database.
NSE: Script Database updated successfully.
Nmap done: 0 IP addresses (0 hosts up) scanned in 0.54 seconds
```

*스크립트를 스크립트 디렉터리에 복사한 뒤 script.db 갱신*

실행할 때는 스크립트 이름·대상·포트를 지정하고 서비스 탐지도 함께 켬.

```
kali@kali:~$ sudo nmap -sV -p 443 --script "http-vuln-cve2021-41773" 192.168.50.124
Starting Nmap 7.92 ( https://nmap.org )
Host is up (0.00069s latency).
PORT    STATE SERVICE VERSION
443/tcp open  http    Apache httpd 2.4.49 ((Unix))
| http-vuln-cve2021-41773:
|   VULNERABLE:
|   Path traversal and file disclosure vulnerability in Apache HTTP Server 2.4.49
|     State: VULNERABLE
|               A flaw was found in a change made to path normalization in Apache HTTP
Server 2.4.49. An attacker could use a path traversal attack to map URLs to files
outside the expected document root. If files outside of the document root are not
protected by "require all denied" these requests can succeed. Additionally this flaw
could leak the source of interpreted files like CGI scripts. This issue is known to be
exploited in the wild. This issue only affects Apache 2.4.49 and not earlier versions.
|
|     Disclosure date: 2021-10-05
|     Check results:
|
|         Verify arbitrary file read: https://192.168.50.124:443/cgi-
bin/.%2e/%2e%2e/%2e%2e/%2e%2e/etc/passwd
...
Nmap done: 1 IP address (1 host up) scanned in 6.86 seconds
```

*Apache 2.4.49 를 대상으로 한 CVE-2021-41773 점검 — 취약 판정과 근거가 함께 출력됨*

출력은 취약 여부만이 아니라 배경 설명과 검증에 쓴 요청 경로까지 제시함. Nmap 은 통상적 의미의 취약점 스캐너는 아니지만, NSE 로 경량 취약점 스캔이 가능함 — 정식 스캐너를 쓸 수 없는 환경이거나 다른 도구의 탐지 결과를 확인하고 싶을 때 유용함.

다만 다른 취약점 스캐너와 같은 고려사항이 그대로 따라붙음. 스크립트 카테고리로 intrusive 인지 safe 인지는 가늠할 수 있으나, NSE 스크립트 자체가 악성 코드를 담고 있어 공격자에게 내 시스템 전체를 내줄 수 있음. 그래서 가져온 스크립트가 필요한 기능을 하는지와 안전한지를 둘 다 확인한 뒤 써야 함.
